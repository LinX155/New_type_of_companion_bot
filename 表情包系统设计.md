# 表情包系统设计

## 0. 文档状态

本文档是表情包系统的专项设计入口，当前 1.0 方案已收敛，可进入开发阶段。

已完成初步参考调研：

- `anka-afk/astrbot_plugin_meme_manager`
- `kawayiYokami/astrbot_plugin_angel_smile`

调研源码已放入：

```text
.research/astrbot_plugin_meme_manager
.research/astrbot_plugin_angel_smile
```

本文档保留已确认的 1.0 设计、边界和后置项；不再维护开发前待讨论清单。

## 1. 表情包系统定位

表情包系统不是装饰模块，而是轻交互模块。

它要解决的问题：

- 降低每次都必须生成文字回复的压力。
- 让 `REACT` 成为真实可见动作，而不是只有 emoji 占位。
- 让角色可以用更像真实聊天的方式表达态度、接话、吐槽、安抚、围观、犯困、卖萌或轻轻拒绝。
- 支持用户发送表情包时，系统能理解、吸收风格，并在合适边界内“偷表情包”。

当前设计框架中已经确认：

- `REACT` 必须存在。
- `REACT.items` 可以包含普通文本、本地表情图资源标记或 `search_meme` 内部检索项，但必须至少包含一个 `meme` 或 `search_meme`；emoji 作为普通 `text` item，单独 emoji 不构成 `REACT`。
- 混合“文字 + 表情包 + 文字”不使用多 action，优先使用 `REACT.items`，因为它表示表情参与型回应。
- 平台一定需要支持表情包系统。
- 表情包系统是当前确定的独立模块，检索、选图、发送格式和偷表情边界都以本文为准。

## 2. 参考项目结论

### 2.1 meme_manager

`astrbot_plugin_meme_manager` 是完整表情包管理系统。

它的核心结构：

```text
memes_data.json
memes/<category>/<image>
```

含义：

- `memes_data.json`：分类名到使用场景描述的映射。
- `memes/<category>/`：该分类下的图片资源目录。

可借鉴点：

- 分类目录加描述文件的结构简单、可读、可手动维护。
- WebUI 管理界面支持添加、删除、移动、复制、批量操作和预览。
- 聊天命令可以进入“上传表情到某分类”的短期状态。
- 默认表情包可以首次初始化，也可以手动恢复。
- 上传时做同分类内精确 hash 去重。
- 可选图床同步可以作为后期扩展，不必进入第一版。
- 发送层支持两种方式：图文混合发送，或文本发送后再补发表情图。

不直接照搬点：

- 它依赖 LLM 在普通文本中输出 `&&happy&&` 这类标记，再由发送阶段清理。
- 它的宽松匹配会解析 `[tag]`、`(tag)`、重复词和裸词，容易误删普通聊天内容。
- 它会把表情 prompt 注入人格或请求 prompt，不适合我们已经确定的 `action + items` 架构。

### 2.2 angel_smile

`astrbot_plugin_angel_smile` 更聚焦“偷表情包”。

它的核心结构同样是：

```text
memes_data.json
memes/<category>/<image>
```

但它的产品思路不同：

- 默认只提供分类和描述，不内置大量素材。
- 目录分为“可用表情”和“暂不可用表情”。
- LLM 只能使用已有素材的分类。
- 当看到合适图片时，LLM 可以调用 `steal_meme_to_smile` 工具保存图片。

可借鉴点：

- “暂不可用分类”很适合引导模型补齐素材库。
- 偷表情是内部工具调用，不应该主动向用户发送“已偷取”提示。
- 工具参数明确。参考项目包含 `description`，但我们的 MVP 收敛为 `image_ref`、`category`、`save_name`，由 `save_name` 承担描述职责。
- 支持本地路径、`file:///` 和 URL。
- 使用路径白名单，避免模型乱读任意文件。
- 使用真实图片格式检测，避免上游图片后缀不可信。
- 使用 dHash 近似去重，避免重复保存压缩、缩放或轻微改动后的同一张表情。
- 使用写锁避免并发保存冲突。

需要补足点：

- 它不负责自动判断什么时候该偷。
- 它不做隐私图片、截图、二维码、证件照等风险过滤。
- 它没有管理界面。
- 它不处理用户授权、撤销、黑名单和风格学习策略。

## 3. 当前设计倾向

### 3.0 第一版目标：分类召回 + 语义文件名精确选图

第一版目标不是单纯“能发”，也不是只做到粗粒度 `meme:<category>` 随机发图。

当前确认：

```text
第一版目标 = 分类召回 + 语义文件名检索 + 精确表情发送
```

1.0 验证范围限定在 WebUI 模拟对话中跑通发表情；真实 QQ/微信发图和真实平台偷表情动作放到平台接入阶段。

含义：

- LLM 第一轮负责判断当前语境适合哪个表情分类，并在 `items` 中给出一个或多个 `search_meme` 检索项。
- 系统批量拦截 `{"search_meme":"<category>:<keywords>"}`，不向用户发送。
- 内部检索只在该分类目录下按语义文件名做简单匹配，返回少量候选 ID。
- LLM 在拿到全部候选数据后继续生成最终结果，继承上下文记录并命中 KV CACHE。
- 转发层只有在 `{"meme":"<file_stem>"}` 精确命中本地文件时才在 WebUI 渲染图片。

示例：

```json
{
  "action": "REACT",
  "items": [
    {"search_meme": "helpless:cry tired"}
  ]
}
```

内部返回候选后，第二轮输出：

```json
{
  "action": "REACT",
  "items": [
    {"text": "啊这也太累了"},
    {"meme": "helpless_miku_cry_floor"},
    {"text": "你先别急着扛。"}
  ]
}
```

这个目标可以优先验证：

- `REACT` 是否能替代一部分文字回复。
- 模型能否根据语境选对表情大类。
- 模型能否在候选中选对具体表情。
- 转发层能否稳定把精确表情 ID 转换成平台可见图片。
- 表情包行为是否真的让聊天更自然，而不是变成噪音。

第一版暂不追求：

- 向量检索表情。
- 长描述检索或 embedding 检索。
- 完整单图 metadata。
- 完整学习用户表情风格。

基础降级规则：

- 分类不存在：不发送该表情，允许降级为 `WAIT` 或 `LIGHT_ACK`。
- 分类为空：允许降级为短文本或普通 emoji 文本。
- 精确表情 ID 不存在：不发送图片，允许重新检索一次或降级。
- 图片发送失败：不向用户暴露内部错误，必要时补一个自然短句。

### 3.1 不采用文本内标签作为主协议

我们的系统已经有统一 JSON：

```json
{
  "action": "REACT",
  "items": [
    {"search_meme": "amused:teasing laugh"}
  ]
}
```

因此不建议让 LLM 在普通聊天文本里写 `&&happy&&` 或 `:happy:`，再由发送层清理。

更适合的方向：

```text
LLM 输出 action + items
items 中的 search_meme 由系统拦截
items 中的 meme 精确 ID 由转发层发送为表情图
```

这样可以避免：

- 表情标记污染聊天文本。
- 清理规则误删普通括号内容。
- 同一条回复里同时混杂文本协议和动作协议。

### 3.2 表情资源采用分类目录加轻量索引

第一版贴近参考项目，不做完整单图 metadata 系统，只做有限增强：

```text
memes/
  memes_data.json
  image_dhash_index.json
  filtered.json
  assets/
    amused/
      xxx.png
    distress/
      xxx.gif
    observing/
      xxx.webp
```

其中：

- `memes_data.json` 保存分类描述，参考两个项目的分类描述文件。
- `assets/<category>/` 保存正式可用表情。
- `image_dhash_index.json` 保存 `file_path -> dhash`，只服务近似去重，参考 `angel_smile`。
- `filtered.json` 保存技术失败或不再重复处理的 hash 和原因，不保存原图。

1.0 初始化时就创建完整目录骨架：

- 创建 18 个固定分类目录，即使暂时没有图片也保留空目录。
- 初始化 `memes_data.json`，写入 18 个固定分类和中文描述。
- 初始化 `image_dhash_index.json` 为 `{}`，第一阶段 WebUI 发表情验证可以暂不使用。
- 初始化 `filtered.json` 为 `[]`，第一阶段只用于 WebUI 展示空统计；后续真实偷表情接入后再写入技术失败记录。

第一版不建立 `assets.json` 或每张图片旁边的 `.json` sidecar。单图的主要身份就是相对路径：

```text
assets/<category>/<filename>
```

具体选图依赖语义化文件名，而不是长描述索引。偷表情入库时，LLM 必须生成稳定、可检索的 `save_name`，文件 stem 同时作为发送 ID：

```text
<category>_<subject>_<expression_or_action>_<optional_scene_or_text>
```

示例：

```text
helpless_miku_cry_floor.webp        -> meme:helpless_miku_cry_floor
distress_bunny_big_cry.gif          -> meme:distress_bunny_big_cry
observing_cat_peeking_door.webp     -> meme:observing_cat_peeking_door
overload_miku_brain_shutdown.png    -> meme:overload_miku_brain_shutdown
smiling_kirby_soft_smile.webp       -> meme:smiling_kirby_soft_smile
```

语义文件名规则：

- `save_name` 只传文件 stem，不带扩展名；真实扩展名由工具识别后追加。
- `save_name` 必须以 `category` 开头，并且前缀和工具参数的 `category` 一致。
- 只允许小写英文、数字和下划线；空格、短横线等非法字符归一化为下划线。
- 推荐 3-6 个语义 token：分类、主体、表情/动作、可选场景或图中文字。
- 主体优先使用视觉可确认对象；不能确定 IP/角色名时，不猜，使用 `anime_girl`、`cat`、`dog`、`white_bear` 这类泛化主体。
- 图中文字只有在强影响使用场景时才压缩进文件名，格式为 `text_<pinyin_or_short_english>`。
- 文件名冲突由工具追加 `_2`、`_3` 或时间戳处理，不要求 LLM 自己解决。

`search_meme` 第一版只做分类过滤和文件名 token 匹配，不读长描述、不做 embedding、不做复杂语义检索。

后续如果要做风格学习、权重、用户偏好、精细选图，再扩展单图 metadata。

### 3.3 初始分类体系固定为 angel_smile 18 类

第一版直接采用 `angel_smile` 的默认 18 个分类作为固定分类集合，不重新发明分类体系，也不在运行时自动扩类。

固定分类：

```text
amused        感到有趣、被逗乐、调侃
distress      悲伤、委屈、挫败、请求怜悯
observing     关注、好奇、围观、暗中观察
disdain       反感、鄙视、嫌弃、下头
overload      疲惫、卡顿、过载、处于困境
surprised     意外、震惊、不可思议
resting       休息、睡觉、准备结束对话
smiling       礼貌微笑、满意、心情愉悦
helpless      无奈、无语、感慨、摆烂
confused      困惑、疑问、装作不知情
affection     喜爱、好感、心动、示爱
praise        认可、点赞、鼓励、庆祝
intimidating  压迫感、严厉警告、不满凝视
bashful       害羞、局促、不好意思
angry         愤怒、激烈反对、严辞拒绝
energetic     兴奋、活力、搞怪、俏皮提示
interaction   社交行为、拜年、送礼、分享等互动
miscellaneous 其他难以归类或多重含义
```

命名规则：

- 分类 ID 使用英文，例如 `helpless`、`observing`。
- WebUI 和 prompt 里展示中文名、中文描述和使用边界。
- `category` 字段只能使用下面 18 个英文分类 ID。
- `search_meme` 值使用 `<category>:<keywords>`，其中 category 使用英文分类 ID 作为召回范围。
- 最终发送使用精确 file stem，例如 `{"meme":"helpless_miku_cry_floor"}`。

层级规则：

- 第一版不做一级/二级分类树。
- 细节差异第一版先不建正式字段；后续需要精细选图时再放进单图 metadata。
- “情绪类”“互动类”“吐槽类”“安抚类”“结束对话类”先不作为正式层级，只作为描述和标签。

入库与发送规则：

- 偷表情入库时必须从 18 个固定分类中选择。
- 第一版不允许 LLM 自动新建分类；没有合适分类时使用最接近分类或 `miscellaneous`。
- `miscellaneous` 可以作为兜底入库分类，但发送时不优先使用，避免模型偷懒全选“其他”。
- 发送时只允许选择已有且有素材的分类。

### 3.4 偷表情基础机制定稿：融合 angel_smile

当前确认：不设置人工审核队列，不要求用户逐张确认、拒绝或改分类。

基础机制直接参考 `angel_smile`，不重新设计人工审核队列，也不要求用户逐张确认。和 `angel_smile` 的差异是：本项目不是 agent 工具产品，不让主聊天 LLM 显式调用偷表情工具；平台表情事件进入后台 `MediaJobQueue` 后，由系统静默完成分析和入库。

分工边界：

```text
angel_smile 机制负责：怎么校验、怎么去重、怎么入库
我们的框架负责：什么时候把表情事件交给后台队列、主聊天怎么轻量回应、怎么发送、怎么和聊天状态/记忆/WebUI 结合
```

保留 `angel_smile` 的核心工程底线：

- 图片事件或图片引用先落到允许目录。
- 后台多模态分析判断是否值得偷。
- 工具接收 `image_ref/category/save_name` 等参数。
- 工具负责路径/URL 解析、格式读取、白名单、去重、并发写入保护。
- 成功后静默保存到本地表情库，不主动打断用户。

我们只做框架适配：

- 存储结构改成 `memes_data.json` + `assets/<category>`，并增加 `image_dhash_index.json`、`filtered.json` 这两个轻量辅助文件。
- 入库时重点保留多模态 LLM 生成的语义化 `save_name`，详细分析和保存状态只进入 debug log。
- 发送侧走 `{"search_meme":"<category>:<keywords>"}` 内部检索，再由第二轮输出 `{"meme":"<file_stem>"}` 精确发送。
- WebUI 只做观察和轻管理，不进入偷表情主链路。

原因：

- 偷表情应该是自然无感的行为，不应该把用户拉进后台运维流程。
- WebUI 可以后续做，但第一版 MVP 不应依赖 WebUI 才能跑通。
- 参考项目 `angel_smile` 的方向也是工具静默入库，不主动打断用户。
- 不再设计人工候选池。参考 `angel_smile`，平台/框架会把图片事件或图片引用作为元信息提供给工具链；本项目由后台分析器决定是否入库，而不是让主聊天模型把工具调用暴露出来。

第一版流程：

```text
用户发表情包 / 贴纸，平台 adapter 生成 MediaRef
  ↓
主聊天先收到 sticker_pending，轻量回应，不等待偷表情
  ↓
MediaJobQueue 后台下载图片并调用多模态分析器
  ↓
后台判断是否值得偷，并生成 category + save_name
  ↓
保存工具做技术校验：路径/URL、格式可读、白名单、去重
  ↓
按语义化 save_name 保存到 assets/<category>
```

也就是说，当前聊天不等待偷表情链路；后台分析、命名、去重和保存都静默完成。普通照片等不适合入库的图片会被后台跳过，只写 debug，不打扰用户。

隐私边界先放在工具说明和系统提示词里：

- 不要保存普通照片、真人自拍、聊天截图、二维码、付款码、账号信息、证件等明显隐私图片。
- 不确定时 `should_steal=false`，不保存。

第一版不把“安全或风格风险”“质量或格式不合适”作为入库拒绝标准，以保留更高自由度。格式问题按参考项目的后缀白名单和文件头扩展名修正处理，不把画质或风格作为拒绝条件。

### 3.5 偷表情使用多模态生成语义文件名

“发送时轻量检索”和“入库时多模态细分析”不冲突。

当前确认：

```text
items.content 保持简单
语义文件名承担描述职责
```

分工：

```text
偷表情入库：多模态 LLM 生成 category + 语义化 save_name
发送表情时：对话 LLM 先用 search_meme item 召回候选，再选择精确 meme ID
```

也就是说，多模态能力优先沉淀到文件名里。文件名既是轻量语义索引，也是最终发送 ID。第一版不把它扩展成长描述索引或完整资产管理字段。

偷表情工具建议参数：

```json
{
  "image_ref": "file:///path/to/image.webp",
  "category": "helpless",
  "save_name": "helpless_miku_cry_floor"
}
```

`save_name` 是必填语义字段，应尽量压缩多模态分析结果：

- 必须以分类 ID 开头。
- 不带文件扩展名。
- 使用小写英文、数字和下划线。
- 包含主体、表情/动作、可选场景或图中文字。
- 推荐 3-6 个语义 token，不写长句，不写中文，不写平台路径。
- 不确定具体 IP 或角色名时不要猜，改用 `anime_girl`、`cat`、`dog`、`white_bear` 等视觉主体。

工具保存图片后，文件名就是主要语义载体。第一版检索只读取文件名，不读取复杂单图描述，也不维护入库日志。

`visible_text`、`secondary_tags`、`style_tags`、`usage_scenarios`、`intensity` 等字段先不进入 MVP。若图中文字很关键，压缩进 `save_name`，例如 `helpless_text_lei_le`。

当前工程边界：

- 已新增无副作用分析器 `MemeStealAnalyzer`，负责把图片交给多模态 LLM 分析，并返回 `should_steal`、`category`、`save_name`、`keywords`、`reason`、`safety`。
- 已新增调试接口 `POST /api/memes/steal/analyze`，输入 `image_ref` 和可选 `context_text`，只返回命名建议，不保存文件、不写入表情库。
- `image_ref` 当前支持 `http(s)` URL、`file:///`、Windows 本地绝对路径、相对路径和 `data:image/*`。
- 本地图片必须位于白名单目录：`memes/assets`、`memes/inbox`、`memes/tmp` 或 `data/onebot_media`；未来 OneBot 下载入口应把 `image/mface` 先落到这些目录之一，再交给分析器。
- 本地图片会被转为 data URL 交给 LLM；当前做路径白名单、后缀白名单、文件大小限制和文件头 MIME 识别。
- 命名结果会被后端二次归一化：固定分类校验、`save_name` 小写英文数字下划线、自动补分类前缀、剔除 `generic/random/image/sticker/meme` 等空泛 token。
- 已新增有副作用的保存工具 `MemeStealSaver`，负责白名单校验、真实文件头识别、dHash 近似去重、写锁、保存到 `memes/assets` 和索引同步。
- 已新增 OneBot 媒体下载入口和后台 `MediaJobQueue`：主聊天只收到 pending 提示，下载、视觉理解、详细偷表情分析、保存和索引同步都在后台执行，不阻塞当前聊天。

### 3.5.1 `steal_meme` 工具技术底线

第一版按参考项目的工程约束实现，不扩展成内容审核系统。

核心原则：

```text
不限制表情风格
只限制工具乱读、乱写、重复写、写坏索引
```

工具底线：

- `image_ref` 参考 `angel_smile`，支持 `http(s)` URL、`file:///` 和本地路径，先解析成可访问的本地文件。
- 本地路径必须存在、是文件，并落在允许根目录内；允许根目录参考 `angel_smile` 的路径白名单做法。
- 入口按后缀白名单放行，参考 `angel_smile`：`.jpg`、`.jpeg`、`.png`、`.gif`、`.webp`、`.bmp`。
- 保存时读取文件前 32 字节识别真实扩展名，参考 `angel_smile` 修正上游把图片统一改成 `.jpg` 的问题。
- 真实扩展名识别范围参考 `angel_smile`：`png`、`jpg`、`gif`、`webp`、`bmp`、`tiff`、`ico`、`heic`、`avif`；识别失败时沿用原后缀或 `.jpg`。
- `category` 和 `save_name` 只做参考项目已有的归一化和安全文件名处理；固定 18 类、语义文件名规则属于前面的分类和协议设计，不在这一工具底线里额外扩展。
- 保存前做 dHash 近似去重，参考 `angel_smile` 默认阈值 `8`。
- 命中近似重复时不再保存新文件，可返回已存在的 matched file 信息给内部工具结果，但不主动向用户解释。
- 写入图片和更新 `image_dhash_index.json` 时加写锁，避免并发保存导致文件名冲突或索引损坏。
- 目标文件名冲突时追加时间戳，避免覆盖已有文件。
- 工具结果返回内部调用方；不记录来源平台、来源用户、来源时间，不做独立偷表情日志。

第一版偷表情工具不新增内容审核、独立图片安全模型、OCR、人工 rejected 队列或偷取日志。

推荐流程：

```text
用户发图/表情
  ↓
图片事件 / 图片引用进入 LLM 上下文
  ↓
LLM 判断值得偷
  ↓
LLM 调用 steal_meme(image_ref, category, save_name)
  ↓
工具做路径/URL解析、格式校验、去重
  ↓
保存图片，并更新 dHash 索引
```

### 3.6 WebUI 表情包 Tab 定位

WebUI 可以有表情包 Tab，但优先级下压，不作为偷表情链路的阻塞点。

当前确认：

```text
WebUI 表情包 Tab = 观察和轻管理
不是 pending 审核台
```

偷表情链路不依赖用户打开 WebUI。表情包 Tab 主要回答：

- 现在有哪些分类和图片？
- 文件名语义是否清楚？
- 能不能删掉？
- 后续接入真实偷表情后，能不能关闭偷表情？
- 每个分类大概有多少表情？

MVP 可做：

- 偷表情开关后置到真实平台偷表情接入阶段。
- 分类数、表情总数、数据目录。
- 分类列表：分类名、描述、数量。
- 分类下图片缩略图预览。
- 删除某张表情。
- 清空某个分类。
- 过滤记录数量和原因统计，不展示被过滤原图。

MVP 暂不做：

- pending 审核。
- 人工打标。
- 逐张编辑 metadata。
- 批量改分类。
- 拖拽移动。
- 复杂权重调整。
- 云同步。
- 多条件搜索。

删除、清空等破坏性操作需要二次确认。关闭偷表情等行为改变操作后置到真实平台偷表情接入阶段。

### 3.7 REACT 是动作，不是文本

`REACT` 必须产生可见表情内容。它可以是纯表情，也可以是“文字 + 表情包 + 文字”的混合回应，但必须至少包含一个 `meme` 或 `search_meme` item。emoji 作为普通 `text` item，不单独作为主协议类型。

示例：

```json
{
  "action": "REACT",
  "items": [
    {"search_meme": "observing:peek curious"}
  ]
}
```

或：

```json
{
  "action": "REACT",
  "items": [
    {"meme": "observing_cat_peeking_door"}
  ]
}
```

或：

```json
{
  "action": "REPLY",
  "items": [
    {"text": "🙂"}
  ]
}
```

转发层只需要理解 `items`，不需要把表情协议当普通聊天文本发送。`search_meme` 是内部检索动作，必须拦截；`meme` 只有精确命中文件 stem 才能发送。

如果模型在第一轮直接输出了不存在的 `{"meme":"<file_stem>"}`，系统不能静默把它当作有效表情发送。当前兜底策略是：

- 校验层检测本地表情库中是否存在该 `file_stem`。
- 若不存在，系统把“这些 meme 不存在，不能发送”作为内部 system 消息反馈给模型，允许模型同轮修正一次。
- 修正时模型如果仍想发表情，应改用 `{"search_meme":"<category>:<keywords>"}`，再进入正常检索和候选选择链路。
- 若修正失败或仍然编造不存在的 `meme`，最终丢弃无效表情 item，只保留其他有效文本或有效 meme，或降级为轻回应。

混合回复示例：

```json
{
  "action": "REACT",
  "items": [
    {"text": "等下"},
    {"meme": "confused_anime_girl_question_mark"},
    {"text": "你这句话也太怪了"}
  ]
}
```

### 3.8 发送中间链路参考 meme_manager

从模型输出到实际发送的中间链路，第一版参考 `astrbot_plugin_meme_manager` 的处理方式。

`meme_manager` 的关键链路是：

```text
on_llm_request
  注入表情目录和使用规则

on_llm_response
  从 LLM 输出中提取表情标签
  校验标签是否存在
  清理文本里的标签
  去重并限制数量

on_decorating_result
  发送前拿到消息链
  清理 Plain 文本残留标签
  根据标签到分类目录随机选图
  生成 Image 组件
  决定图文混发还是文本后补发表情

after_message_sent
  如果没有图文混发，就把 pending images 补发
  清理临时文件
```

我们参考的是它的中间处理阶段，而不是照搬“分类内随机选图”的策略。

我们的版本保留这套“响应后处理 + 发送前装饰 + 发送后补发”的中间链路，但入口协议改成结构化 `action + items`，并加入一次批量内部检索：

```text
第一轮 LLM 输出 action + items，其中一个或多个 item 为 {"search_meme":"<category>:<keywords>"}
  ↓
ReactionExtractor 批量拦截 search_meme，不转发给用户
  ↓
SearchMeme 分别在对应分类下按文件名 token 匹配，返回少量候选 ID
  ↓
LLM 拿到完整候选数据后继续输出最终 action + items，并用 {"meme":"<file_stem>"} 替换内部 search item
  ↓
MemeRenderer 精确匹配 file_stem 对应图片
  ↓
MessageDecorator 生成平台中立消息组件
  ↓
WebUI 渲染本地图片；QQ / 微信实际发送后置到平台接入阶段
```

第一版确认：

- `search_meme` 是内部检索协议，不向用户发送。
- `meme` 是最终发送协议，必须精确命中文件 stem。
- 不采用 `&&tag&&` 作为主协议，也不让模型在 `text` item 里写表情标记。
- 第一版允许模型在内部检索返回候选后选择具体图片。
- 系统不做长描述语义检索，只做分类过滤、文件名 token 匹配和精确 ID 校验。
- 不允许 LLM 直接输出本地路径。
- 一轮不设表情数量硬上限；多个表情用多个 `meme` item 表达，不支持 `meme:amused|meme:smiling` 这种拼接协议。
- 图文混发能力交给平台 Adapter 决定；不支持混发时参考 `meme_manager`，先发文本后补发表情图。
- 分类不存在、分类为空、候选为空、精确 ID 不存在或发送失败时，不向用户暴露内部错误，降级为短文本、普通 emoji 文本或静默不发。

实现时需要注意：`meme_manager` 使用插件实例上的 `found_emotions` 保存临时状态。我们的框架不能使用全局临时状态承载单轮结果，必须把提取出的 `search_meme` 请求、候选列表、选中的 `file_stem/file_path`、pending images 等放在当前消息上下文或转发任务上下文中，避免并发消息串线。

### 3.9 search_meme MVP 检索规则

`search_meme` 是内部工具，不产生用户可见消息。

输入格式：

```text
{"search_meme":"<category>:<keywords>"}
```

示例：

```json
{"search_meme":"helpless:cry tired"}
{"search_meme":"observing:peek curious"}
{"search_meme":"overload:brain shutdown"}
```

检索范围：

- 只检索 `assets/<category>/`。
- `category` 必须是固定 18 类之一。
- 不跨分类检索。
- 不读取日志或 description。
- 不做 embedding、向量检索或长描述语义检索。

匹配规则：

- 文件名使用 file stem，例如 `helpless_miku_cry_floor`。
- 文件名按 `_`、`-`、空格切分 token，并统一小写。
- keywords 同样切分 token，并统一小写。
- exact token 命中优先。
- substring 命中次之。
- 命中数量更多的候选优先。
- 同分时按稳定顺序或轻量随机返回，不引入最近使用历史。

返回规则：

- 默认返回 top 5，最多不超过 8 个候选。
- 返回给 LLM 的只包含 `file_stem`，不暴露本地路径。
- 如果关键词没有命中，但分类下有图，返回该分类下少量候选。
- 如果分类不存在或分类为空，返回空候选。
- 一轮对话允许多个 `search_meme` item，但只进入一次批量检索流程，避免循环检索。

返回示例：

```json
{
  "candidates": [
    "helpless_miku_cry_floor",
    "helpless_cat_facepalm",
    "helpless_text_lei_le"
  ]
}
```

第二轮 LLM 只能从候选中选择：

```json
{
  "action": "REACT",
  "items": [
    {"meme": "helpless_cat_facepalm"}
  ]
}
```

如果第二轮输出的 `file_stem` 不在候选中，或本地无法精确命中文件，则不发送该图片，允许降级为短文本、emoji 或静默不发。

## 4. 1.0 讨论收敛

当前已无 1.0 开发前必须继续头脑风暴的问题。

已确认补充：

- 表情包是否体现角色人格或贴合当前语境：交给 LLM 根据上下文判断，不在表情包系统里增加硬规则。
- 第一阶段需要样例素材包支持 WebUI 发表情验证；样例素材由用户自行准备。
- 云同步、备份、向量检索、用户风格学习、显式反馈、复杂平台差异和测试评估方案不作为 1.0 开发前阻塞项；后续遇到真实需求再单独开题。

当前表情协议以本文第 3 节、`产品设计.md` 和 `技术架构.md` 为准；`开发计划.md` 只记录仍未完成的工程项。
