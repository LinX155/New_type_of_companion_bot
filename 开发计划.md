# 前置任务：样例表情包素材库

- 第一阶段 WebUI 验证前，需要先准备少量样例表情包数据，用于跑通 `search_meme:<category>:<keywords>` 到 `meme:<file_stem>` 的发表情链路。
- 样例素材由用户自行准备；开发侧只要求文件按固定 18 类目录和语义化 `save_name` 命名规范放置。
- 样例库不追求覆盖全部分类，优先覆盖高频分类，每类少量图片即可。

# 第一阶段：简易的WEBUI，用于验证是否能够按照预期对话
前端需要包含：
- 对话窗口
- 大模型API配置窗口
- 模拟的"戳一戳"或者"拍一拍"按键
- SOUL.md配置模块
- 记忆线程模块时间配置（默认为每天下午一点一次定时记忆分析，晚上七点一次定时记忆分析，半夜3点半是整理线程。）
- 表情包模块
  - 1.0 只验证 WebUI 内发表情，不验证真实 QQ/微信发表情或偷表情动作。
  - WebUI 对话链路需跑通 `search_meme:<category>:<keywords>` 到 `meme:<file_stem>`，并渲染本地表情图片。
  - `search_meme` 返回候选后由同一次完整对话流程继续生成最终结果，保留上下文和 KV CACHE；不做人工确认或异步确认。
  - 表情文件名使用语义化 `save_name`：`<category>_<subject>_<expression_or_action>[_scene_or_text]`，小写英文、数字、下划线，`file_stem` 即最终 `meme` ID。
  - 初始化 `memes_data.json`、`image_dhash_index.json`、`filtered.json` 和 18 个 `assets/<category>/` 目录；两个辅助索引第一阶段可为空。
  - 后续接 `steal_meme` 时按参考项目实现工具底线：`image_ref` 路径白名单、后缀白名单、文件头修正扩展名、dHash 阈值 `8` 近似去重、写锁写入。
  - WebUI 表情包 Tab 第一版定位为观察和轻管理，不做审核台。
  - 顶部状态：表情总数、固定 18 类数量、数据目录。
  - 分类列表：18 个固定分类、中文描述、图片数量，空分类也展示，`miscellaneous` 置后。
  - 图片网格：缩略图、`file_stem`、大图预览、复制 `meme:<file_stem>`、删除单张。
  - 过滤摘要：展示 filtered 数量和原因统计，不展示被过滤原图。


后端需要包含：
- 大模型正确接入，且每次都能完整向API端传送全部上下文
- 所有状态机正常运转，能够正确对话
- SOUL.md正确注入（知道自己是谁，边界是什么）
- 记忆线程正确（知道怎么操作记忆模块和话题模块）
- 表情包系统能够正常使用（知道怎么search，知道怎么发送，search拦截不发送给用户）


# 第二阶段：探索QQ NT架构和相关项目，找到接入QQ的解决方案
参考研究项目：
- hermes agent的QQ接口
- napcat 项目  

需要解决的问题：
- 如何传输且仅传输text字段
- QQ中的"戳一戳"是怎么返回的？我们是不是能获取？

QQ 接入 TODO：
- [ ] OneBot 事件接收映射：解析私聊 `message[]` 中的 `image` / `mface` 段，转换为内部 `EventType.IMAGE` / `EventType.STICKER`，并在 `raw` 中保留 `file`、`url`、`file_id`、`summary`、`onebot_message_id` 等字段。
- [ ] 本地下载入口：为用户发来的 `image` / `mface` 建立下载到本地临时目录的入口，后续把临时文件路径作为 `image_ref` 交给已落地的 `MemeStealAnalyzer` 做多模态分析和语义化命名。
- [ ] 下载边界：下载入口先只做平台资源落地，不直接入正式表情库；正式入库仍需经过 `should_steal`、分类命名、dHash 去重、写锁和保存工具链。

# TODO / Delay 项

以下事项是前面明确标记为 delay、需要先调研参考项目后再继续定方案的内容。

# 审查归档与剩余工程 TODO

本节合并自原剩余 TODO 文档。已完成项保留为开发审查归档；未完成项继续作为后续工程 TODO。


## 自动审查意见

### P0 已完成

- [√] 混合多消息底层协议已落地：当前协议从 `action + text` 兼容式升级为 `action + items`，解决“用户连续发多条，对方只能回一条”和“文字/emoji/表情包不能混合回复”的聊天感失真。
  - 核心原则：一次决策，多条有序发送单元；不是主动消息，也不是多次调用 LLM。
  - 新主协议为 `items: [{type, content}] | null`；旧 `text: string | string[] | null` 仅作为兼容入口，内部统一归一化为 `items`。
  - `WAIT` / `END_CHAT` 无可见 `items`。
  - `REPLY` 以普通文字回复为主；混合 `text`、`emoji`、`meme`、`search_meme` 的表情参与型回应优先使用 `REACT`。
  - `LIGHT_ACK` 仍由 prompt 约束为短、轻、少，不做硬数量截断。
  - `REACT` 是表情参与型回应，可包含一个或多个 `emoji`、`meme`、`search_meme`，也可以混入普通文本；但必须至少包含一个表情相关 item。
  - 可见发送单元、表情包数量、`search_meme` 数量均不设产品硬上限；仍受模型输出、JSON 解析和平台发送能力等工程边界约束。
  - `search_meme` 同轮批量解析：系统一次性检索所有 search item，再由第二轮 LLM 输出最终 `items`，用 `meme:<file_stem>` 替换内部 search item。
  - 不由发送层按标点自动拆句；是否拆成多条、哪里插入表情，由 LLM 在同一个决策时刻决定。
  - 发送层按 `items[0] -> items[1] -> ...` 顺序发送，不额外制造固定延迟。
  - 同一组 items 绑定同一个 `job_id` 和 `snapshot_id`；每发送一个 item 前都检查 snapshot 是否仍然有效。
  - 如果发送中途出现新的用户消息、撤回、戳一戳或其他会改变 buffer 的平台事件，剩余 items 立即停止发送，旧结果按 stale/drop 处理。
  - 幂等键继续使用 `job_id + snapshot_id + send_index`，`send_index` 表示同一轮里的第几个发送单元。
  - WebUI 支持同一轮响应中连续渲染文本、emoji 和表情包，并在调试面板展示 `job_id` / `snapshot_id` / `send_index` / `item_type`。
- [√] COLD 冷启动元信息的 `last_user_message_age` 计算错误已修复：避免 `EventGate._handle_chat_message` 先覆盖 `last_user_message_at` 再创建 snapshot，导致系统提示词无法判断离线生活态下“上次用户消息距今多久”。
- [√] `ENTER_CHAT` / `END_CHAT` 状态迁移已补齐：避免 `ENTER_CHAT` 且 `text:null` 不进入 HOT、`END_CHAT` 不退回 COLD。
- [√] `MEMORY_CORE.md`、`/mem`、`/forget` 已统一为五分区和 `[来源]: 内容` 单行格式，避免污染核心记忆和系统提示词注入。
- [√] `REACT` 协议校验和可见渲染已补齐：避免把 `emoji:*`、`search_meme:*` 或 `meme:*` 等内部协议当聊天内容暴露。

### 已完成设计缺口

- [√] 主动消息模块已落地：WebUI 主动消息设定/查看入口、主动消息调度、每日上限、免打扰、未回复退避、候选 `used/expired/blocked` 状态更新，以及发送前经 EventGate 做撞车和幂等检查。
- [√] 主回复上下文已读取 `TOMORROW_TOPICS.md`。
- [√] 系统提示词已补充“记忆使用方式”边界：自然聊天中不应显式说“根据我的记忆/来源”，记忆应该改变行为而不是审计式展示。

### P2 / 轻量优化

- [ ] 凌晨整理线程 prompt 增加“共同梗 / 暗号 / 昵称 / 专属表情含义”的轻提示：不新增独立模块，不新增关系系统；只把长期稳定、自然反复出现或用户通过 `/mem` 明确要求的共同语境，克制写入 `MEMORY_CORE.md` 的“相处习惯”；长期未出现、用户否定或明显过期的内容应删除或降权；主回复使用时避免机械复读和显式炫耀记忆。
- [ ] 重构 COLD/HOT 进入规则以增加 WAIT 机会：COLD 状态下的普通可见 `REPLY` / `LIGHT_ACK` / `REACT` 先按 one-shot reply 处理，不自动进入 HOT；只有 `ENTER_CHAT`、拍一拍 / 戳一戳、或后续明确形成连续互动时才进入 HOT。同步收窄 prompt 中 `ENTER_CHAT` 的触发条件，补充 COLD 下 `WAIT` 的正向示例，避免模型把“刚看到消息”误判成“必须立刻进入热聊”。该改动涉及状态迁移、prompt 和测试，先作为 P2 重构项保留。
