# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地或关闭的阶段性任务不在本文归档；当前架构事实以 `产品设计.md`、`技术架构.md`、`表情包系统设计.md` 和 `社交软件接入调研归档.md` 为准。

## 群聊版小夏落地开发顺序

目标：群聊版小夏与现有私聊能力严格隔离，最后共享同一个启动入口。除 `群聊版小夏搭建.md` 明确要求的差异外，群聊版能力要与私聊版对齐，尤其是记忆、异步线程、调度、发送拦截、审计日志和配置入口；同时不能复用或削弱私聊 HOT / COLD、输入状态拦截、主动消息、记忆、发送拦截和表情包链路。开发顺序按“先接收观测，再读群判断，再允许发送”推进。

- [x] 第一阶段：群聊只读接入与 session 隔离。
  - [x] 解析 OneBot `message.group`，生成群聊 `ChatEvent`，但不进入现有私聊 graph，不触发任何发送。
  - [x] 新增群聊 session 规则：`qq_group_<group_id>`；`group_id` 是群 session，`sender_id/q号` 是稳定发言人索引。
  - [x] raw 事件保留 `group_id`、`sender_id/q号`、`message_id`、引用、图片、表情包等工程字段。
  - [x] 严格丢弃或隔离平台群名片/群昵称，不写入 prompt、记忆、历史、checkpoint 或用户可见输出。
  - [x] WebUI / raw log 能看到群聊事件、群 session、发送者 q号和消息类型，便于先观察真实群聊数据。
  - [x] 回归测试：私聊消息仍走 `qq_private_<user_id>`；群聊消息不会创建私聊 session，不会触发私聊回复。

- [x] 第二阶段：群聊事件缓冲与只读上下文窗口。
  - [x] 为 `qq_group_<group_id>` 建立群聊专用 buffer/window，不复用私聊输入状态检测、HOT / COLD 和 send-group gate 语义。
  - [x] buffer 内只保存工程身份索引和安全摘要；模型可见上下文只能看到 q号、记忆中已确认的 q号:昵称、消息文本和安全媒体摘要。
  - [x] 如果“群友身份”记忆中没有 q号对应昵称，模型可见窗口不提供昵称字段；回复时不使用指代词的口径在第四阶段群聊 prompt 中继续约束。
  - [x] 普通图片先只记录事件与下载状态，不默认触发视觉理解或引用回复。
  - [x] 表情包 / 贴纸只作为 meme 事件进入群聊 buffer，不让主线程看表情包图像。

- [x] 第三阶段：群聊表情包事件与偷表情后台隔离。
  - [x] 表情包后台线程持续监听，下载后先 hash / dHash 去重，再进入入库分析。
  - [x] 群聊主线程只接收极简 meme 事件：已入库返回入库名称，未入库返回 `unknown`。
  - [x] 不使用 `<meme:入库名称>` 或任何尖括号文本协议，不新增模型可输出的表情包文本 marker。
  - [x] 发送自己的表情包仍复用现有 `&&category:keywords&&` / 兼容 `||category:keywords||` / 二轮 selector / `:meme:<file_stem>` 闭环。

- [x] 第四阶段：群聊专用 prompt 与轻量状态机。
  - [x] 群聊版小夏不是任何人的女友，角色 prompt 与私聊 SOUL 隔离。
  - [x] 群聊 runtime 不使用私聊 HOT / COLD、用户输入状态检测与拦截，但保留 `WAIT`。
  - [x] 输出要求短、有趣、可发散；群友发言默认可能互不相关，不强行解释深层含义。
  - [x] 输出安全检查必须禁止 q号、平台群名片、内部 meme 事件、引用协议和其它系统字符泄露。

- [x] 第五阶段：群聊回复触发调度。
  - [x] 被 @ 后 pending 2 分钟，读取最新群聊窗口，再由模型判断是否回复以及是否引用。
  - [x] 实现随机 roll 触发：距离上次 LLM 群聊发言超过 X 分钟后 roll；当前采用加减模式，`a` 在 `0.00-10.00` 间取浮点，`a > b` 才触发读取与回复；消息密度和图片事件用 q 加上去，低活跃和表情包占比用 p 减下去。
  - [x] roll 机制参数当前未定死，开发时可以先凭经验给默认值，但必须提供统一调试入口：可以是 `set_roll.json`，也可以是在第五阶段调度模块靠前部分集中声明的配置块；用户必须能直接调整 X、随机数范围、基础阈值 b、消息数阈值 A/B、图片系数 q2、消息密度系数 q1、低活跃惩罚 p1、表情包占比阈值 o、表情包惩罚 p2 等所有变量；默认 p/q 均大于 1。
  - [x] roll 参数入口需要和 debug / raw log 对齐：每次判断都记录实际使用的参数快照，避免线上行为和当前可调配置对不上。
  - [x] 触发原因、窗口统计、roll 值、阈值、最终是否回复必须写入 debug / raw log。
  - [x] stale 规则：等待期间群聊继续刷新时，旧判断不能插队发送，必要时用最新窗口重跑。

- [x] 第六阶段：普通图片理解与引用回复。
  - [x] 普通图片只在群聊读取触发后才做视觉理解；无趣、过期或不适合插话时返回 `WAIT`。
  - [x] 如果最终回复直接针对某张图片，发送层引用原图片 `message_id`；模型不输出 `[[quote]]` 或其它引用协议。
  - [x] 如果回复主要针对群聊文本，不强行引用图片。
  - [x] 图片理解结果只作为当轮临时上下文或短期 media cache，不污染长期记忆。

- [x] 第七阶段：群聊发送层。
  - [x] OneBot client 增加 `send_group_msg` 包装，支持文本、图片、表情包和可选 reply 段。
  - [x] 群聊发送必须经过同一套用户可见输出安全分类器，额外增加 q号 / 平台昵称泄露检查。
  - [x] 增加群聊频率限制和去重，避免复读、主动参与或表情包发送造成刷屏。
  - [x] 首次上线默认只允许白名单群开启发送能力。

- [x] 第八阶段：群聊完整记忆系统（重做验收）。
  - 目标：按私聊版记忆能力做完整同构实现，只保留设计文档明确要求的群聊差异；不能用规则写入或单次 LLM 写 CORE 代替完整记忆系统。
  - 明确允许的差异：
    - 群聊长期 CORE 文件是 `memory/sessions/qq_group_<group_id>/GROUP_MEMORY.md`，不是私聊 `MEMORY_CORE.md`。
    - 群聊身份索引使用 q号；平台群名片 / 平台昵称不得进入 prompt、记忆、历史、checkpoint 或用户可见输出。
    - 群聊 `GROUP_MEMORY.md` 固定包含“群友身份 / 共同记忆 / 个人相关记忆”；个人相关记忆按 `<q号><时间><来源><记忆内容>` 结构化。
    - `/mem` 与 `/forget` 只能操作发起者本人身份或本人相关记忆，不能替别人写、删或绑定身份；共同记忆只能记录群体事实、梗、氛围和公共约定。
    - 主动消息 / TOPIC 暂时按设计文档“用户级逻辑复用”，但群聊不得读取私聊 TOPIC 去主动发群消息。
  - 当前已存在但必须复核的基础：
    - [x] 已有 `GROUP_MEMORY.md` 文件骨架与“群友身份 / 共同记忆 / 个人相关记忆”三板块。
    - [x] 已有 q号到确认昵称的 prompt 注入与群聊主回复输出防泄漏检查。
    - [x] 已有规则版 `/mem`、`/forget` 和普通文本身份观察雏形；后续必须降级为 fallback 或辅助校验，不能作为主实现。
  - 当前实现偏差必须先修正：
    - [x] 禁止继续沿用“群聊普通文本事件 -> 后台 LLM -> 直接覆盖 `GROUP_MEMORY.md`”的实现；普通聊天线程只能生成/更新当天群聊 dm，长期 CORE 必须由凌晨整理线程收束。
    - [x] `build_group_memory_analysis_messages(...)` 当前如果输出完整 `GROUP_MEMORY.md`，需要改为输出完整群聊日记忆 dm；旧的直接 CORE 输出测试要改成失败回归样例。
    - [x] `GroupMemoryManager.analyze_window_via_llm(...)` 当前如果直接写 `GROUP_MEMORY.md`，需要拆成日间 dm 写入方法；直接 CORE 写入只允许凌晨整理和通过校验后的 `/mem` / `/forget` 命令。
    - [x] `_schedule_group_memory_thread(...)` 当前如果按每条文本事件触发 CORE 更新，需要降级为观察期可选的 dm 刷新/排队入口，或完全交给 scheduler 定时日间记忆线程；不得因为群聊刷屏造成多条 LLM 并发覆盖同一记忆文件。
    - [x] 当前规则版 `observe_group_text()` 只能作为低风险身份候选辅助，不得绕过 LLM 日间记忆线程直接把普通闲聊长期化到 CORE。
    - [x] 当前已有测试中凡是断言“群聊记忆线程直接更新 `GROUP_MEMORY.md`”的用例都要改写为“先写 dm，凌晨整理后才长期化”。
  - 实施顺序：
    - [x] 8.1 先冻结/标记现状偏差：列出所有会直接写 `GROUP_MEMORY.md` 的入口，按“允许 CORE 写入 / 只允许 dm 写入 / 只允许诊断记录”三类归档。
    - [x] 8.2 先补群聊记忆文件管理层：`GROUP_MEMORY.md`、`dm/YYYY-MM-DD.md`、状态读取、清理联动、路径状态和基础模板全部集中在 `GroupMemoryManager`，不散落到 routes。
    - [x] 8.3 再补四套独立群聊记忆 prompt：日间 dm、凌晨 CORE、`/mem`、`/forget`；prompt 定义集中在 `app/llm/prompts.py`，`app/core/group_memory.py` 只负责调用、校验和落盘。
    - [x] 8.4 再改命令链路：`/mem` / `/forget` 先走硬边界预检，再按 group session 串行调用 LLM，最后做输出校验、合并保护和引用回复。
    - [x] 8.5 再接 scheduler：私聊 job 范围和群聊 job 范围分开；群聊日间记忆、群聊凌晨整理作为独立 job 类型接入。
    - [x] 8.6 再补历史抽取与审计：从可见 `ConversationEvent` 和 raw payload 组装群聊记忆 transcript，确保 q号/message_id 只作为工程字段，平台群名片永不进入模型文本。
    - [x] 8.7 再补 WebUI 状态与调试入口：能看到群聊 CORE 路径、dm 路径、最近日间记忆、最近凌晨整理、最近命令队列状态，但不新增数据库 schema。
    - [x] 8.8 最后补测试并跑回归：先跑群聊记忆相关单测，再跑 OneBot 群聊适配、发送链路、私聊记忆和全量测试。
  - 文件结构补齐：
    - [x] 为群聊 session 建立与私聊类似的日记忆目录：`memory/sessions/qq_group_<group_id>/dm/YYYY-MM-DD.md`。
    - [x] 新增群聊日记忆模板，面向群聊语义维护“今日群聊大事 / 群友身份候选 / 共同话题与梗 / q号相关近期状态”等板块。
    - [x] `GROUP_MEMORY.md` 作为群聊长期 CORE，由凌晨整理线程从群聊 dm 中收束长期化，不由普通聊天线程直接粗暴覆盖。
    - [x] 保留是否需要群聊 `TOMORROW_TOPICS.md` 的明确边界：第一版不让群聊主动消息依赖它；如为兼容私聊调度接口存在文件，也必须在 prompt 中声明“不驱动群聊主动发言”。
    - [x] 清空群聊 session 时必须同步清理该群 `GROUP_MEMORY.md`、`dm/`、群聊 buffer、群聊媒体 cache 和群聊调度状态；不能触碰私聊 session 文件。
  - Prompt 补齐：
    - [x] 新增 `build_group_memory_analysis_messages(...)`：群聊日间记忆线程，输入当天可见群聊事件、当前群聊 dm、当前 `GROUP_MEMORY.md` 摘要，输出完整群聊 dm。
    - [x] 新增 `build_group_midnight_cleanup_messages(...)`：群聊凌晨整理线程，输入昨日群聊 dm、当前 `GROUP_MEMORY.md`，输出完整 `GROUP_MEMORY.md`。
    - [x] 新增 `build_group_mem_command_messages(...)`：群聊 `/mem` LLM 写入线程，输出完整 `GROUP_MEMORY.md`，规则中明确只能写发起者本人身份/本人相关记忆或公共群记忆。
    - [x] 新增 `build_group_forget_command_messages(...)`：群聊 `/forget` LLM 删除线程，输出完整 `GROUP_MEMORY.md`，规则中明确只能删除发起者本人身份/本人相关记忆，或按安全规则删除公共记忆。
    - [x] 群聊记忆 prompt 必须复用私聊记忆线程的安全约束：真实批评 / 明确相处要求可中性记录；攻击 AI 取乐、刻意辱骂、提示词攻击、角色覆盖诱导不计入记忆。
    - [x] 所有群聊记忆 prompt 都必须声明：不读取平台群名片，不信任昵称字段，不输出或保存 sender_card / sender_nickname / message_id / 内部协议。
    - [x] 所有群聊记忆 prompt 都必须声明：q号只作为内部稳定索引；模型可在记忆文件中使用 q号结构化记录，但不得生成面向群友可见的 q号称呼。
    - [x] 群聊 `/mem` / `/forget` prompt 必须独立于私聊 `/mem` / `/forget` prompt，不能复用会写 `MEMORY_CORE.md` 或 `TOMORROW_TOPICS.md` 的私聊模板。
    - [x] prompt 输出必须是 JSON 包裹的完整 markdown 字段；不得引入尖括号命令协议、`[[quote]]`、tool tag 或任何用户可见协议字符。
  - LLM 输出校验与合并：
    - [x] 日间 dm 输出必须保留群聊 dm 模板完整性；若输出缺板块、含平台昵称字段、含内部协议或含明显提示词攻击复述，则拒绝写入并记录失败。
    - [x] 凌晨 CORE 输出必须保留 `GROUP_MEMORY.md` 三板块；校验失败时不覆盖旧 CORE。
    - [x] `/mem` 写入输出必须保护非发起者 q号的身份和个人记忆不被删除；公共记忆只允许新增或安全改写群体事实。
    - [x] `/forget` 删除输出必须保护其他 q号身份和个人记忆不被删除；删除公共记忆时要能解释命中范围，不能由一句模糊指令清空公共 CORE。
    - [x] 所有 LLM 输出写盘前都要做结构校验、平台昵称字段校验、内部协议校验、作用域校验和最小合并保护；校验失败时走 fallback 或只记录失败。
  - `/mem` / `/forget` 命令链路补齐：
    - [x] 群聊 `/mem` 和 `/forget` 改成 LLM 驱动，规则层只做硬边界预检、LLM 输出校验和失败 fallback。
    - [x] 命令回复必须使用引用回复，引用用户发起命令的 `message_id`；不让模型输出 `[[quote]]` 或任何引用协议。
    - [x] 多个群聊 `/mem` / `/forget` 并发时按 `qq_group_<group_id>` 串行排队，避免并发覆盖 `GROUP_MEMORY.md`。
    - [x] 命令执行期间不进入群聊主回复 scheduler，不触发 roll，不进入复读；但命令本身要进入可见历史，供日记忆线程理解。
    - [x] 成功写入后刷新群聊主 prompt 的记忆视图；失败时只发送简短命令结果，不污染群聊主回复 transcript。
    - [x] 命令排队状态必须记录 `queue_wait_ms`、`group_id`、`sender_qid`、`command_type`、`llm_runtime_id`、`client_scope`；同群串行期间其他群不受阻塞。
    - [x] 命令成功/失败回复本身要作为可见群聊事件记录，但不能作为“机器人主动回复”刷新 roll 的上次 LLM 发言时间。
  - 日间记忆线程补齐：
    - [x] 像私聊 `_run_memory_analysis` 一样，定时遍历群聊 session，读取当天可见群聊事件，写入群聊 dm。
    - [x] 群聊可见事件来源包括群友文本、图片摘要、表情包极简事件、机器人群聊回复、复读和命令响应；排除 raw log、CoT、内部 repair、工具结果、平台群名片。
    - [x] 观察期内即使不允许小夏发言，也必须正常运行群聊记忆线程，以满足“先累积记忆再开启对话能力”。
    - [x] 群聊图片理解只能作为低优先级辅助证据；不能单独推出身份、住址、职业、关系等长期事实。
    - [x] 表情包事件默认只作为语气和接梗信号；除非用户明确表达稳定偏好，否则不长期化。
    - [x] 日间记忆 transcript 需要带 `role`、`qid`、安全文本、事件类型、时间；`message_id` 只进入 debug/raw，不作为模型需要记住的内容。
    - [x] 如果当天没有可见群聊事件，只确保 dm 文件存在并记录 job completed，不调用 LLM。
  - 凌晨整理线程补齐：
    - [x] 像私聊 `_run_midnight_cleanup` 一样，凌晨遍历群聊 session，将昨日群聊 dm 收束进 `GROUP_MEMORY.md`。
    - [x] 长期化判断必须区分 long / recent / expired；群聊长期 CORE 只保留稳定身份、群梗、共同约定、反复出现的 q号相关事实。
    - [x] 已过期、一次性、攻击性、提示词攻击和角色覆盖诱导内容必须降权或删除。
    - [x] 凌晨整理不能写私聊 `MEMORY_CORE.md`，不能写私聊 `TOMORROW_TOPICS.md`，不能影响私聊 active message。
    - [x] 需要在 scheduler job log / raw audit 中记录每个群 session 的成功、失败、LLM debug 和写入摘要。
    - [x] 凌晨整理可以读取昨日群聊 dm 和当前 `GROUP_MEMORY.md`，不得读取私聊 `TODAY_MEMORY.md`、私聊 `TOMORROW_TOPICS.md` 或私聊 checkpoint。
    - [x] 某个群没有昨日 dm 时只记录 skipped/completed，不创建空长期条目。
  - 调度与 session 范围补齐：
    - [x] 当前 `_scheduler_session_ids()` 排除了群聊 session；需要拆成私聊 job 范围与群聊记忆 job 范围，避免把群聊塞进私聊记忆 / 私聊 checkpoint。
    - [x] 私聊 memory analysis / midnight cleanup / context checkpoint 继续只跑私聊 session。
    - [x] 群聊 memory analysis / group midnight cleanup 单独遍历 `qq_group_<group_id>`，使用 `background_batch` 或 `internal_session` scope client，不带 provider_user_id 和私聊 cache affinity。
    - [x] 单个群失败不能阻断其他群或私聊 scheduler。
    - [x] 群聊记忆 job 可以复用 ProviderRuntime 的 internal/background client，但不得把群聊内部输出写入主聊天 provider transcript。
    - [x] 观察期、白名单关闭发送、只读群状态都不能阻止群聊记忆 job 运行。
  - 历史与审计补齐：
    - [x] 群聊收到的用户事件必须落 `ConversationEvent`，但文本中不得包含平台群名片；需要保留 q号、message_id 等工程字段到 raw payload。
    - [x] 群聊机器人发送、复读、命令回复、表情包发送也要落可见 `ConversationEvent`，供群聊日记忆线程读取。
    - [x] raw/audit 增加 `group_memory_thread`、`group_memory_command`、`group_midnight_cleanup`、`queue_wait_ms`、`llm_runtime_id`、`client_scope` 等诊断字段。
    - [x] WebUI 状态页展示群聊记忆文件路径、dm 文件路径、最近一次日记忆线程、最近一次凌晨整理、最近一次 `/mem` 队列状态。
    - [x] raw/audit 中可以保留 q号、group_id、message_id 作为工程字段；任何模型输入字段、markdown 记忆字段和用户可见字段都不得包含平台群名片。
    - [x] WebUI 调试视图要能区分“规则辅助观察”“日间记忆线程”“凌晨整理线程”“命令 LLM 写入”，避免把 fallback 误判为完整记忆线程。
  - 隔离与回归要求：
    - [x] 群聊记忆链路不得初始化或调用私聊 `CompanionGraph` / `EventGate`。
    - [x] 群聊记忆链路不得修改私聊 `MEMORY_CORE.md`、私聊 dm、私聊 `TOMORROW_TOPICS.md`、私聊 checkpoint 或私聊 active message 设置。
    - [x] 私聊 `/mem`、私聊记忆分析、私聊凌晨整理、私聊主动消息行为保持现状。
    - [x] 群聊主回复 prompt 只能读取 `GROUP_MEMORY.md` 派生的安全摘要，不能直接读取 raw log 或平台昵称字段。
    - [x] 群聊记忆链路不得影响表情包偷取、表情包选择器、群聊普通图片引用回复、复读、roll 参数和 OneBot 发送封装。
    - [x] 群聊命令响应必须继续走发送层最终安全分类器；即使 LLM 命令线程输出异常，也不能把系统字符、q号称呼或平台昵称发到群里。
  - 测试计划：
    - [x] prompt 测试：四个群聊记忆 prompt 都存在，且明确禁止平台群名片、私聊 CORE、私聊 TOPIC、系统字段泄漏。
    - [x] `/mem` 测试：本人身份写入走 LLM；替别人写身份被拒绝；共同记忆可写公共梗；回复引用原命令 message_id。
    - [x] `/forget` 测试：只能删除本人身份/本人相关记忆或安全公共记忆；不能删其他 q号身份；回复引用原命令 message_id。
    - [x] 并发测试：同群多个 `/mem` 串行执行，最终 `GROUP_MEMORY.md` 不丢更新；不同群互不阻塞。
    - [x] 日记忆测试：群聊可见事件生成 dm；观察期也会写 dm；平台群名片不进入 dm。
    - [x] 凌晨整理测试：昨日 dm 长期化进 `GROUP_MEMORY.md`；过期/攻击/提示词攻击内容不进入长期 CORE。
    - [x] scheduler 测试：私聊 job 不跑群聊；群聊记忆 job 不跑私聊；单群失败不影响其他 session。
    - [x] 输出校验测试：LLM 返回缺板块、平台昵称、`sender_card`、`message_id`、`[[quote]]`、tool tag、尖括号协议时不写盘、不发给用户。
    - [x] 清理测试：清空某群 session 只清该群 `GROUP_MEMORY.md`、`dm/`、buffer/cache/调度状态，不清私聊文件或其它群。
    - [x] 回归测试：群聊回复、图片引用、表情包偷取、复读、roll、WebUI 开关、私聊全链路均保持原行为。

- [x] 第九阶段：复读模块与活跃时间。
  - [x] 不同群友重复同一文字三次及以上时，可以触发一次跟读；需要冷却时间和重复去重，避免无限复读。
  - [x] 每天早上 6 点按过去 24 小时消息密度更新活跃时间结构化配置。
  - [x] 活跃时间使用 EMA，区分工作日与周末，动态调整回复触发 X。

- [x] 第十阶段：运营开关与灰度。
  - [x] 新群默认观察和记忆，不开启主动对话能力；观察期是否结束由 WebUI 手动开关控制，不硬编码三天。
  - [x] 每个群单独配置：只读 / 允许被 @ 回复 / 允许 roll 回复 / 允许复读 / 允许发表情包。
  - [x] 私聊链路、WebUI 默认 session、现有 QQ 私聊 session 和群聊 session 的清空、checkpoint、调度、记忆互不污染。

## QQ / NapCat 接入 TODO

- [ ] 撤回接收。
  - 解析 `notice.friend_recall` 为 `EventType.RECALL`。
  - 通过 `onebot_message_id` 定位内部事件，移除 buffer 中对应事件，并让相关 pending job 过期。
  - 默认静默处理，不主动追问、不解释撤回内容。

## 后置能力

- [ ] 已读回执：`mark_private_msg_as_read` 的调用时机另行决定。
- [ ] QQ 表情回应：`set_msg_emoji_like` 后置。
- [ ] 主动戳一戳后置。
- [ ] 群聊版小夏：见“群聊版小夏落地开发顺序”。
- [ ] 语音 / 视频 / 文件理解后置。

## 底层鲁棒性剩余：阶段二观测补齐

目标：缓存健康判断已经从“完整 request 必须 append-only”切到“稳定前缀 append-only，临时尾巴可变”。剩余工作只补观测细节和回归验收，不回退到让一次性 system 消息滞留。

- [ ] 补充 ephemeral tail 观测字段。
  - `ephemeral_tail_hash`：本轮 runtime / reminder / repair / media / meme tail。
  - `ephemeral_tail_message_count`：临时尾巴消息数。
  - `ephemeral_tail_types`：记录当前尾部包含 runtime、final_reminder、hot_reminder、repair、media、meme_search 中哪些类型。
- [ ] 细化 rebuild reason 字段。
  - 将 `prefix_rebuild_reason` 细化为 `stable_prefix_rebuild_reason`。
  - 将 `full_request_prefix_rebuild_reason` 细化为 `full_request_changed_reason`。
- [ ] 明确哪些变化应该算 stable prefix 重建。
  - 主 system prompt 模板变更。
  - SOUL / MEMORY_CORE / TODAY_MEMORY / TOMORROW_TOPICS 等稳定 prompt 输入发生需要重载的变更。
  - LLM identity / model / base_url / cache_session_id 改变。
  - conversation checkpoint / compaction 插入。
  - 会话切换或 transcript reset。
- [ ] 明确哪些变化不应该算 stable prefix 重建。
  - msg_index、last_message_age、chat_status 等本轮 runtime 字段变化。
  - HOT reminder 出现或消失。
  - `FINAL_ACTION_OUTPUT_REMINDER` 内容作为当前尾部提醒加入。
  - repair / media / meme search 本轮临时尾巴变化。
- [ ] 补齐阶段二回归测试。
  - repair / meme / media tail 变化时，不触发 stable prefix rebuild。
  - LLM identity 或主 system prompt hash 变化时，触发 stable prefix rebuild。
  - checkpoint / compaction 插入时，记录明确 rebuild reason。
- [ ] 完成阶段二验收。
  - 日志能解释“为什么完整 request 不再是 append-only”，并能证明稳定前缀仍然按预期增长。
  - cache 相关告警或人工判断不再依赖完整 request append-only。
  - provider 实际 cached tokens 与 stable prefix 指标可以并排观察，便于判断真实缓存收益是否下降。
  - 如果实际 cached tokens 明显下降，再单独评估是否需要 provider-specific cache control。
