# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地或关闭的阶段性任务不在本文归档；当前架构事实以 `产品设计.md`、`技术架构.md`、`表情包系统设计.md` 和 `社交软件接入调研归档.md` 为准。

## 群聊版小夏落地开发顺序

目标：群聊版小夏与现有私聊能力严格隔离，最后共享同一个启动入口，但不能复用或削弱私聊 HOT / COLD、输入状态拦截、主动消息、记忆、发送拦截和表情包链路。开发顺序按“先接收观测，再读群判断，再允许发送”推进。

- [x] 第一阶段：群聊只读接入与 session 隔离。
  - [x] 解析 OneBot `message.group`，生成群聊 `ChatEvent`，但不进入现有私聊 graph，不触发任何发送。
  - [x] 新增群聊 session 规则：`qq_group_<group_id>`；`group_id` 是群 session，`sender_id/q号` 是稳定发言人索引。
  - [x] raw 事件保留 `group_id`、`sender_id/q号`、`message_id`、引用、图片、表情包等工程字段。
  - [x] 严格丢弃或隔离平台群名片/群昵称，不写入 prompt、记忆、历史、checkpoint 或用户可见输出。
  - [x] WebUI / raw log 能看到群聊事件、群 session、发送者 q号和消息类型，便于先观察真实群聊数据。
  - [x] 回归测试：私聊消息仍走 `qq_private_<user_id>`；群聊消息不会创建私聊 session，不会触发私聊回复。

- [x] 第二阶段：群聊事件缓冲与只读上下文窗口。
  - [x] 为 `qq_group_<group_id>` 建立群聊专用 buffer/window，不复用私聊输入状态检测、HOT / COLD 和 send-group gate 语义。
  - [x] buffer 内只保存工程身份索引和安全摘要；模型可见上下文只能看到 q号、记忆中已确认的 q号:昵称、消息文本和安全媒体摘要。
  - [x] 如果“群友身份”记忆中没有 q号对应昵称，模型可见窗口不提供昵称字段；回复时不使用指代词的口径在第四阶段群聊 prompt 中继续约束。
  - [x] 普通图片先只记录事件与下载状态，不默认触发视觉理解或引用回复。
  - [x] 表情包 / 贴纸只作为 meme 事件进入群聊 buffer，不让主线程看表情包图像。

- [x] 第三阶段：群聊表情包事件与偷表情后台隔离。
  - [x] 表情包后台线程持续监听，下载后先 hash / dHash 去重，再进入入库分析。
  - [x] 群聊主线程只接收极简 meme 事件：已入库返回入库名称，未入库返回 `unknown`。
  - [x] 不使用 `<meme:入库名称>` 或任何尖括号文本协议，不新增模型可输出的表情包文本 marker。
  - [x] 发送自己的表情包仍复用现有 `&&category:keywords&&` / 兼容 `||category:keywords||` / 二轮 selector / `:meme:<file_stem>` 闭环。

- [x] 第四阶段：群聊专用 prompt 与轻量状态机。
  - [x] 群聊版小夏不是任何人的女友，角色 prompt 与私聊 SOUL 隔离。
  - [x] 群聊 runtime 不使用私聊 HOT / COLD、用户输入状态检测与拦截，但保留 `WAIT`。
  - [x] 输出要求短、有趣、可发散；群友发言默认可能互不相关，不强行解释深层含义。
  - [x] 输出安全检查必须禁止 q号、平台群名片、内部 meme 事件、引用协议和其它系统字符泄露。

- [x] 第五阶段：群聊回复触发调度。
  - [x] 被 @ 后 pending 2 分钟，读取最新群聊窗口，再由模型判断是否回复以及是否引用。
  - [x] 实现随机 roll 触发：距离上次 LLM 群聊发言超过 X 分钟后 roll；当前采用加减模式，`a` 在 `0.00-10.00` 间取浮点，`a > b` 才触发读取与回复；消息密度和图片事件用 q 加上去，低活跃和表情包占比用 p 减下去。
  - [x] roll 机制参数当前未定死，开发时可以先凭经验给默认值，但必须提供统一调试入口：可以是 `set_roll.json`，也可以是在第五阶段调度模块靠前部分集中声明的配置块；用户必须能直接调整 X、随机数范围、基础阈值 b、消息数阈值 A/B、图片系数 q2、消息密度系数 q1、低活跃惩罚 p1、表情包占比阈值 o、表情包惩罚 p2 等所有变量；默认 p/q 均大于 1。
  - [x] roll 参数入口需要和 debug / raw log 对齐：每次判断都记录实际使用的参数快照，避免线上行为和当前可调配置对不上。
  - [x] 触发原因、窗口统计、roll 值、阈值、最终是否回复必须写入 debug / raw log。
  - [x] stale 规则：等待期间群聊继续刷新时，旧判断不能插队发送，必要时用最新窗口重跑。

- [x] 第六阶段：普通图片理解与引用回复。
  - [x] 普通图片只在群聊读取触发后才做视觉理解；无趣、过期或不适合插话时返回 `WAIT`。
  - [x] 如果最终回复直接针对某张图片，发送层引用原图片 `message_id`；模型不输出 `[[quote]]` 或其它引用协议。
  - [x] 如果回复主要针对群聊文本，不强行引用图片。
  - [x] 图片理解结果只作为当轮临时上下文或短期 media cache，不污染长期记忆。

- [x] 第七阶段：群聊发送层。
  - [x] OneBot client 增加 `send_group_msg` 包装，支持文本、图片、表情包和可选 reply 段。
  - [x] 群聊发送必须经过同一套用户可见输出安全分类器，额外增加 q号 / 平台昵称泄露检查。
  - [x] 增加群聊频率限制和去重，避免复读、主动参与或表情包发送造成刷屏。
  - [x] 首次上线默认只允许白名单群开启发送能力。

- [x] 第八阶段：群聊记忆线程。
  - [x] 新增“群友身份”板块：保守维护 q号:昵称 对应关系，只能来自对话中明确自称或用户本人 `/mem` 记录“我是XXX”。
  - [x] `/mem` 和 `/forget` 只允许操作发起者自己的身份信息，不能记录或删除其他人的信息。
  - [x] 新增“共同记忆”板块：群里的梗、氛围、共同话题和长期上下文。
  - [x] 个人相关记忆使用 `<q号><时间><来源><记忆内容>` 结构化格式，平台群名片不能作为来源。

- [x] 第九阶段：复读模块与活跃时间。
  - [x] 不同群友重复同一文字三次及以上时，可以触发一次跟读；需要冷却时间和重复去重，避免无限复读。
  - [x] 每天早上 6 点按过去 24 小时消息密度更新活跃时间结构化配置。
  - [x] 活跃时间使用 EMA，区分工作日与周末，动态调整回复触发 X。

- [x] 第十阶段：运营开关与灰度。
  - [x] 新群默认观察和记忆，不开启主动对话能力；观察期是否结束由 WebUI 手动开关控制，不硬编码三天。
  - [x] 每个群单独配置：只读 / 允许被 @ 回复 / 允许 roll 回复 / 允许复读 / 允许发表情包。
  - [x] 私聊链路、WebUI 默认 session、现有 QQ 私聊 session 和群聊 session 的清空、checkpoint、调度、记忆互不污染。

## QQ / NapCat 接入 TODO

- [ ] 撤回接收。
  - 解析 `notice.friend_recall` 为 `EventType.RECALL`。
  - 通过 `onebot_message_id` 定位内部事件，移除 buffer 中对应事件，并让相关 pending job 过期。
  - 默认静默处理，不主动追问、不解释撤回内容。

## 后置能力

- [ ] 已读回执：`mark_private_msg_as_read` 的调用时机另行决定。
- [ ] QQ 表情回应：`set_msg_emoji_like` 后置。
- [ ] 主动戳一戳后置。
- [ ] 群聊版小夏：见“群聊版小夏落地开发顺序”。
- [ ] 语音 / 视频 / 文件理解后置。

## 底层鲁棒性剩余：阶段二观测补齐

目标：缓存健康判断已经从“完整 request 必须 append-only”切到“稳定前缀 append-only，临时尾巴可变”。剩余工作只补观测细节和回归验收，不回退到让一次性 system 消息滞留。

- [ ] 补充 ephemeral tail 观测字段。
  - `ephemeral_tail_hash`：本轮 runtime / reminder / repair / media / meme tail。
  - `ephemeral_tail_message_count`：临时尾巴消息数。
  - `ephemeral_tail_types`：记录当前尾部包含 runtime、final_reminder、hot_reminder、repair、media、meme_search 中哪些类型。
- [ ] 细化 rebuild reason 字段。
  - 将 `prefix_rebuild_reason` 细化为 `stable_prefix_rebuild_reason`。
  - 将 `full_request_prefix_rebuild_reason` 细化为 `full_request_changed_reason`。
- [ ] 明确哪些变化应该算 stable prefix 重建。
  - 主 system prompt 模板变更。
  - SOUL / MEMORY_CORE / TODAY_MEMORY / TOMORROW_TOPICS 等稳定 prompt 输入发生需要重载的变更。
  - LLM identity / model / base_url / cache_session_id 改变。
  - conversation checkpoint / compaction 插入。
  - 会话切换或 transcript reset。
- [ ] 明确哪些变化不应该算 stable prefix 重建。
  - msg_index、last_message_age、chat_status 等本轮 runtime 字段变化。
  - HOT reminder 出现或消失。
  - `FINAL_ACTION_OUTPUT_REMINDER` 内容作为当前尾部提醒加入。
  - repair / media / meme search 本轮临时尾巴变化。
- [ ] 补齐阶段二回归测试。
  - repair / meme / media tail 变化时，不触发 stable prefix rebuild。
  - LLM identity 或主 system prompt hash 变化时，触发 stable prefix rebuild。
  - checkpoint / compaction 插入时，记录明确 rebuild reason。
- [ ] 完成阶段二验收。
  - 日志能解释“为什么完整 request 不再是 append-only”，并能证明稳定前缀仍然按预期增长。
  - cache 相关告警或人工判断不再依赖完整 request append-only。
  - provider 实际 cached tokens 与 stable prefix 指标可以并排观察，便于判断真实缓存收益是否下降。
  - 如果实际 cached tokens 明显下降，再单独评估是否需要 provider-specific cache control。
