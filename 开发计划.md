# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地的阶段性任务不在本文归档；当前架构事实以 `产品设计.md`、`技术架构.md`、`表情包系统设计.md` 和 `社交软件接入调研归档.md` 为准。

## P0：记忆写入可靠性

- [ ] 把 Markdown 记忆写入链路改成 append-only + 小 JSON 意图。
  - 状态：已确认为 P0，但方案暂未定，先不实施。
  - 初步方向：LLM 只输出 `{section, text, ttl, source}` 这类小意图，代码负责去重、排序、状态更新和原子写入。
  - 待讨论重点：哪些内容能写入长期记忆、哪些只进当日记忆；用户明确 `/mem`、凌晨整理和模型推测的优先级边界；如何避免把模型内部推理写成用户事实。

## QQ / NapCat 接入 TODO

当前主线采用 NapCat + OneBot v11 + reverse WebSocket。目标是让 QQ 成为平台 adapter，不改主 harness，不新增 QQ 专属 action；QQ 侧只负责事件转换、消息段发送、媒体下载、平台 ID 记录和必要的平台降级。

### 第一阶段：连接验证

目标：只证明 QQ 私聊主链路能稳定接入现有产品，范围限定为聊天、表情发送、`/mem` 指令、用户输入状态检测与发送截断。

当前已落地：

- 正式 NapCat reverse WebSocket 地址固定为 `ws://127.0.0.1:8000/onebot/ws`，不再兼容临时抓包口 `6199/ws`。
- 后端已新增 OneBot reverse WebSocket 服务端入口，支持连接状态、token 校验、心跳元信息、同 WS action 发送与 `echo` response 关联。
- 已支持私聊文本消息接收：`post_type=message`、`message_type=private`、`message[]` 数组格式会转为内部 `ChatEvent`。
- 已支持 QQ 私聊文本回复：内部 text item 会转为 OneBot `send_private_msg` 的 `text` 消息段。
- 已支持本地 meme 发送：内部 meme item 通过 file stem 定位本地文件，并转为 OneBot `image` 消息段。
- 已支持 QQ `/mem` 和 `/forget` 指令分流：不走主聊天 harness，结果以 QQ 文本回复。
- 已支持 `notice.notify.input_status` 输入状态检测：转换为内部 `USER_COMPOSING`，只影响发送门禁，不进入主聊天 buffer。
- 已加平台路由保护：只有本轮最新输入来自 QQ 时，助手输出才会回 QQ；WebUI 触发的回复不会因为历史里有 QQ 消息而误发到 QQ。

剩余工作集中在真实 NapCat 长连接验收。

- [ ] 第一阶段验收。
  - NapCat 能连接后端，状态接口能看到连接状态。
  - QQ 私聊发“你好”，产品主链路能回复到 QQ。
  - LLM 输出多条 `items` 时，QQ 里按顺序收到多条气泡。
  - 用户要求表情时，QQ 能收到本地 meme 图片。
  - QQ 中发送 `/mem 我喜欢夜跑` 能写入记忆，且不触发主聊天 harness。
  - 用户输入状态发生在回复发送前时，旧回复能被等待、截断或 stale drop，不打断用户连发。

### 第二阶段：真实 QQ 体验补齐

目标：在第一阶段连通后，补齐 QQ 场景最影响自然度的能力：偷并存取表情包、主动消息、引用回复。

- [ ] 用户图片 / 表情包接收。
  - 识别 `image`、`mface`、`face` 消息段。
  - `face` 默认转轻量占位，不进入偷表情链路。
  - `image` 和 `mface` 建立内部 `MediaRef`，保留 `source=qq`、`qq_user_id`、`onebot_message_id`、`segment_type`、`file_id`、`url`、`path`、`local_path`、`hash`。
  - 回灌给 LLM 时只放轻量占位，例如 `[图片]`、`[表情: mface]`，不把 OneBot JSON 塞进 prompt。
- [ ] 下载用户发来的图片 / 表情。
  - 优先使用 NapCat / OneBot 可用接口获取图片文件，例如 `get_image`、`get_msg` 或实际事件中的 `url/path/file_id`。
  - 下载到 `data/onebot_media` 的临时目录，不直接进入正式表情库。
  - 记录下载失败、文件过大、格式不支持等技术原因。
- [ ] 偷表情入库。
  - 复用现有表情包系统：多模态命名、固定分类、dHash 去重、写锁、保存到 `memes/assets`、同步 `image_dhash_index.json`。
  - 偷表情必须静默，不主动告诉用户“已保存”。
  - 只有 LLM / 工具链判断值得保存时才入库；adapter 不用规则硬判“这是不是表情包”。
  - 入库失败只写 debug log，不污染聊天。
- [ ] 主动消息接入 QQ。
  - 绑定唯一 QQ 私聊目标；MVP 不默认群发，也不向所有历史联系人发送。
  - 主动消息仍走现有主动消息线程和 `ActionDecision -> SendItem` 归一化。
  - 发送前复用 EventGate 检查：HOT/COLD、当天上限、用户近期消息、USER_COMPOSING、snapshot stale。
  - 第一版主动消息可只支持 text item；meme 混发后续再测。
- [ ] 引用回复。
  - 接收用户 `reply` 消息段时，用 `get_msg` 或本地记录还原被引用消息的轻量上下文。
  - 发送侧保存每条用户消息和机器人消息的 `onebot_message_id`。
  - 第一版不新增主 harness action；adapter 根据当前 snapshot 的锚点，在需要时给第一条 outgoing message 前置 OneBot `reply` 段。
  - 优先场景：用户连续多条消息后，助手精准引用其中一条回应；用户主动引用一条旧消息时，助手理解引用上下文。
- [ ] 第二阶段验收。
  - 用户发 QQ 表情或图片后，系统能下载、记录、必要时静默入库。
  - 新入库表情可在后续 `search_meme -> meme` 链路中被选中并发回 QQ。
  - 主动消息能发到绑定 QQ 私聊对象，且不会和用户正在输入 / 刚发消息撞车。
  - 用户引用某条消息时，助手能理解被引用对象；助手必要时也能引用用户消息回复。

### 第三阶段：接入设计中的其他预想功能

第三阶段不在当前实现范围，只保留入口：

- [ ] 戳一戳 / 拍一拍接收为 `EventType.NUDGE`。
- [ ] 撤回接收为 `EventType.RECALL`，并通过 `onebot_message_id` 定位内部事件。
- [ ] 机器人输入状态 `set_input_status`：在 LLM 生成开始时展示“正在输入”，需要节流。
- [ ] 已读回执：`mark_private_msg_as_read` 的调用时机另行决定。
- [ ] QQ 表情回应、主动戳一戳、群聊、多用户记忆命名空间、语音 / 视频 / 文件理解后置。

### 当前关键阻塞项

- [ ] 需要用户完成 NapCat / QQ 登录和反向 WS 配置。扫码、账号登录和 WebUI token 属于账号验证，不能由代码绕过。
- [ ] 需要把 NapCat WebUI 的 reverse WebSocket 地址改为 `ws://127.0.0.1:8000/onebot/ws` 并保持长连接。
- [ ] 需要真实长连接验收 OneBot action response、QQ 收消息效果、本地 `file:///...` meme 图片发送是否被 NapCat 接受。
- [ ] 第二阶段主动消息需要先绑定唯一 QQ 私聊目标，否则产品上不知道该主动发给谁。

## P1 / P2 优化

- [ ] 会话摘要由代码管理版本：约每 80 个可见气泡生成一次 `conversation_checkpoint`，作为明确 cache 重建事件。
- [ ] 扩展 autotest 指标：新增风格预算、emoji 重复、meme 过度使用、同 stem 重复、deterministic downgrade 统计，避免只看 parse 成功率。
