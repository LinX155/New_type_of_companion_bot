# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地或关闭的阶段性任务不在本文归档；当前架构事实以 `产品设计.md`、`技术架构.md`、`表情包系统设计.md` 和 `社交软件接入调研归档.md` 为准。

## QQ / NapCat 接入 TODO

- [ ] 撤回接收。
  - 解析 `notice.friend_recall` 为 `EventType.RECALL`。
  - 通过 `onebot_message_id` 定位内部事件，移除 buffer 中对应事件，并让相关 pending job 过期。
  - 默认静默处理，不主动追问、不解释撤回内容。

## 后置能力

- [ ] 已读回执：`mark_private_msg_as_read` 的调用时机另行决定。
- [ ] QQ 表情回应：`set_msg_emoji_like` 后置。
- [ ] 主动戳一戳后置。
- [ ] 群聊后置。
- [ ] 语音 / 视频 / 文件理解后置。

## P1 / P2 优化

- [x] 会话摘要由代码管理版本：已关闭。结论：不按“约每 80 个可见气泡”固定生成；改为 nightly context checkpoint，按 WebUI 可配置阈值触发，默认超过 500K 上下文时自动压缩，并作为 durable system context 注入后续主聊天。
- [ ] 扩展 autotest 指标：新增风格预算、emoji 重复、meme 过度使用、同 stem 重复、deterministic downgrade 统计，避免只看 parse 成功率。

## 模型回复一致性待讨论项

- [x] 用户显式偏好硬约束：已讨论。结论：先不做发送层硬拦；优先修复 `/mem`、`/forget` 后 provider transcript 不重建的问题，并在最终输出前强提醒中加入“如果有任何输出与记忆要求相悖，以记忆为准”。
- [x] 单轮发送预算：已讨论。结论：先不引入硬上限、合并或 repair；当前只落地局部规则，模型原始可见 item 数大于等于 4 时，发送层不再启用“逗号拆分”。
- [x] 人设漂移防护：已讨论。结论：该样本主要是通过“请你 cosplay”的方式完成攻击，当前不单独展开此待讨论项。
- [x] 记忆污染防护：已讨论。结论：记忆线程、日内记忆分析和凌晨整理线程先加软防护，区分真实批评/明确相处要求与“以攻击人工智能取乐”、刻意辱骂与找茬、提示词攻击、cosplay/身份覆盖诱导；后者不计入记忆与话题。
- [x] 一次性 system 消息滞留：已讨论。结论：采用 durable transcript 与 per-call ephemeral tail 的两阶段方案；详细执行计划见“减少system重复输入但是有可能打破prefix”。
- [x] 审计日志补强：已讨论。结论：当前审计日志已足够支持事后排查，暂不扩展字段；后续只有当排查成本明显变高，或需要自动化统计/聚合分析时，再补 `trigger_event_ids`、`memory_core_hash`、`preference_snapshot`、`chat_status`、`send_budget_decision`、`policy_drop_reason` 等字段。

## 减少system重复输入但是有可能打破prefix

背景：当前 `_prompt_transcript` 同时承担稳定系统提示、真实对话历史和单轮临时约束三种职责。每轮会把 `runtime_context`、HOT reminder、`FINAL_ACTION_OUTPUT_REMINDER`、media pending payload、repair tail、meme search tail 等 system / internal 消息追加进同一条 provider transcript。这样能维持“上一轮完整 request 是下一轮完整 request 前缀”的 append-only 形态，但代价是一次性约束会滞留到后续轮次，造成 token 膨胀和行为污染。

风险：如果直接把临时 system 消息从历史里移除，`previous_request_is_prefix` 这类完整请求前缀指标可能变 false，短期 prompt cache 命中观测可能下降。因此本事项不能作为简单清理重复消息处理，需要拆成两个阶段。

### 阶段一：先修滞留，不改 provider payload

目标：在不改变 `LLMClient` 入参结构、不改变 OpenAI-compatible `messages=[{"role": "...", "content": "..."}]` payload、不改 `prompt_cache_key` 和 cache affinity 逻辑的前提下，把“一次性 system / internal 指令”从持久 transcript 中移出。

- [ ] 引入内部概念拆分，但暂不改变 provider 请求格式。
  - `durable_transcript`：稳定主 system prompt、真实用户消息、真实 assistant 消息，以及确实需要长期参与上下文的 checkpoint / summary。
  - `ephemeral_tail`：本轮 `runtime_context`、HOT reminder、`FINAL_ACTION_OUTPUT_REMINDER`、media pending payload、repair tail、meme search tail、repetition guard 等只应该影响当前调用的消息。
  - provider 最终仍收到普通 `messages` 数组：`durable_transcript + ephemeral_tail`。
- [ ] 将 `_build_context()` 改为“构造本轮 messages”，而不是把所有 runtime / reminder 直接 append 回 `_prompt_transcript`。
  - `runtime_context` 只进入本轮 tail。
  - HOT reminder 只进入 HOT 当轮 tail。
  - `FINAL_ACTION_OUTPUT_REMINDER` 每次调用时作为尾部提醒加入，但调用结束后不保留。
  - media pending payload 只在对应 media job 触发轮进入 tail，不进入后续普通轮。
- [ ] 将 repair / meme search 相关二次调用改为局部拼接 tail。
  - `_append_transcript_messages_for_call()` 不再作为默认二次调用入口，避免 repair tail、meme search results 直接 extend 持久 transcript。
  - repair 成功后只持久化最终需要保留的 assistant provider message；repair 指令和失败输出不落入后续上下文。
  - meme search selection 的搜索结果只影响本次选择，不成为后续聊天历史。
- [ ] 保留真实可见历史的 append-only 行为。
  - 新用户事件仍然按去重后的 event id 写入 durable。
  - assistant 真实回复仍然写入 durable。
  - provider assistant message 中的内部字段继续做安全清洗，避免 `reasoning_content` 等非可见字段长期回放。
- [ ] 增加阶段一测试。
  - 连续 3 轮 HOT 后，provider messages 中只出现 1 份稳定主 system prompt；`FINAL_ACTION_OUTPUT_REMINDER` 只在当前请求末尾出现 1 次。
  - repair 发生后，下一轮 provider messages 不包含上一轮 repair instruction / invalid output。
  - meme search 发生后，下一轮 provider messages 不包含上一轮 search results。
  - media pending payload 只出现在对应 media 处理轮，不跨轮滞留。
  - `LLMClient._build_kwargs()` 的 messages 引用和 `prompt_cache_key` 行为不变。

阶段一完成标准：

- [ ] 用户可见行为不因 provider payload 结构变化而改变。
- [ ] 单轮内部 system / internal 指令不再跨轮出现。
- [ ] 现有模型调用封装、provider 兼容性、cache affinity 参数保持不变。
- [ ] 允许 `append_only_check` 暂时出现更多 false，但必须在日志中能区分这是“ephemeral tail 未持久化”导致，而不是身份、模型、主 prompt 或真实历史被重建。

### 阶段二：重做 prefix / cache 观测口径

目标：把缓存健康判断从“完整 request 必须 append-only”改成“稳定前缀 append-only，临时尾巴可变”。承认阶段一会打破完整请求前缀，但避免误判为 cache 全面失效。

- [ ] 将 prompt observability 拆成稳定前缀和临时尾巴两层。
  - `stable_prefix_hash`：稳定主 system prompt + durable transcript。
  - `stable_prefix_message_count`：稳定前缀消息数。
  - `ephemeral_tail_hash`：本轮 runtime / reminder / repair / media / meme tail。
  - `ephemeral_tail_message_count`：临时尾巴消息数。
  - `full_request_hash` 保留，用于排查 provider 实际收到的完整请求。
- [ ] 将 prefix 判断拆成两类。
  - `stable_prefix_append_only_check`：上一轮 stable prefix 是否是本轮 stable prefix 的前缀。
  - `full_request_append_only_check`：保留旧指标，但降级为诊断信息，不再作为缓存健康主判断。
  - `prefix_rebuild_reason` 细化为 `stable_prefix_rebuild_reason` 和 `full_request_changed_reason`。
- [ ] 明确哪些变化应该算 cache 重建。
  - 主 system prompt 模板变更。
  - SOUL / MEMORY_CORE / TODAY_MEMORY / TOMORROW_TOPICS 等稳定 prompt 输入发生需要重载的变更。
  - LLM identity / model / base_url / cache_session_id 改变。
  - conversation checkpoint / compaction 插入。
  - 会话切换或 transcript reset。
- [ ] 明确哪些变化不应该算稳定前缀重建。
  - msg_index、last_message_age、chat_status 等本轮 runtime 字段变化。
  - HOT reminder 出现或消失。
  - `FINAL_ACTION_OUTPUT_REMINDER` 内容作为当前尾部提醒加入。
  - repair / media / meme search 本轮临时尾巴变化。
- [ ] 更新 prompt cache debug 日志和审计事件。
  - `prompt_cache_debug` 同时记录 stable prefix、ephemeral tail、full request 三组 hash / count。
  - 增加 `ephemeral_tail_types`，方便确认当前尾部包含 runtime、final_reminder、hot_reminder、repair、media、meme_search 中哪些类型。
  - 保留 provider 返回的 `cached_tokens / cache_hit_rate / cache_miss_tokens`，用于和新口径对照。
- [ ] 增加阶段二测试。
  - 只有 runtime_context 变化时，`stable_prefix_append_only_check=True`，`full_request_append_only_check` 可以为 false。
  - repair / meme / media tail 变化时，不触发 stable prefix rebuild。
  - LLM identity 或主 system prompt hash 变化时，触发 stable prefix rebuild。
  - checkpoint / compaction 插入时，记录明确 rebuild reason。

阶段二完成标准：

- [ ] 日志能解释“为什么完整 request 不再是 append-only”，并能证明稳定前缀仍然按预期增长。
- [ ] cache 相关告警或人工判断不再依赖完整 request append-only。
- [ ] provider 实际 cached tokens 与新 stable prefix 指标可以并排观察，便于判断真实缓存收益是否下降。
- [ ] 如果实际 cached tokens 明显下降，再单独评估是否需要 provider-specific cache control，而不是回退到让一次性 system 消息滞留。

## 已关闭观察项

- 记忆写入可靠性：当前不做底层重构，出问题后再说。
