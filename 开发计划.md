# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地的阶段性任务不在本文归档；当前架构事实以 `产品设计.md`、`技术架构.md`、`表情包系统设计.md` 和 `社交软件接入调研归档.md` 为准。

## P0：多用户 session 隔离底座

目标：支持同一个本地部署实例、同一个 NapCat QQ 号同时服务多个 QQ 私聊用户。每个用户必须拥有独立主对话线程；除 `SOUL.md` 和全局表情包系统外，能隔离的状态、历史、记忆、话题、主动消息和调试数据都应按 `session_id` 隔离。

这不是群聊能力，也不是多账号 SaaS。第一目标是小范围公测时避免串人、串记忆、串话题和串主动消息；长期本地单聊部署仍应保持零配置可用。

### 产品边界

- `SOUL.md` 全局共享：它代表“我是谁、我如何说话、陪伴关系边界”，不是某个用户的私人记忆。
- 表情包系统全局共享：所有用户都可以贡献和使用同一套 `memes/assets`、`memes_data.json`、`image_dhash_index.json`。
- QQ 私聊按用户隔离：`qq_private_<qq_user_id>` 是第一版 session 命名规则。
- WebUI 本地测试保留独立默认 session：建议使用 `webui_default`，避免和 QQ 私聊混在一起。
- 群聊暂不纳入第一版隔离目标；群聊后续需要 `qq_group_<group_id>_<user_id>` 或按群共享线程另行设计。
- 不做跨用户主动推荐、跨用户记忆共享、跨用户话题同步。

### 必须隔离的内容

- 主对话运行时：HOT/COLD、buffer、pending job、snapshot、msg_index、last_user_message_at、输入中状态、nudge / poke 状态。
- provider transcript：append-only prompt 历史、reasoning / CoT 回传、repair / meme 二轮上下文、cache session / prefix affinity。
- 可见对话历史：`ConversationEvent.session_id` 必须按 session 写入和读取。
- raw 调试日志：`RawChatLog.session_id` 必须按 session 写入；跨用户调试只能在开发视图聚合，不能进入 prompt。
- 记忆文件：每个 session 独立 `MEMORY_CORE.md`、`dm/YYYY-MM-DD.md`。
- 明日话题：每个 session 独立 `TOMORROW_TOPICS.md`。
- `/mem`、`/forget`：只作用于当前消息来源对应的 session。
- 定时记忆分析：按活跃 session 分别读取当日 conversation events，分别写对应 session 的 dm 和 tomorrow topics。
- 凌晨整理线程：按 session 分别整理长期记忆和明日话题。
- 主动消息：每个 session 单独计算候选、今日上限、未回复主动消息、发送目标和撞车判断。
- 图片理解结果：普通图片分析结果只进入当前 session 的后续上下文；静默偷到的表情包仍进入全局 meme 库。
- 可见性和重复控制：emoji / meme 最近重复、长文本拆气泡、发送幂等都按 session 计算。

### 目录与命名方案

第一版采用文件命名空间，不把记忆系统立刻迁移进数据库：

```text
memory/
  sessions/
    qq_private_10001/
      MEMORY_CORE.md
      TOMORROW_TOPICS.md
      dm/
        2026-06-21.md

    qq_private_10002/
      MEMORY_CORE.md
      TOMORROW_TOPICS.md
      dm/
        2026-06-21.md

    webui_default/
      MEMORY_CORE.md
      TOMORROW_TOPICS.md
      dm/
        2026-06-21.md
```

兼容要求：

- 根目录现有 `MEMORY_CORE.md`、`TOMORROW_TOPICS.md`、`dm/` 视为历史单用户资料。
- 首次启用多 session 时，本机现有历史内容允许格式化清空；不需要迁移旧私人记忆。
- `webui_default` 从空模板开始。
- QQ 新用户默认创建空模板，不继承 `webui_default` 的私人记忆。
- `SOUL.md` 继续留在根目录。

### 阶段 0：盘点与安全迁移准备

- [x] 明确所有读取 `DEFAULT_SESSION_ID`、`session_id="default"`、根目录记忆文件、全局 `TOMORROW_TOPICS.md` 的代码路径。
- [x] 列出会写入长期文件的入口：主 prompt 读取、`/mem`、`/forget`、定时记忆分析、凌晨整理、主动消息候选消费、WebUI 记忆编辑接口。
- [x] 列出运行时单例：`EventGate`、`SnapshotManager`、`CompanionGraph` 内部 provider transcript、media debug、input status task、active message counters。
- [x] 设计一次性格式化策略：旧根目录 `MEMORY_CORE.md`、`TOMORROW_TOPICS.md`、`dm/` 可以清空并重建为空模板，旧 conversation events 的 `default` 可按开发数据处理。
- [x] 验证 SQLite schema 已有 `session_id` 字段的表；缺少 session 字段的主动消息相关记录需要补字段或通过 `ConversationEvent` 记录规避。

阶段验收：

- 能输出一份“需要改造的全局状态清单”。
- 不修改业务行为，只确认改造范围。
- 确认旧文件不会被删除或覆盖。

### 阶段 1：session identity 与来源映射

- [x] 新增统一 `SessionIdentity` / `SessionResolver`。
- [x] WebUI 请求默认映射到当前下拉选中的 session；首次启动默认 `webui_default`。
- [x] OneBot 私聊事件映射到 `qq_private_<user_id>`。
- [x] QQ 私聊新用户自动创建 session；公测为邀请制，不建立白名单。
- [x] OneBot 输入状态、poke、图片、表情、引用消息都必须带同一个 session_id。
- [x] `ChatEvent`、`ConversationSnapshot` 创建时不再硬编码 default。
- [x] `RawChatLog` 记录平台、user_id、session_id 三者，方便调试但不混用。
- [x] `/api/status` 增加当前 session 视图；后续可扩展为 session 列表。

阶段验收：

- A、B 两个 QQ 用户发消息时，日志中能看到两个不同 session_id。
- WebUI 发消息时进入当前下拉选中的 session。
- 不要求此阶段完全隔离运行时，只先把 session_id 正确传到底。

### 阶段 2：主对话运行时多 session

- [x] 新增 `SessionRuntimeManager`，按 session_id 管理运行时对象。
- [x] 每个 session 独立持有 `EventGate`、`SnapshotManager`、`CompanionGraph` 或其内部状态容器。
- [x] `EventGate` 的 buffer、pending_job_id、HOT/COLD、msg_index_today、last_user_message_at 按 session 隔离。
- [x] snapshot stale 判断只影响当前 session。
- [x] 用户 A 正在输入、正在生成、正在 HOT，不影响用户 B。
- [x] 媒体 pending 和后台完成结果按 session 回到对应 graph，不进入其他用户 prompt。
- [x] 多 session 允许并行生成；每个 session 内仍保持 single-flight，避免同一用户自己的消息乱序。
- [x] WebSocket 状态广播可以继续全局广播，但 payload 必须带 session_id；前端只展示当前下拉选中的 session 或明确聚合。

阶段验收：

- 两个 QQ 用户连续交错发消息，不互相打断 pending job。
- A 的 HOT 不影响 B 的 COLD。
- A 的输入中状态不触发 B 的 pending / cancel。
- A 和 B 可以同时触发 LLM 生成；成本不是第一限制。
- `/api/status` 能看出不同 session 的状态差异。

### 阶段 3：provider transcript 与缓存隔离

- [x] provider transcript 必须按 session_id 分开保存和重建。
- [x] append-only 检查按 session 做，不能拿 A 的 previous request 和 B 的 current request 比。
- [x] repair、context repair、meme 二轮、media pending、image result 回灌只进入当前 session transcript。
- [x] cache affinity / prompt_cache_key 需要包含稳定 session 维度，避免服务商侧 cache session 混用。
- [x] 清空某个 session 的对话时，只重置该 session transcript、prompt cache debug、media queue prompt payload。
- [x] `/api/conversation/clear` 默认只清空 WebUI 当前下拉选中的 session。
- [x] 全局清库仍可作为开发工具保留，但产品入口默认只清当前 session。

阶段验收：

- A 的 raw LLM request 不包含 B 的历史消息、记忆、图片分析结果、repair 内容或 CoT。
- B 的 raw LLM request 不包含 A 的任何上下文。
- 分别清空 A / B 时互不影响。
- append-only debug 能按 session 展示。

### 阶段 4：记忆与明日话题文件隔离

- [x] `MemoryFileManager` 改为支持 `for_session(session_id)` 或显式 session 参数。
- [x] `read_memory_core`、`write_memory_core`、`read_today_memory`、`write_today_memory`、`read_tomorrow_topics`、`write_tomorrow_topics` 全部按 session 路径工作。
- [x] 根目录 `SOUL.md` 仍由全局 manager 读取。
- [x] `/mem` 和 `/forget` 从当前事件 session 读取 / 写入对应 session 的 `MEMORY_CORE.md`。
- [x] 主 prompt 读取当前 session 的 `MEMORY_CORE.md`、`dm/YYYY-MM-DD.md`、`TOMORROW_TOPICS.md`。
- [x] 不新增 WebUI 记忆编辑界面；如果未来恢复编辑入口，必须明确当前 session。
- [x] 首次访问 session 时自动创建模板文件。

阶段验收：

- A 执行 `/mem 我不喜欢被催睡觉`，只写入 A 的 `MEMORY_CORE.md`。
- B 的 prompt 不出现 A 的 `/mem` 内容。
- A 的 `TOMORROW_TOPICS.md` 和 B 的 `TOMORROW_TOPICS.md` 是两份文件。
- 当前本地旧资料可被格式化清空；新 `webui_default` 使用空模板。

### 阶段 5：定时记忆、凌晨整理与主动消息隔离

- [x] 维护全量 session 列表：来自 `ConversationEvent.session_id`、QQ 私聊自动创建记录、或 session 文件目录。
- [x] 记忆分析任务按全量 session 循环执行：只读取该 session 当天可见用户消息。
- [x] 凌晨整理任务按全量 session 循环执行：只读取该 session 的 dm、memory core、tomorrow topics。
- [x] 主动消息调度按 session 选候选：只读该 session 的 `TOMORROW_TOPICS.md`。
- [x] 每个 session 单独计算今日主动消息次数和未回复主动消息。
- [x] QQ 主动消息发送目标由 session binding 决定：`qq_private_<user_id>` 只能发给这个 user_id。
- [x] QQ 主动消息在多用户隔离落地后仍默认关闭；等主动消息完整实现和验收后再打开。

阶段验收：

- A 的未闭合话题不会写入 B 的 tomorrow topics。
- A 的主动消息不会发给 B。
- A 达到今日主动消息上限，不影响 B。
- 定时任务失败能记录具体 session_id，单个 session 失败不阻断其他 session。

### 阶段 6：WebUI、调试与管理体验

- [x] WebUI 对话栏右上角新增 session 下拉选择。
- [x] QQ 事件进入后，WebUI 能实时显示对应 session 新消息；界面展示当前下拉选中的 session。
- [x] `/api/conversation`、`/api/status`、`/api/conversation/clear` 支持 session 参数；不传时使用当前 WebUI session。
- [x] 调试日志查询支持按 session 过滤。
- [x] 媒体 job 状态按 session 过滤展示。
- [x] 增加“完全清除当前用户”按钮：二次确认后删除当前 session 私有的全部数据，包括运行时状态、conversation events、raw logs、provider transcript、media prompt payload、memory/sessions/<session_id> 目录；不删除 `SOUL.md`，不删除全局表情包。
- [x] 公测期间建议在下拉中显示 session 标识、平台、最近消息时间；不新增记忆编辑界面。

阶段验收：

- 开发者能确认某条 QQ 消息属于哪个 session。
- 清空 WebUI 测试对话不会清空 QQ 用户 A / B 的记忆和状态。
- 清空 A 不影响 B。
- 完全清除 A 会删除 A 的私有目录和私有日志 / 状态，不影响 B、`SOUL.md` 和全局表情包。

### 阶段 7：回归与公测前验收

- [x] 单用户 WebUI 旧流程完全不退化。
- [x] QQ 单用户流程完全不退化：文本、meme、图片、表情包、poke、输入状态仍能工作。
- [x] QQ 双用户交错对话压测：A/B 各 10 轮连续消息，确认 prompt、状态、记忆、话题、raw log 不串。
- [x] `/mem`、`/forget` 双用户隔离测试。
- [x] 图片理解结果双用户隔离测试。
- [x] 主动消息候选双用户隔离测试。
- [x] 数据格式化测试：旧根目录记忆存在时，按确认策略清空并重建空模板，不误给 QQ 新用户。

落地状态：已新增 `SessionIdentity` / `SessionRegistry` / `SessionRuntimeManager`，并完成 WebUI、OneBot 私聊、主运行态、provider transcript/cache、记忆文件、定时任务、主动消息候选、media job、raw debug 和管理入口的 session 隔离。自动化覆盖见 `tests/session_isolation_test.py`、现有 OneBot/media/prompt/send gate 回归和 WebUI build；真实双 QQ 账号公测前仍建议做一次手工 smoke，但不再需要新的底层架构改造。

### 已确认决策

- 不建立 QQ 私聊白名单；公测是邀请制。
- QQ 私聊新用户自动创建独立 session。
- WebUI 只需要提供 session 切换；入口放在对话栏右上角下拉选择。
- 现有本地历史内容可以完全格式化清空，不需要保留或迁移。
- QQ 主动消息先关闭，等主动消息链路完整后再启用。
- 多用户允许并行生成；每个 session 内仍保持 single-flight。
- 清空对话默认清当前 WebUI 选中的 session。
- 定时记忆和凌晨整理全量扫描所有 session。
- WebUI 增加“完全清除当前用户”：二次确认后抹除该 session 私有全部数据和目录结构。
- 表情包继续全局共享；不记录来源用户。
- 群聊不纳入本次考虑范围。
- 不新增 WebUI 记忆编辑界面。
- provider cache / transcript 必须按 session 分开。
- 普通图片分析结果默认只进入当前 session 的近期上下文和 debug；当日记忆可谨慎参考“用户文字 + 图片理解”，但长期核心记忆必须有用户文字或后续对话确认。表情包理解结果不进入长期记忆，除非用户明确表达对某类表情包的稳定偏好。

## P0：记忆写入可靠性

- [ ] 把 Markdown 记忆写入链路改成 append-only + 小 JSON 意图。
  - 状态：已确认为 P0，但方案暂未定，先不实施。
  - 初步方向：LLM 只输出 `{section, text, ttl, source}` 这类小意图，代码负责去重、排序、状态更新和原子写入。
  - 待讨论重点：哪些内容能写入长期记忆、哪些只进当日记忆；用户明确 `/mem`、凌晨整理和模型推测的优先级边界；如何避免把模型内部推理写成用户事实。

## QQ / NapCat 接入 TODO

当前主线采用 NapCat + OneBot v11 + reverse WebSocket。QQ 是平台 adapter，不改主 harness，不新增 QQ 专属 action；QQ 侧只负责事件转换、消息段发送、媒体下载、平台 ID 记录和必要的平台降级。

### 第二阶段：真实 QQ 体验补齐

目标：补齐 QQ 场景最影响自然度的能力：图片理解、偷并存取表情包、主动消息、引用回复。

### 当前 P0：QQ 图片理解与静默偷表情

这部分是第二阶段最重要能力。目标不是把 QQ 图片 JSON 直接塞进主聊天，也不是让下载 / 视觉 / 偷表情阻塞当前对话；而是在主聊天 prompt 中先追加轻量 pending 内部提示，再由后台 `MediaJobQueue` 异步完成下载、视觉理解、偷表情分析、入库和索引同步。

#### 产品语义

- 用户发普通图片时，第一语义是“分享”。如果同时有文字，当前主回复优先接文字，不编造图片内容；图片后台理解完成后，结果进入后续 prompt，用户追问“刚才那张图”时能接上。
- 用户只发普通图片时，当前主回复可以先轻轻接住，例如“我看看”，但不能描述未知画面；后台理解完成后写 debug，并作为后续轮次可用的内部结果。
- 用户发表情包 / 贴纸时，第一语义更接近“emoji / 心情气泡”。主线程只需要把“用户发了表情包”当作语气、心情或接梗信号轻量回应；详细看图、判断是否值得偷、分类、重命名、去重和保存都走后台。
- 偷表情包是内部静默能力：值得保存时自动入库，不主动告诉用户“已保存”“我偷了”。
- 图片理解失败时不能编造图片内容；主聊天只能轻问一句或基于用户文字回应。
- 视觉能力按目标模型 `mimo-v2.5` 已具备来实现；不做 provider-aware fallback，不为无视觉能力模型额外设计产品分支。
- 如果用户切换到无视觉能力模型，API 端可能在图片理解 harness 报错；该错误只进入现有失败 / 降级链路，不要求系统保证看图功能可用。

#### OneBot / NapCat 接收范围

- 接收 `image`、`mface`、`face` 消息段。
- `face` 是 QQ 基础表情，只转成 `[QQ表情: face:<id>]` 这类轻量文本，不下载、不偷表情。
- `mface` 一律按表情包 / 贴纸处理。
- `image` 默认按普通图片处理；如果满足以下任一条件，标记为 `is_sticker=true` 并进入表情理解 / 偷表情链路：
  - `sub_type == 1`。
  - `summary` 包含“表情”。
  - `file == "marketface"`。
  - NapCat 后续真实样本显示其他稳定贴纸字段。
- `MediaRef` 至少保留：`source=qq`、`qq_user_id`、`onebot_message_id`、`segment_index`、`segment_type`、`sub_type`、`summary`、`file`、`file_id`、`file_unique`、`url`、`path`、`file_size`、`is_sticker`、`local_path`、`sha256`、`download_status`、`download_error`。
- 回灌给主聊天的用户消息只放轻量占位，例如 `[图片]`、`[表情]`、`用户文字 + [图片]`；不把完整 OneBot JSON、URL、base64、local path 暴露给主 harness。
- 已观测 NapCat 私聊真实样本：
  - 普通图片上报为 `type=image`、`sub_type=0`、`summary=""`、`file=<hash>.jpg`、`url=multimedia.nt.qq.com.cn/download...`、`file_size=<bytes>`，应判为 `message.image / is_sticker=false`。
  - 收藏 / 动画表情上报仍是 `type=image`，不是 `mface`；字段为 `sub_type=1`、`summary="[动画表情]"`、`file=<hash>.jpg`、`url=multimedia.nt.qq.com.cn/download...`、`file_size=<bytes>`，应判为 `message.sticker / is_sticker=true`。
  - 直接访问事件里的 `url` 可以下载成功；已观测响应 `Content-Type=image/jpeg`，文件头也是 JPEG，即使 `summary` 写的是 `[动画表情]`。
  - QQ 自有表情 / 黄豆系列上报为 `type=face`，只有 `id` 以及 NapCat raw 里的 `faceType` 等轻量字段；没有 `url`、`file`、`file_size`，也不会生成 `media_refs`，因此不进入下载和偷表情。
  - 已观测 QQ 自有基础表情为 `faceType=1`；QQ 新版内置大表情仍是 `type=face`，但 `faceType=3`，raw 中可能带 `faceText`、`packId`、`stickerId`、`sourceType`、`stickerType`、`chainCount`。这些仍按 QQ 内置表情处理，只作为轻量占位传给主聊天。
  - 当前判定优先级：`type=face` 一律是 QQ 内置表情，不下载；`type=image/sub_type=1` 是可下载收藏 / 动画表情；`type=image/sub_type=0` 是普通图片。
  - 暂未观测到 `type=mface`；MVP 不依赖 `mface` 才识别表情，仍保留兼容分支。

#### 下载链路

下载入口放在 OneBot adapter 层，落地到 `data/onebot_media/YYYY-MM-DD/`，只作为临时媒体库和视觉分析白名单目录，不直接进入正式 `memes/assets`。

下载优先级：

1. 如果事件段有 `path` 且本机可读，先复制到 `data/onebot_media`，不要直接用 NapCat 原路径。
2. 如果事件段有 `url`，直接 HTTP 下载；NapCat 图片 URL 有过期窗口，所以收到事件后应尽快下载。
3. 如果 `url` 不存在或已过期，优先调用 `get_image(file=<file>)` 或 `get_image(file_id=<file_id>)` 刷新图片信息。
4. 如果仍失败，调用 `get_msg(message_id)` 重新获取消息段，再重复 `path/url/get_image`。
5. 如果只有 `file_id/file` 或普通文件路径，调用 `get_file`。
6. 如果遇到大文件、跨设备部署、普通接口不稳定，再接 NapCat `download_file_stream`；MVP 可先记录为后置 fallback。

下载后的校验：

- 单文件大小上限沿用 `MemeStealAnalyzer` 当前 12MB；超过只记录 debug，不送视觉模型。
- 保存文件名用 `message_id + segment_index + file_unique/sha256`，不信任上游文件名。
- 读取文件头识别真实 MIME 和扩展名，修正上游统一 `.jpg` 的情况；参考 `angel_smile` 的 png/jpg/gif/webp/bmp/tiff/ico/heic/avif 文件头检测。
- 支持格式先收敛为 `.jpg`、`.jpeg`、`.png`、`.gif`、`.webp`、`.bmp`；其他格式可下载留档，但不进视觉 harness 和偷表情。
- 计算 `sha256` 做同一事件内去重；正式入库前再做 dHash 近似去重。
- 所有失败写 `RawChatLog(event_type="onebot_media_download")`，包括 `download_status`、`source_field`、`error_message`，但不污染用户聊天。

#### 异步媒体管线

触发图片 / 表情事件后，主聊天请求仍走原有基础 harness。区别是：在当前用户消息之后、最终 action reminder 之前，只追加轻量 pending 内部 system message；真正的下载、视觉和入库放到后台队列。

主线程 pending payload：

- 普通图片：追加 `media_pending`，提示模型“这是用户分享，后台正在理解；如果有文字，优先回复文字；当前不要编造图片内容”。
- 表情包 / 贴纸：追加 `sticker_pending`，提示模型“这是心情、语气或接梗信号；轻轻接住即可；不要等待后台入库，不要把分析讲给用户”。
- pending payload 不包含下载 URL、base64、本地路径或 OneBot raw JSON。
- pending payload 会写入 `/api/status.media` 和 `RawChatLog`，用于调试当前主回复看到的内部状态。

后台第一套结果：`image_understanding_result`

- 对普通图片输出：
  - `kind`: `photo | screenshot | object | scene | document | meme_like | unknown`
  - `visible_summary`: 一句话说明图片大概是什么。
  - `relation_to_context`: 它和前文有什么关系；不知道就写 `unknown`。
  - `user_intent`: 用户为什么发这张图；默认先按“分享”理解，再根据上下文细化为吐槽、求确认、求夸、求安慰、继续上文等。
  - `desired_response`: 用户大概希望得到什么回应。
  - `reply_style`: 建议主聊天短回、认真看图、调侃、安慰、追问、少分析等。
  - `confidence`: `high | medium | low`。
- 对表情包 / 贴纸输出：
  - `kind`: `sticker`
  - `user_mood`: 用户当下大概情绪；只需要粗粒度，例如开心、无语、撒娇、委屈、接梗、催促。
  - `interaction_intent`: 它在聊天里承担的轻互动含义；不知道就写 `mood_only`。
  - `reply_bias`: 建议轻轻接住、回表情包、短句、跟梗或等待；不要建议主聊天复述“你这个表情表达了……”。
  - `confidence`: `high | medium | low`。
- 这套 harness 只解释用户输入，不决定主 action，不输出 `REPLY/REACT/WAIT`；尤其不能要求主聊天把表情包分析结论讲给用户听。
- 失败时记录 `image_understanding_result.status="failed"`；当前主回复不等待它，后续轮次看到失败结果时不能臆造。

后台第二套结果：`meme_intake_result`

- 只对 `is_sticker=true`、`mface` 或第一套判断为 `meme_like` 的图片触发。
- 复用当前 `MemeStealAnalyzer` 的多模态命名能力，输出 `should_steal`、`category`、`save_name`、`keywords`、`reason`、`safety`。
- 如果 `should_steal=true`，继续调用真正的 `steal_meme` 入库工具；如果为 false，只把原因作为 debug，不影响聊天。
- 工具结果写 debug，不自动发给用户：
  - `status`: `saved | duplicate | skipped | failed`
  - `file_stem`: 保存或命中的本地 stem；失败或跳过则为 null。
  - `category`: 固定分类 ID。
  - `reason`: 简短内部原因。
- `meme_intake_result` 主要服务入库和未来检索，不是给主聊天生成可见分析话术。主聊天不能告诉用户“我保存了”，也不需要解释为什么偷或没偷。

#### append-only 与上下文规则

- 当前用户消息仍以 `[图片]` / `[表情]` 形式进入 prompt transcript。
- 当前主回复只固化 `media_pending` / `sticker_pending`，保证主聊天不等待慢任务。
- 后台完成的 `image_understanding_result` 会被安全化后追加到后续 prompt transcript，未来用户说“刚才那张图”时仍能接上。
- `meme_intake_result` 只写 debug 和入库结果，不作为可见话术来源，不要求主聊天复述。
- 不把图片 base64、下载 URL、本地路径、OneBot raw JSON 固化进主聊天 transcript。
- 同一张图片在同一轮 repair / meme 二轮中必须复用已生成的 pending 状态，不重复入队。
- 对 `sha256` 或 `file_unique` 做短期缓存，避免同一张图连续触发多次下载和分析。

#### `steal_meme` 入库工具

在现有 `MemeStealAnalyzer` 之外补真正有副作用的保存工具，参考 `angel_smile` 的路径解析、格式校验、文件头识别、写锁和命名冲突处理；但 dHash 索引不照搬 `angel_smile` 的手动 register 模式，优先复用当前项目 `MemeCatalog.sync_dhash_index()` 的自动同步机制。

- 输入：`image_ref`、`category`、`save_name`，可附带 `analysis_result`。
- `image_ref` 只接受 `data/onebot_media`、`memes/inbox`、`memes/tmp`、`memes/assets` 下的本地文件或明确允许的 `file:///`。
- 保存前校验路径白名单、文件存在、大小、后缀、真实文件头。
- 分类必须是固定 18 类之一；`save_name` 走现有小写英文数字下划线归一化，并必须以分类前缀开头。
- 保存前做 dHash 近似去重，阈值参考 `angel_smile` 默认 `8`。
- 重复时不保存新文件，返回 matched file stem。
- 保存目标：`memes/assets/<category>/<save_name>.<real_ext>`。
- 命名冲突时追加短 hash 或时间戳，不能覆盖已有文件。
- 写图片必须加写锁；写入后调用当前 catalog 同步逻辑刷新 `image_dhash_index.json`，让索引以 `memes/assets` 真实文件系统为准。
- 去重可在保存前用当前索引做快速检查；保存后仍以自动同步结果为最终事实，避免索引漂移。
- 入库失败写 `filtered.json` 或 `RawChatLog(event_type="meme_steal")`，不要向用户解释技术原因。

#### 主链路行为

- 普通图片 + 文字：主聊天优先回复文字内容，同时知道用户还分享了图片；不要编造图片画面。
- 只有普通图片：主聊天先轻轻接住，后台理解结果供后续轮次使用；MVP 不主动插入第二条补充消息，避免打断用户。
- 表情包：主聊天把它当作心情 / 语气信号，不要默认分析、不需要严格围绕表情包展开行动；大多数情况下轻轻接住、回一个表情包、短句或继续原话题即可。
- 表情包理解结果是内部辅助，不是用户可见内容；禁止输出“你这个表情说明你现在……”这类分析腔，除非用户明确问这个表情是什么意思。
- 用户发图时如果还在输入中，图片 / 表情本身是已发送事件，应清掉输入中状态并正常触发主链路。
- 下载、视觉分析或偷表情失败不阻断主聊天；失败只写 debug，后续轮次看到失败状态后按“没加载出来/看不清”自然处理。
- `/mem`、`/forget` 仍不走主聊天 harness；如果命令里带图，MVP 先不支持图文命令。

#### 实施清单

- [x] `app/adapters/onebot11/events.py`：补全 `MediaRef` 字段，保留 `file_id`、`file_unique`、`path`，完善 `image/mface/marketface` 判定。
- [x] `app/adapters/onebot11/client.py`：新增 `get_msg`、`get_image`、`get_file`、`download_file_stream` 请求方法。
- [x] 新增 `app/adapters/onebot11/media.py`：实现下载、复制、MIME 检测、大小限制、sha256、落盘到 `data/onebot_media`。
- [x] `app/llm/prompts.py`：新增统一的 `build_image_understanding_messages`，按普通图片 / 表情包两种语义输出小 JSON，按 `mimo-v2.5` 支持视觉输入实现。
- [x] `app/memes/steal.py`：在现有无副作用分析器旁新增保存工具，复用白名单、文件头检测、命名归一化和当前 `MemeCatalog` 的 dHash 自动同步；保存和索引同步的重文件操作移入线程池。
- [x] 新增 `app/core/media_jobs.py`：后台 `MediaJobQueue` 负责下载、视觉理解、偷表情分析、保存、去重和索引同步；不占用 `EventGate.pending_job_id`。
- [x] `app/core/graph.py`：主线程只追加 `media_pending` / `sticker_pending` 并入队，不等待下载、视觉或入库；后台完成的 `image_understanding_result` 会安全进入后续 prompt。
- [x] `app/api/routes.py`：记录 pending 和后台完成 debug log；`/api/status.media` 显示当前 prompt pending，`/api/status.media_jobs` 显示后台队列状态。
- [x] `tests/onebot_adapter_test.py`：覆盖普通图片、收藏 / 动画表情、QQ 内置 face、下载字段保留。
- [x] 新增媒体下载单测：本地 path 复制、`get_image` fallback、大小超限、真实 MIME 修正。
- [x] 新增 prompt / graph / queue 单测：图片事件只追加 pending 且不阻塞主回复；后台队列能完成图片理解和静默偷表情；后台图片结果能在后续 prompt 中被使用且不泄露本地路径。

#### 验收

- QQ 私聊发普通图片：当前回复不等待下载 / 视觉；如果混合文字，优先接文字；后台完成后报告里可看到 `image_understanding_result`，后续追问能接上。
- QQ 私聊发表情包 / mface：当前回复不等待偷表情；主线程轻量接住，不输出分析腔；后台完成后报告里可看到 `meme_intake_result`。
- 值得偷的表情包会静默进入 `memes/assets/<category>`，同步 `image_dhash_index.json`，后续可被 `search_meme` 检索并发回 QQ。
- 重复表情不会重复入库，只返回 duplicate debug。
- 下载失败、URL 过期、格式不支持、视觉模型/API 报错时不崩溃、不胡说图片内容；无视觉模型不作为支持场景。
- [ ] 主动消息接入 QQ。
  - 依赖 P0 多用户 session 隔离；每个 `qq_private_<user_id>` 只能向对应 QQ 私聊目标发送。
  - MVP 不默认群发，也不向所有历史联系人发送；公测阶段先关闭 QQ 主动消息，等链路完整后再启用。
  - 主动消息仍走现有主动消息线程和 `ActionDecision -> SendItem` 归一化。
  - 发送前复用 EventGate 检查：HOT/COLD、当天上限、用户近期消息、USER_COMPOSING、snapshot stale。
  - 第一版主动消息可只支持 text item；meme 混发后续再测。
- [ ] 引用回复。
  - 接收用户 `reply` 消息段时，用 `get_msg` 或本地记录还原被引用消息的轻量上下文。
  - 发送侧保存每条用户消息和机器人消息的 `onebot_message_id`。
  - 第一版不新增主 harness action；adapter 根据当前 snapshot 的锚点，在需要时给第一条 outgoing message 前置 OneBot `reply` 段。
  - 优先场景：用户连续多条消息后，助手精准引用其中一条回应；用户主动引用一条旧消息时，助手理解引用上下文。
- [ ] 第二阶段验收。
  - 用户发 QQ 表情或图片后，系统能下载、记录、必要时静默入库。
  - 新入库表情可在后续 `search_meme -> meme` 链路中被选中并发回 QQ。
  - 主动消息能发到绑定 QQ 私聊对象，且不会和用户正在输入 / 刚发消息撞车。
  - 用户引用某条消息时，助手能理解被引用对象；助手必要时也能引用用户消息回复。

### 第三阶段：接入设计中的其他预想功能

第三阶段不在当前实现范围，只保留入口：

- [ ] 撤回接收为 `EventType.RECALL`，并通过 `onebot_message_id` 定位内部事件。
- [ ] 机器人输入状态 `set_input_status`：在 LLM 生成开始时展示“正在输入”，需要节流。
- [ ] 已读回执：`mark_private_msg_as_read` 的调用时机另行决定。
- [ ] QQ 表情回应、主动戳一戳、群聊、语音 / 视频 / 文件理解后置。

### 当前关键阻塞项

- [ ] 第二阶段主动消息依赖 P0 多用户 session 隔离和 session -> QQ 私聊目标绑定，否则产品上不知道该主动发给谁，也无法保证话题和记忆不串人。

## P1 / P2 优化

- [ ] 会话摘要由代码管理版本：约每 80 个可见气泡生成一次 `conversation_checkpoint`，作为明确 cache 重建事件。
- [ ] 扩展 autotest 指标：新增风格预算、emoji 重复、meme 过度使用、同 stem 重复、deterministic downgrade 统计，避免只看 parse 成功率。
