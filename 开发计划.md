# 开发计划 / TODO

本文只保留当前仍未完成或仍需决策的工程事项。已经落地的阶段性任务不在本文归档。

## P0：官方 Agent 回传结构修复

目标：主聊天链路必须按 Mimo / DeepSeek 当前 thinking 模型的官方 OpenAI-compatible Agent 回传方式组织 messages。下一轮请求不能只回传用户可见文本，必须把上一轮 provider 返回的 assistant message 原样作为 assistant 历史回传，包括 `content`、`reasoning_content`、`tool_calls` 等官方 message 字段；usage、cache tokens、finish_reason、response id 等响应元信息只进 debug，不回传到 messages。

实现状态：基础回传结构、Pi-style 通用 transcript 安全层、模型配置切换隔离、通用 cache affinity 和 HOT turn system reminder 已在主聊天 graph 落地并通过单测；真实端点 autotest 仍按下方清单跟踪。

官方依据：

- Mimo deep thinking：多轮/Agent 场景需要回传 `reasoning_content`；工具调用场景丢失 `reasoning_content` 会导致请求错误。参考：https://mimo.mi.com/docs/usage-guide/passing-back-reasoning_content
- DeepSeek thinking mode：多轮示例使用 `messages.append(response.choices[0].message)`；工具调用场景必须保留带 `reasoning_content` 的 assistant message。参考：https://api-docs.deepseek.com/guides/thinking_mode
- Anthropic extended thinking：thinking blocks 属于 assistant 历史的一部分，工具结果后继续调用时必须保留对应 thinking blocks。参考：https://platform.claude.com/docs/en/build-with-claude/extended-thinking
- DeepSeek legacy `deepseek-reasoner` / reasoning_model 文档不再作为本项目兼容目标或设计依据；当前只按 Mimo 与 DeepSeek 当前 thinking 模型的官方回传协议实现。
- role、message 字段和响应元信息边界必须来自当前官方文档/SDK schema；不得为了方便本地 harness 自行发明 role、伪造 tool 消息或把响应级元信息塞进 messages。

兼容性结论：

- 当前 Mimo / DeepSeek thinking 模型的回传协议没有严格冲突，可以统一使用 OpenAI-compatible Agent transcript。
- 下一轮请求统一追加 provider 返回的 `response.choices[0].message` 作为 `role=assistant` 历史，保留其中的 `content`、`reasoning_content`、`tool_calls` 等官方 message 字段。
- 只有真正启用 provider-native `tool_calls` 时，才追加官方 `role=tool` + `tool_call_id` 的工具结果消息；本地 harness、meme 检索和 repair 在切到 provider-native tools 前不得伪造 `tool` role。
- `usage`、`cached_tokens`、`reasoning_tokens`、`finish_reason`、response id、model、created、object 等响应级元信息只进入 debug/trace，不进入 messages。
- Mimo 关于 thinking + tool calls 稳定性的提示属于运行策略差异，不是回传协议冲突；如果真实端点显示不稳定，再按 provider 策略选择关闭 thinking 或降低工具链复杂度。
- 参考 Pi Agent Harness 后，本项目只吸收对聊天产品有用的通用设计：provider transcript 与用户可见历史分离、assistant thinking 作为历史字段回传、空 assistant 不回放、模型配置变化时隔离 provider transcript、cache affinity 可观测。当前不引入完整 agent runtime、不把 `search_meme` 改成 provider-native tool call、不做厂商分支适配。

### P0-1：引入 provider 原始消息层（已落地）

- 已新增 `LLMResponseEnvelope` 数据结构，包含：
  - `assistant_message`: 可再次发送给 provider 的官方 assistant message。
  - `content`: `assistant_message.content`，允许为 `None`。
  - `reasoning_content`: `assistant_message.reasoning_content`，允许为 `None`。
  - `usage`: token、cached token、reasoning token 等 debug 元信息。
  - `raw_response_meta`: `finish_reason`、response id、model 等只读 debug 元信息，不进入下轮 messages。
- `LLMClient` 已新增返回完整 envelope 的调用方法；旧 `chat_completion()` 保留给 `/mem`、`/forget`、主动消息等非主链路，主聊天 graph 使用 envelope。
- 已对 `assistant_message` 做 provider-safe 过滤，只保留官方 messages schema 允许的字段，例如 `role`、`content`、`reasoning_content`、`tool_calls`、`function_call`、`name`、`tool_call_id`。不得把 `usage`、`cached_tokens`、`finish_reason`、`annotations`、`audio`、`refusal` 等响应/调试字段混入下一轮 messages。
- role 白名单已与当前 provider 官方 schema 对齐；当前主链路只使用官方允许的 `system`、`user`、`assistant`，只有 provider-native `tool_calls` 真正启用时才引入官方 `tool` role。
- 已保留 `content: null` 与 `reasoning_content` 的原始语义，不把空 content 自动改写成空字符串再回传。

### P0-2：重构主 prompt transcript 为官方 messages transcript（已落地）

- 已将当前 `_prompt_transcript` 从“用户可见聊天文本历史”升级为“provider messages transcript”。
- COLD 首轮：
  - 追加完整 `role=system` 主系统提示词。
  - 追加本轮固化 `runtime_context` system message。
  - 追加本轮用户输入 `role=user`。
  - 追加本轮最终输出提醒 `role=system`。
  - 调用 LLM 后，把 provider 返回的完整 assistant message 追加到 transcript。
- HOT 后续轮：
  - 不重建旧前缀，不删除、不压缩、不改写已发送过的任何 message。
  - 在尾部追加本轮新增 runtime/system/user/reminder。
  - 在本轮用户输入之后、最终输出提醒之前追加短 `role=system` 的 `SYSTEM_REMINDER / HOT_TURN_REMINDER`，同时提醒 harness 输出格式和聊天风格边界；不重发完整主系统提示词，不伪装成 `role=user`。
  - 调用 LLM 后，继续追加 provider 返回的完整 assistant message。
- 用户可见历史继续单独维护；用户侧只显示最终 `SendItem`，绝不显示 `reasoning_content`、harness JSON、repair 文本或 debug 元信息。
- `/mem`、`/forget`、主动消息和定时记忆任务仍不进入主聊天 transcript；这些链路如需 thinking 适配，单独设计。

### P0-3：按官方 Agent loop 记录内部二轮与修复过程（已落地）

- 主 harness 首轮输出不合法时，先把“不合法的原始 assistant message”追加到 transcript，再追加本地 repair instruction。
- repair instruction 使用官方合法 role 追加；在当前没有 provider-native `tool_calls` 的前提下，不伪造 `role=tool` 或 `tool_call_id`。只有未来切到 provider-native tools 时，才按官方 `assistant.tool_calls -> tool` 消息结构回传。
- repair instruction 必须用 `role=system` 追加，并显式标记为 `SYSTEM_REMINDER / ACTION_HARNESS_PROTOCOL_ERROR`：上一轮失败输出是内部 blocked draft，用户没有看到，不能当成已发送聊天历史；修复任务只是把该草稿重写为合法 action + items。
- repair LLM 返回后，把 repair 的完整 assistant message 继续追加到 transcript，再用其 `content` 解析最终 decision。
- meme 检索二轮同理：先保留第一轮包含 `search_meme` 的完整 assistant message，再追加 `role=system` 的本地检索结果消息，再追加二轮选择的完整 assistant message。
- `search_meme` 已定性为本地 harness 二轮能力，不是 provider-native tool call；本地检索结果可以显式标记为 `SYSTEM_REMINDER / INTERNAL_MEME_SEARCH_RESULT`，但不得伪造 `role=tool` 或 `tool_call_id`。
- 缺失 meme 修复、contextual repair、JSON repair 都按同一条规则：模型每次真实返回的 assistant message 都进入 transcript，代码生成的修复/检索/错误提示只作为新增消息追加，不回写旧消息。

### P0-4：content 为空但 reasoning_content 含可执行 JSON 的处理（已落地）

- 如果 provider 返回 `content is None/""` 且 `reasoning_content` 本身是合法短格式 harness JSON，可将 `reasoning_content` 作为本轮候选 `raw_output` 进入 parser，避免 Mimo fallback 成“嗯”。
- 这一步只是解析兜底，不改变 transcript；transcript 仍回传 provider 原始 assistant message，不能把 CoT 内容复制成普通 `content` 伪造历史。
- debug trace 必须标记 `raw_output_source=reasoning_content_rescue`，并记录 `content_empty_with_reasoning=true`。

### P0-5：Prefix cache 与调试观测改造（已部分落地）

- 已将 append-only 校验改为比较“实际发送给 provider 的 messages”，包含 `reasoning_content` 和所有内部二轮 assistant message。
- 已增加通用 cache affinity：每个本地会话维护稳定 `cache_session_id`，请求携带 `session_id` / `x-client-request-id` / `x-session-affinity` 请求头，并尝试使用 SDK 官方 `prompt_cache_key`；如果端点拒绝该参数，自动降级为不发送 `prompt_cache_key`，不影响主链路运行。
- debug log 已记录：
  - 每轮发送 messages 的 role 序列。
  - assistant message 是否包含 `reasoning_content`。
  - `content_empty_with_reasoning` 次数。
  - `raw_output_source`: `content` / `reasoning_content_rescue` / `repair_content` / `meme_second_round_content`。
  - provider 返回的 prompt tokens、cached tokens、reasoning tokens。
  - `cache_session_id`、`prompt_cache_key`、cache hit rate、cache miss tokens 和 cache 参数降级原因。
- debug log 只用于开发诊断；不得进入用户可见消息、长期记忆、`/api/conversation` 普通展示。

### P0-5b：Pi-style 通用 transcript 安全层（已落地）

- 发送给 provider 前只保留当前项目实际使用的合法 role：`system`、`user`、`assistant`；本地 harness 不伪造 `tool` role。
- assistant 历史只保留官方 message 字段：`role`、`content`、`reasoning_content`、`tool_calls`、`function_call`、`name`、`tool_call_id`。
- 空 assistant message 不进入 provider transcript；条件是 `content`、`reasoning_content`、`tool_calls`、`function_call` 全部为空。
- provider transcript 与当前 LLM identity 绑定；`base_url`、`model`、thinking 配置变化时，清空 provider transcript，下一轮用用户可见历史重建安全上下文，避免把上一模型的 CoT / tool id / 私有字段回放给新模型。
- `/api/config` 更新会触发 provider transcript 隔离和 cache session 重置；不会清空普通用户可见历史。

### P0-6：测试与真实端点验收

- 已完成单测覆盖：
  - provider assistant message 包含 `content + reasoning_content` 时，下一轮 messages 必须原样包含该 assistant message。
  - `content=None`、`reasoning_content` 为合法 harness JSON 时，parser 能走 `reasoning_content_rescue`，且 transcript 不伪造 content。
  - usage/cache/debug 元信息不会进入下一轮 messages。
  - repair、meme 二轮、contextual repair 都会把每次 assistant message append 到 transcript。
  - 请求 N 的 provider messages 必须是请求 N+1 的严格前缀。
  - 空 assistant message 不回放。
  - LLM identity 变化时 provider transcript 隔离，但用户可见历史保留。
  - cache affinity 不改变 messages，cache session 会随配置变化重置。
  - HOT 轮追加 `role=system` 的 `SYSTEM_REMINDER / HOT_TURN_REMINDER`，COLD 轮不追加；该提醒位于当前用户输入之后、最终输出提醒之前。
- [ ] 真实端点 autotest：Mimo 与 DeepSeek 各 20 轮，记录 strict 成功率、reasoning 回传率、空 content rescue 次数、cache 命中率、DeepSeek 协议漂移率。

## P0：Harness 后续修复

- [ ] 增加完整 harness trace 落盘：每轮记录 `initial_raw_output`、`initial_parse_status`、`initial_parse_errors`、`repair_raw_output`、`repair_parse_status`、`repair_parse_errors`、`relaxed_reason`、`contextual_errors` 和 `final_decision`，用于复盘真实端点失败原因。需复用 P0 官方 Agent 回传结构中的 envelope/debug 字段，避免另建一套不一致日志。
- [ ] JSON 数组草稿拦截：`[` 开头的短格式草稿不能走 `natural_text_coerced` 可见发送，必须按 JSON-like 协议错误进入本地规整或 repair。
- [ ] COLD 状态 action 确定性修正：COLD + `REACT` + text item 时本地改为 `ENTER_CHAT`；COLD + 展开型 `REPLY` 本地改为 `ENTER_CHAT`，只把无法确定的复杂情况交给 repair。

## QQ 接入 TODO

- [ ] OneBot reverse WebSocket 服务端入口，例如 `/onebot/ws` 或兼容当前 NapCat 配置的 `/ws`。
- [ ] OneBot 连接管理：连接生命周期、心跳、断线清理、当前连接状态和 `echo` response 关联。
- [ ] OneBot token 校验；token 不进入前端调试面板和普通日志。
- [ ] 私聊 `message[]` 文本段接收并转换为内部 `EventType.TEXT`。
- [ ] 私聊文本发送：把内部 text item 转为 OneBot `text` 消息段。
- [ ] 多 `items` 串行发送：保证顺序，不并发发送，每个 item 发送前继续执行 snapshot stale 检查。
- [ ] 用户 `image` / `mface` 下载到本地临时目录，再把临时文件路径作为 `image_ref` 交给表情包分析/偷取链路。
- [ ] 下载边界：平台资源落地不直接入正式表情库；正式入库仍需经过 `should_steal`、分类命名、dHash 去重、写锁和保存工具链。
- [ ] 戳一戳 / 拍一拍接收并映射为 `EventType.NUDGE`。
- [ ] 撤回接收并映射为 `EventType.RECALL`，通过 OneBot `message_id` 定位内部事件。
- [ ] 本地 `meme` item 转 QQ 图片发送：解析 file stem、定位本地图片、转换为 NapCat/OneBot 可接受的文件引用。

## 记忆与长期上下文 TODO

- [ ] 凌晨整理线程 prompt 增加“共同梗 / 暗号 / 昵称 / 专属表情含义”的轻提示：不新增独立模块，只把长期稳定、自然反复出现或用户通过 `/mem` 明确要求的共同语境，克制写入 `MEMORY_CORE.md` 的“相处习惯”。
- [ ] 把 Markdown 记忆写入链路改成 append-only + 小 JSON 意图：LLM 只输出 `{section, text, ttl, source}` 这类小意图，代码负责去重、排序、状态更新和原子写入。
- [ ] 建立记忆优先级和 TTL：主回复 prompt 视图按“明确偏好 > 正在发生的近期状态 > 未闭合问题 > 普通闲聊”裁剪。

## P1 / P2 优化
- [ ] 会话摘要由代码管理版本：约每 80 个可见气泡生成一次 `conversation_checkpoint`，作为明确 cache 重建事件。
- [ ] 按需提供精确时间信息：当前 P0 每轮追加固化时间快照；后续可改为只有用户提到时间、日期、作息、主动消息等场景才追加精确时间。
- [ ] 建立 prompt cache 回归测试：覆盖 COLD、HOT、表情包二轮、repair、超过 30 轮和跨天。
- [ ] 评估服务商显式 prompt cache 能力：不作为运行依赖，只在具体服务商稳定支持时做增强适配。
- [ ] 重构 COLD/HOT 进入规则以增加 WAIT 机会：COLD 下普通可见 `REPLY` / `LIGHT_ACK` / `REACT` 是否先按 one-shot reply 处理，需要重新评估产品体验后再动状态迁移、prompt 和测试。
