# 项目鲁棒性设计

本文档记录项目在外部能力、平台能力和增强模块不可用时的降级原则。

核心目标：

- 核心聊天体验不能被任何单一外部能力绑死。
- 外部能力失败时，系统应内部降级，而不是在自然聊天中暴露技术错误。
- `MEMORY_CORE.md + dm/ + TOMORROW_TOPICS.md + action/text` 是核心链路。
- 表情包、图片、语音、检索、平台特有事件等都属于增强能力，不能成为必需依赖。

## 1. 软依赖原则

所有外部能力默认按软依赖处理。

包括但不限于：

- QQ / 微信平台事件。
- 拍一拍 / 戳一戳。
- 表情包发送。
- 图片理解。
- 语音能力。
- 记忆检索。
- 外部长期记忆后端。

能力不可用时：

- 不阻塞核心聊天。
- 不伪造不存在的平台事件。
- 不在自然聊天里报技术错误。
- 优先退回更基础的 `action`。
- 错误细节进入日志、调试界面或工程监控。

## 2. 核心 action 降级

核心系统至少必须能使用：

```text
WAIT
REPLY
LIGHT_ACK
```

增强能力失败时，优先按以下方向降级：

```text
REACT -> LIGHT_ACK / REPLY -> WAIT
```

原则：

- 能发短文本就不暴露能力失败。
- 不适合发文本时，可以 `WAIT`。
- 不为了掩盖能力失败而编造内容。

## 3. 典型降级场景

### 3.1 表情包不可用

如果原计划输出：

```json
{
  "action": "REACT",
  "text": "local_sticker:peek"
}
```

但表情包资源或平台发送能力不可用，可以降级为：

```json
{
  "action": "LIGHT_ACK",
  "text": "探头"
}
```

或在语境不适合文字时：

```json
{
  "action": "WAIT",
  "text": null
}
```

### 3.2 拍一拍 / 戳一戳不可用

如果平台无法提供拍一拍 / 戳一戳事件：

- 不支持强制看手机触发。
- 不伪造拍一拍事件。
- 仍保留普通消息触发和主动消息机制。

### 3.3 图片理解不可用

如果用户发图，但图片理解能力不可用：

- 不胡乱描述图片。
- 可以等待用户补充文字。
- 可以轻问一句，例如“这是什么？”
- 如果语境不适合追问，可以 `WAIT`。

### 3.4 记忆检索不可用

如果历史检索、向量库、BM25 或外部长期记忆后端不可用：

- 只使用 `MEMORY_CORE.md` 和近期 `dm/`。
- 不影响 `/mem`、`/forget` 和 Markdown 记忆主链路。
- 不在聊天中告诉用户“检索失败”。

### 3.5 外部长期记忆后端不可用

如果未来接入外部长期记忆系统，该系统必须是可选后端。

不可用时：

- 回退到 `MEMORY_CORE.md + dm/`。
- 不反向污染 Markdown 记忆。
- 不让外部记忆系统反客为主。

## 4. 技术错误边界

技术错误不进入自然聊天。

不应发送：

```text
表情包插件不可用。
图片识别失败。
记忆检索超时。
平台事件缺失。
```

允许进入：

- raw chat log。
- 调试日志。
- WebUI 或开发界面。
- 工程监控系统。

自然聊天中的表现应尽量是：

- 短文本降级。
- 沉默等待。
- 普通回复。
- 不提后台原因。

## 5. 不做的事

当前不做：

- 同时接入过多外部生态。
- 把外部长期记忆系统作为核心依赖。
- 把表情包、图片、语音作为核心聊天必需能力。
- 在自然聊天中解释技术失败。
- 为不可用的平台能力编造替代事件。

优先级：

```text
先保证核心陪伴体验稳定，再增加增强能力。
```

# 需要结合架构看是否能够增强

- [ ] JSON 解析失败时由代码兜底为合法 `ActionDecision`，避免把原始模型输出直接进入可见聊天。
- [ ] `WAIT`、`END_CHAT` 的 `text` 由代码强制置空，避免模型误填文本造成前端误展示。
- [ ] `REACT` 的协议格式由代码校验，只允许 `emoji:*`、`search_meme:<category>:<keywords>`、`meme:<file_stem>` 进入后续链路。
- [ ] 明确表情包请求由代码识别并强制进入 `REACT` 候选链路，降低模型漏判导致的行为不一致。
- [ ] `COLD` 状态下的明确问题、打招呼、点名回应请求由代码保护，避免模型错误输出 `WAIT`。
- [ ] 可见回复文本在发送前做内部协议清洗，拦截 action 名称、文件路径、来源标记、记忆分区名等内部信息。
- [ ] 表情包检索、渲染或平台发送失败时由代码降级到 `LIGHT_ACK`、`REPLY` 或 `WAIT`，不把技术原因暴露给用户。
- [ ] 记忆检索失败时由代码回退到核心记忆或空相关记忆，不在自然聊天中说明检索失败。
- [ ] 模型输出不可恢复或违反硬边界时由代码触发安全重试或保守降级，而不是继续相信该轮输出。
- [ ] 对最终出站消息增加统一可见性网关，集中检查内部协议泄漏、空文本动作、过长回复和不允许的技术错误说明。
- [ ] `dm/YYYY-MM-DD.md` 和 `TOMORROW_TOPICS.md` 优先改为 append-only 写入链路：LLM 只输出 section/text 等小 JSON 追加意图，不返回完整 Markdown；代码负责 section 白名单、单行长度限制、去重、来源/时间/ID/TTL 补全、写前备份、进程内锁和 `os.replace` 原子写入；主回复读取时使用裁剪后的 prompt 视图，避免文件增长后污染上下文。

## 6. 提示词组装与 Prefix Cache 修复清单

目标：让 LLM 请求按 append-only transcript 增长。已经发送过的前缀不要被重排、改写或替换；新的用户消息、助手回复、本轮时间和状态只追加到末尾。这样即使用户继续聊天、时间变化、状态从 COLD 进入 HOT，也能复用前面已经算过的 KV cache，降低延迟和成本，同时不改变用户看到的聊天体验。

当前已确认的致命问题：

- `current_timestamp` 每轮动态生成，且带秒级时间。
- 历史消息只取最近 30 条，超过 30 条后窗口滑动，导致消息前缀持续变化。

除此之外，当前还存在：

- `chat_status`、`msg_index`、`last_message_age` 被渲染进主 system prompt 中部，导致后面的角色和记忆也跟着失去缓存。
- `cold_start_meta` 又在 history 前重复注入 `timestamp/msg_index/last_user_message_age/status`，动态信息重复出现。
- COLD 展开 `SOUL.md/MEMORY_CORE.md/dm/TOMORROW_TOPICS.md`，HOT 改成占位文本。该差异属于当前产品设计，不改产品规则；但 COLD -> HOT 后不能把请求重建成“HOT system prompt 开头”，而应在 COLD 轮次后 append HOT runtime 指令和新用户消息。
- 主回复直接读取完整 Markdown 记忆文件，没有稳定的 prompt 视图、长度上限和版本观测。
- 表情包二轮调用和协议修复调用会复制整套 base messages，再追加动态候选、错误和原始输出。
- 当前没有 prompt block hash、cached tokens、prompt 长度等观测，无法量化缓存是否真的命中。

参考原则：

- Claude Code 的缓存组织方式是每轮重新发送完整上下文，但新内容追加在末尾；API 按请求开头的 prefix 做精确匹配，上一轮未变化的前缀从 cache 读取。
- 因此本项目的关键不是“每轮重新生成一份最新 prompt”，而是“上一轮请求内容在下一轮请求里保持原样作为前缀”。
- 如果中途需要增加新的系统级指令或运行状态，不要修改最前面的 system prompt；应追加为新的 runtime/system 消息，让旧前缀保持不变。
- 官方依据：
  - Anthropic Prompt caching: cache hit 要求 cache breakpoint 前的 prompt segment 100% 完全一致。
  - Anthropic Messages API: Messages API 是无状态的，客户端每轮发送完整 conversation history；mid-conversation system message 追加在历史尾部，不会使它之前的 cached prefix 失效。
  - Anthropic Advisor prompt caching: 第 N 次调用的 prompt 是第 N-1 次调用的 prompt 再追加一个 segment，因此 prefix 稳定。
  - OpenAI Prompt caching: 可缓存内容包括完整 messages array；系统会缓存已处理过的最长 prefix，1024 token 起、之后按 128 token 增量命中。

硬规则：

- [x] 请求 N+1 必须尽量等于“请求 N + 上轮 assistant 输出 + 本轮新增 runtime meta + 本轮用户输入 + 本轮输出提醒”。
- [x] 任何已经出现在请求 N 里的内容，进入请求 N+1 时不得因为状态变化、时间变化、记忆变化而被改写、删除、重排或换位置。
- [x] 每轮 runtime meta、时间戳、输出前强提醒都是当轮固化事件；如果需要靠近输出，就追加一条新的，不回头移动旧的。
- [x] 只有以下事件允许重建前缀：用户清空会话、上下文超限触发 checkpoint、模型/工具/schema/系统提示词版本切换、显式记忆视图版本切换。

### P0：不改变产品体验的必要修复

P0 原则：先修“稳定性和可观测性”，不改聊天人格、不删除记忆、不改变 action 协议，不让用户感知到行为突然变化。

实施状态：P0 主链路已落地。主回复使用 append-only transcript；runtime_context、时间戳和最终输出提醒按轮次固化追加；记忆文件进入主回复前会生成裁剪视图；prompt cache debug log 记录 hash、长度、cached tokens 和 append-only 校验结果。P1 checkpoint 仍未实施。

- [x] 建立 append-only prompt block 分层，不再把所有内容每轮重新拼成一个大 system 字符串。
  - 稳定块 1：身份、Action Harness、JSON schema、COLD/HOT 规则、表情包协议、风格边界、记忆使用规则。
  - 半稳定块 2：`SOUL.md`、`MEMORY_CORE.md`、当日记忆视图、`TOMORROW_TOPICS.md` 视图。
  - 对话块 3：已发送成功的用户消息、助手可见回复、工具结果、当轮固化 runtime meta，按发生顺序 append-only 保留。
  - 本轮尾部块 4：本轮状态、时间、上一条消息间隔、当前 buffer、最终 JSON 输出强提醒。
  - 验收：下一轮请求必须以前一轮请求为前缀，除非发生 `/clear`、摘要压缩、记忆视图版本切换或模型/配置切换。

- [x] 把 `chat_status`、`msg_index`、`last_message_age` 从主 system prompt 移到 append-only runtime meta message。
  - 用途不变：模型仍然知道当前是 COLD/HOT、这是今天第几条消息、距离上一条用户消息多久。
  - 位置改变：这些信息作为当轮运行快照追加到对话尾部；进入历史后不回写、不重排、不覆盖。
  - 验收：连续两轮只改变 `msg_index` 时，上一轮完整请求内容必须在下一轮请求开头保持原样。

- [x] 合并并去重 `cold_start_meta`。
  - 当前 `last_message_age/msg_index/status` 已经在主 prompt 和 `cold_start_meta` 里重复。
  - 保留一个统一的 `runtime_context`，字段包括 `chat_status`、`msg_index_today`、`last_user_message_age`、`cold_start_timestamp`。
  - 验收：同一个动态字段在 messages 中只出现一次。

- [x] 修复 timestamp 组装方式。
  - 时间戳不再作为“当前可变字段”回填到前缀中。
  - 每轮生成一个当轮固化时间快照，优先使用 `YYYY-MM-DD HH:mm 周X`；这条快照像聊天记录一样 append 到当轮上下文，后续请求原样保留。
  - 新一轮需要新时间时，只在末尾追加新的时间快照，不修改旧快照。
  - 验收：上一轮的时间戳在下一轮请求中保持原样；新时间只出现在追加尾部。

- [x] 替换最近 30 条滑动窗口为 append-only transcript；checkpoint 保持为 P1。
  - 默认保留完整 append-only 对话前缀，保证 COLD -> HOT 和 HOT 连续聊天都能命中前缀。
  - P0 不再每轮滑动丢弃旧消息。
  - P1 再在自然断点生成 `conversation_checkpoint`，用 checkpoint 替代更早原文；这是一次明确的 cache 重建事件，而不是每轮滑动。
  - 验收：第 31 条不会自动丢弃第 1 条；只有未来触发 checkpoint 时才压缩早期历史。

- [x] 为 `SOUL.md/MEMORY_CORE.md/dm/TOMORROW_TOPICS.md` 生成“主回复 prompt 视图”。
  - 不是删除原文件，只是读取进 LLM 前做裁剪。
  - P0 决策：COLD 入场或新会话开始时固化一次 profile/memory prompt snapshot；HOT append-only 期间不刷新、不回写、不因为底层 Markdown 文件变化而改写前缀。
  - 下一次 COLD、新会话、显式记忆视图版本切换或 checkpoint 重建时，才读取新的文件视图。
  - 保留标题结构和重要条目；限制单个文件进入 prompt 的最大字符数。
  - `TOMORROW_TOPICS.md` 只给 pending 且未过期的候选；不要把 used/blocked/expired 全量塞给主回复。
  - `dm/YYYY-MM-DD.md` 只给今日仍影响当前聊天的短视图。
  - 验收：文件增长不会线性增加主回复 prompt 长度；同一 COLD/HOT 会话内，已进入请求前缀的记忆视图保持原样。

- [x] 保持 HOT 产品规则，但 COLD -> HOT 必须使用 append-only 组装。
  - 产品决策：HOT 不额外注入短版人设和记忆；COLD 读取完整角色/记忆，并用 `ENTER_CHAT.items` 生成入场回复。
  - cache 目标：COLD 入场后的下一轮 HOT 请求，应以前一轮 COLD 请求为前缀，再 append 助手入场回复、HOT runtime meta 和新用户消息。
  - 技术边界：不把 HOT system prompt 重建到请求开头；如需 HOT 轻量占位或状态提醒，追加到对话尾部。
  - 验收：HOT 仍使用当前轻量占位方案，未新增短版人设/记忆；但 COLD -> HOT 后上一轮 COLD 前缀可命中。

- [x] 为 prompt 组装增加观测字段。
  - 记录每次请求的 `stable_block_hash`、`profile_block_hash`、`runtime_block_hash`、总字符数、估算 token 数、模型返回的 cached tokens（如果服务商返回）。
  - P0 不做产品经理可见的调试界面；只写入面向开发人员的 debug log。
  - debug log 默认不记录完整 prompt 正文，避免隐私和日志膨胀；只记录 hash、长度、cached tokens、append-only 校验结果和 prefix rebuild 原因。
  - 验收：开发人员可以通过日志判断缓存命中率变化、是否发生前缀重建、重建原因是什么。

- [x] 表情包二轮和协议修复沿用同一个 base prompt，不重新临时拼接不同结构。
  - 第一轮、表情包二轮、repair 轮都使用同一套分块函数。
  - 二轮只追加：上轮 decision、候选表情、错误列表、修复要求。
  - 验收：二轮请求的前缀必须等于第一轮请求的 base messages。

### P1：需要小步验证的优化

P1 原则：进一步降低 token、提高长期聊天质量，但可能轻微影响模型知道多少上下文，需要灰度和观察。

- [ ] 按需提供时间信息。
  - P0 已采用“每轮固化时间快照 append-only”，不会回写旧前缀。
  - P1 可以进一步改成：只有用户提到时间、日期、今天/明天/刚才/等会儿、作息、主动消息时才追加精确时间。
  - 好处：更稳定、更省 token。
  - 风险：模型在普通聊天里可能少一点“早晚语境”。

- [ ] 建立记忆优先级和 TTL。
  - `MEMORY_CORE.md` 继续长期保留。
  - `dm` 和 `TOMORROW_TOPICS` 的 prompt 视图按优先级筛选：明确偏好 > 正在发生的近期状态 > 未闭合问题 > 普通闲聊。
  - 过期内容不进入主 prompt，但仍保留在文件或归档中。
  - 好处：降低上下文污染。
  - 风险：低优先级旧话题可能不会被主动想起。

- [ ] 把 Markdown 记忆写入链路改成 append-only + 小 JSON 意图。
  - LLM 不再返回完整 Markdown 文件，只返回 `{section, text, ttl, source}` 这类小意图。
  - 代码负责去重、排序、状态更新、原子写入。
  - 好处：降低文件被 LLM 整体改坏的风险，也让 prompt 视图更可控。
  - 风险：需要一次比较完整的记忆模块改造。

- [ ] 会话摘要由代码管理版本。
  - P1 决策：约每 80 个可见气泡做一次 `conversation_checkpoint` 压缩；当前主流长上下文模型窗口足够大，该问题优先级低于 P0 append-only 修复。
  - 摘要只保留未来仍有用的信息，不复述完整聊天。
  - 好处：解决无限长聊天和最近 N 条滑窗的问题。
  - 风险：摘要质量会影响长期上下文，需要调试和人工查看。

- [ ] 建立 prompt cache 回归测试。
  - 固定一组模拟对话，连续构建多轮 messages。
  - 断言请求 N 的 messages 序列必须是请求 N+1 的前缀；新增内容只能 append 到尾部。
  - 单独记录 full_request_hash、prefix_hash、append_only_check，不再只看稳定块 hash。
  - 覆盖 COLD、HOT、表情包二轮、repair、超过 30 轮、跨天。
  - 好处：以后改 prompt 不会无意破坏 cache。

- [ ] 评估是否使用服务商显式 prompt cache 能力。
  - P0 工程决策：不绑定任何服务商的显式 cache 能力；先保证本地 messages 完整 append-only。
  - 如果当前模型/网关返回 cached tokens 统计，就记录到 debug log；如果不返回，不报错、不影响系统运行。
  - P1 再按具体服务商能力评估 cache key、cache control、cached tokens 等增强适配。
  - 风险：不同服务商能力不一致，不能把系统运行绑死在某一家能力上。

### 需要产品侧确认的取舍

这些不是技术做不到，而是会影响“她怎么聊天”的产品感，需要先确认方向：

- [x] 是否以完整 append-only transcript 作为 P0 最高优先级？
  - 结论：是。为了完整缓存命中和记忆连贯性，请求历史必须尽量完整保留并按顺序追加。
  - 执行口径：不再使用最近 30 条滑动窗口作为主链路；不因为 COLD/HOT 状态变化重建请求开头；时间戳、状态和输出提醒都作为当轮固化事件保留。
  - 允许例外：用户清空会话、上下文超限触发 checkpoint、模型/工具/schema/系统提示词版本切换、显式记忆视图版本切换。

- [x] 是否依赖服务商显式 prompt cache 能力？
  - 结论：不依赖。P0 只保证本地 messages 完整 append-only 和 debug log 观测。
  - 执行口径：服务商返回 cached tokens 就记录；不返回也不报错。不得把系统运行绑死在某一家模型或网关的 cache control 能力上。
  - P1 可选：后续按具体服务商能力做增强适配。

- [x] HOT 状态是否也始终带一份短版角色和记忆？
  - 结论：不带，保持现有 HOT 方案。
  - 通俗解释：COLD 入场时已经读取完整角色和记忆，并可以一次性返回进入热聊的回复；进入 HOT 后主要靠最近聊天继续接话，更像正常连续聊天。
  - 执行口径：不新增 HOT 短版人设/记忆；但 COLD -> HOT 的请求组装必须 append-only，不能把 HOT prompt 重建到请求开头。

- [x] 普通闲聊是否每轮都需要知道准确当前时间？
  - 通俗解释：时间不是不能给，而是不能每轮回头改前面的“当前时间”。正确做法是把每轮当时的时间做成固化快照，像聊天记录一样追加。
  - 结论：P0 每轮追加分钟级固化时间快照，并完整保留前面对话里的旧时间戳；后续请求只追加新时间，不修改旧时间。
  - P1 可选：如果未来 token 压力明显，再讨论是否减少精确时间快照频率。

- [x] 超过 30 轮后，旧原文是“继续原样塞给模型”，还是“变成摘要”？
  - 通俗解释：为了缓存，不能每轮滑动删除旧消息；应该先 append-only 保留，等到自然断点或超阈值时一次性压缩成摘要。
  - 结论：P0 取消最近 30 条滑动窗口；P1 约每 80 个可见气泡做一次 checkpoint 压缩。
  - 优先级：该问题重要性低于 P0 append-only 修复，不阻塞先修缓存命中主链路。

- [x] 记忆文件很长时，是否允许主回复只读取裁剪视图？
  - 通俗解释：不是删记忆，只是她每次回话不会把所有旧资料都翻一遍。
  - 结论：允许，并且记忆视图在一次 COLD/HOT 会话内固化。
  - 执行口径：COLD 入场或新会话开始时生成一次 profile/memory prompt snapshot；HOT append-only 期间不刷新这张小抄。后台 Markdown 文件变化不会回头改当前请求前缀，下一次 COLD、新会话、显式版本切换或 checkpoint 重建时再读取新视图。
  - 产品含义：保留原始记忆文件完整性，同时让当前聊天更连贯、更利于缓存命中。

- [x] 调试界面是否允许查看完整 prompt？
  - 结论：P0 不做产品界面展示，改为开发人员 debug log。
  - 执行口径：日志记录 hash、长度、cached tokens、append-only 校验结果、prefix rebuild 原因；默认不记录完整 prompt 正文。
  - 产品含义：产品经理不需要在界面里看 prompt 细节，开发人员能通过日志排查缓存命中即可。
