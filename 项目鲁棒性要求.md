# 项目鲁棒性设计

本文档记录项目在外部能力、平台能力和增强模块不可用时的降级原则。

核心目标：

- 核心聊天体验不能被任何单一外部能力绑死。
- 外部能力失败时，系统应内部降级，而不是在自然聊天中暴露技术错误。
- `MEMORY_CORE.md + dm/ + TOMORROW_TOPICS.md + action/text` 是核心链路。
- 表情包、图片、语音、检索、平台特有事件等都属于增强能力，不能成为必需依赖。

## 1. 软依赖原则

所有外部能力默认按软依赖处理。

包括但不限于：

- QQ / 微信平台事件。
- 拍一拍 / 戳一戳。
- 表情包发送。
- 图片理解。
- 语音能力。
- 记忆检索。
- 外部长期记忆后端。

能力不可用时：

- 不阻塞核心聊天。
- 不伪造不存在的平台事件。
- 不在自然聊天里报技术错误。
- 优先退回更基础的 `action`。
- 错误细节进入日志、调试界面或工程监控。

## 2. 核心 action 降级

核心系统至少必须能使用：

```text
WAIT
REPLY
LIGHT_ACK
```

增强能力失败时，优先按以下方向降级：

```text
REACT -> LIGHT_ACK / REPLY -> WAIT
```

原则：

- 能发短文本就不暴露能力失败。
- 不适合发文本时，可以 `WAIT`。
- 不为了掩盖能力失败而编造内容。

## 3. 典型降级场景

### 3.1 表情包不可用

如果原计划输出：

```json
{
  "action": "REACT",
  "text": "local_sticker:peek"
}
```

但表情包资源或平台发送能力不可用，可以降级为：

```json
{
  "action": "LIGHT_ACK",
  "text": "探头"
}
```

或在语境不适合文字时：

```json
{
  "action": "WAIT",
  "text": null
}
```

### 3.2 拍一拍 / 戳一戳不可用

如果平台无法提供拍一拍 / 戳一戳事件：

- 不支持强制看手机触发。
- 不伪造拍一拍事件。
- 仍保留普通消息触发和主动消息机制。

### 3.3 图片理解不可用

如果用户发图，但图片理解能力不可用：

- 不胡乱描述图片。
- 可以等待用户补充文字。
- 可以轻问一句，例如“这是什么？”
- 如果语境不适合追问，可以 `WAIT`。

### 3.4 记忆检索不可用

如果历史检索、向量库、BM25 或外部长期记忆后端不可用：

- 只使用 `MEMORY_CORE.md` 和近期 `dm/`。
- 不影响 `/mem`、`/forget` 和 Markdown 记忆主链路。
- 不在聊天中告诉用户“检索失败”。

### 3.5 外部长期记忆后端不可用

如果未来接入外部长期记忆系统，该系统必须是可选后端。

不可用时：

- 回退到 `MEMORY_CORE.md + dm/`。
- 不反向污染 Markdown 记忆。
- 不让外部记忆系统反客为主。

## 4. 技术错误边界

技术错误不进入自然聊天。

不应发送：

```text
表情包插件不可用。
图片识别失败。
记忆检索超时。
平台事件缺失。
```

允许进入：

- raw chat log。
- 调试日志。
- WebUI 或开发界面。
- 工程监控系统。

自然聊天中的表现应尽量是：

- 短文本降级。
- 沉默等待。
- 普通回复。
- 不提后台原因。

## 5. 不做的事

当前不做：

- 同时接入过多外部生态。
- 把外部长期记忆系统作为核心依赖。
- 把表情包、图片、语音作为核心聊天必需能力。
- 在自然聊天中解释技术失败。
- 为不可用的平台能力编造替代事件。

优先级：

```text
先保证核心陪伴体验稳定，再增加增强能力。
```
