# 项目鲁棒性设计

本文档记录项目在外部能力、平台能力和增强模块不可用时的降级原则。

核心目标：

- 核心聊天体验不能被任何单一外部能力绑死。
- 外部能力失败时，系统应内部降级，而不是在自然聊天中暴露技术错误。
- `MEMORY_CORE.md + dm/ + TOMORROW_TOPICS.md + action/items` 是核心链路。
- 表情包、图片、语音、检索、平台特有事件等都属于增强能力，不能成为必需依赖。

## 1. 软依赖原则

所有外部能力默认按软依赖处理。

包括但不限于：

- QQ / 微信平台事件。
- 拍一拍 / 戳一戳。
- 表情包发送。
- 图片理解。
- 语音能力。
- 记忆检索。
- 外部长期记忆后端。

能力不可用时：

- 不阻塞核心聊天。
- 不伪造不存在的平台事件。
- 不在自然聊天里报技术错误。
- 优先退回更基础的 `action`。
- 错误细节进入日志、调试界面或工程监控。

## 2. 核心 action 降级

核心系统至少必须能使用：

```text
WAIT
REPLY
LIGHT_ACK
```

增强能力失败时，优先按以下方向降级：

```text
REACT -> LIGHT_ACK / REPLY -> WAIT
```

原则：

- 能发短文本就不暴露能力失败。
- 不适合发文本时，可以 `WAIT`。
- 不为了掩盖能力失败而编造内容。

## 3. 典型降级场景

### 3.1 表情包不可用

如果原计划输出：

```json
{
  "action": "REACT",
  "items": [
    {"meme": "observing_cat_peeking_door"}
  ]
}
```

但表情包资源或平台发送能力不可用，可以降级为：

```json
{
  "action": "LIGHT_ACK",
  "items": [
    {"text": "探头"}
  ]
}
```

或在语境不适合文字时：

```json
{
  "action": "WAIT",
  "items": null
}
```

### 3.2 拍一拍 / 戳一戳不可用

如果平台无法提供拍一拍 / 戳一戳事件：

- 不支持强制看手机触发。
- 不伪造拍一拍事件。
- 仍保留普通消息触发和主动消息机制。

### 3.3 图片理解不可用

如果用户发图，但图片理解能力不可用：

- 不胡乱描述图片。
- 可以等待用户补充文字。
- 可以轻问一句，例如“这是什么？”
- 如果语境不适合追问，可以 `WAIT`。

### 3.4 记忆检索不可用

如果历史检索、向量库、BM25 或外部长期记忆后端不可用：

- 只使用 `MEMORY_CORE.md` 和近期 `dm/`。
- 不影响 `/mem`、`/forget` 和 Markdown 记忆主链路。
- 不在聊天中告诉用户“检索失败”。

### 3.5 外部长期记忆后端不可用

如果未来接入外部长期记忆系统，该系统必须是可选后端。

不可用时：

- 回退到 `MEMORY_CORE.md + dm/`。
- 不反向污染 Markdown 记忆。
- 不让外部记忆系统反客为主。

## 4. 技术错误边界

技术错误不进入自然聊天。

不应发送：

```text
表情包插件不可用。
图片识别失败。
记忆检索超时。
平台事件缺失。
```

允许进入：

- raw chat log。
- 调试日志。
- WebUI 或开发界面。
- 工程监控系统。

自然聊天中的表现应尽量是：

- 短文本降级。
- 沉默等待。
- 普通回复。
- 不提后台原因。

## 5. 不做的事

当前不做：

- 同时接入过多外部生态。
- 把外部长期记忆系统作为核心依赖。
- 把表情包、图片、语音作为核心聊天必需能力。
- 在自然聊天中解释技术失败。
- 为不可用的平台能力编造替代事件。

优先级：

```text
先保证核心陪伴体验稳定，再增加增强能力。
```

# 剩余增强项

- [ ] `COLD` 状态下的明确问题、打招呼、点名回应请求由代码保护，避免模型错误输出 `WAIT`。
- [ ] 对最终出站消息增加统一可见性网关，集中检查内部协议泄漏、空文本动作、过长回复和不允许的技术错误说明。
- [ ] `dm/YYYY-MM-DD.md` 和 `TOMORROW_TOPICS.md` 优先改为 append-only 写入链路：LLM 只输出 section/text 等小 JSON 追加意图，不返回完整 Markdown；代码负责 section 白名单、单行长度限制、去重、来源/时间/ID/TTL 补全、写前备份、进程内锁和 `os.replace` 原子写入；主回复读取时使用裁剪后的 prompt 视图，避免文件增长后污染上下文。

## 6. 提示词组装与 Prefix Cache 修复清单

目标：让 LLM 请求按官方 Agent messages transcript 增长。已经发送过的前缀不要被重排、改写或替换；新的用户消息、provider 原始 assistant message、本轮时间和状态、内部 repair/meme 二轮消息只追加到末尾。这样即使用户继续聊天、时间变化、状态从 COLD 进入 HOT，也能复用前面已经算过的 KV cache，同时满足 Mimo / DeepSeek thinking 模型对 `reasoning_content` 回传的要求。

当前落地设计：

- 主回复使用完整 append-only transcript，不再使用最近 30 条滑动窗口作为主链路。
- 请求 N+1 必须等于“请求 N + provider 返回的完整 assistant message + 本轮新增 runtime/system/user/reminder 消息”。不能只追加用户可见文本。
- 已进入历史前缀的内容不因为状态变化、时间变化或记忆文件变化而回写、删除、重排。
- 每轮 `runtime_context`、时间戳和最终输出提醒都是当轮固化事件，只追加到末尾。
- 只有用户清空会话、上下文超限触发 checkpoint、模型/工具/schema/系统提示词版本切换、显式记忆视图版本切换等事件允许重建前缀。
- `chat_status`、`msg_index_today`、`last_user_message_age` 和时间快照作为 append-only runtime meta message 提供，不再回填到主 system prompt 中部。
- `SOUL.md`、`MEMORY_CORE.md`、`dm` 和 `TOMORROW_TOPICS.md` 进入主回复前生成裁剪视图；一次 COLD/HOT 会话内已固化的视图不因底层文件变化而改写。
- COLD -> HOT 不重建请求开头；HOT 每轮在本轮用户输入之后、最终输出提醒之前追加短 `role=system` 的 `SYSTEM_REMINDER / HOT_TURN_REMINDER`，同时提醒 harness 输出格式和聊天风格边界；不重发完整主系统提示词，不伪装成 `role=user`。
- prompt cache debug log 记录实际发送 messages 的 role 序列、block hash、长度、估算 token、服务商返回的 cached tokens、reasoning tokens、append-only 校验结果和 prefix rebuild 原因。
- 表情包二轮和协议修复沿用同一个 transcript：先追加模型真实返回的完整 assistant message，再追加本地检索结果、错误或修复要求，再追加二轮/修复返回的完整 assistant message。
- `search_meme` 是本地 harness 二轮能力，不是 provider-native tool call；本地表情检索结果使用 `role=system` 追加，并可显式标记为 `SYSTEM_REMINDER / INTERNAL_MEME_SEARCH_RESULT`，不得伪造 `role=tool`、`tool_call_id` 或 provider-native tool result。
- 协议修复提示必须用 `role=system` 追加，并显式标记为 `SYSTEM_REMINDER / ACTION_HARNESS_PROTOCOL_ERROR`：上一轮失败输出是内部 blocked draft，用户没有看到，不能当成已发送聊天历史；修复任务只是把该草稿重写为合法 `action + items`。
- `reasoning_content` / CoT 只作为 provider assistant message 字段和 debug 信息回传，不进入用户可见输出、长期记忆或普通聊天记录。
- usage、cached tokens、finish_reason、response id 等响应元信息只做 debug，不混入下一轮 messages。
- role、message 字段和响应元信息边界必须按当前 Mimo / DeepSeek thinking 模型官方 schema 实现；不得自行发明 role 或伪造 provider-native tool 消息。
- DeepSeek legacy `deepseek-reasoner` / reasoning_model 文档不作为当前设计依据。
- 发送前执行通用 provider-safe 过滤：当前项目只发送 `system`、`user`、`assistant`；assistant 历史只保留官方 message 字段；空 assistant message 不回放。
- provider transcript 与当前 LLM identity 绑定；`base_url`、`model` 或 thinking 配置变化时清空 provider transcript，并用用户可见历史重建安全上下文。
- 本地 cache affinity 使用稳定 `cache_session_id`、请求头和 SDK 官方 `prompt_cache_key`；如果端点拒绝 `prompt_cache_key`，自动降级，不影响主链路运行。

参考原则：

- Prompt cache 命中依赖请求开头 prefix 精确一致。
- 每轮重新发送完整上下文没有问题，关键是上一轮请求内容在下一轮请求里保持原样作为前缀。
- 如果中途需要增加新的系统级指令或运行状态，不修改最前面的 system prompt，而是追加为新的 runtime/system 消息。
- thinking 模型的官方 Agent 回传要求优先于本地“可见聊天文本历史”抽象；本地显示历史和 provider messages transcript 必须分离维护。
- 当前不引入完整 agent runtime、不把 `search_meme` 改成 provider-native tool call、不做厂商分支适配；只吸收成熟 Agent 项目的通用 transcript / cache / 安全过滤原则。

### P1：需要小步验证的优化

P1 原则：进一步降低 token、提高长期聊天质量，但可能轻微影响模型知道多少上下文，需要灰度和观察。

- [ ] 按需提供时间信息。
  - P0 已采用“每轮固化时间快照 append-only”，不会回写旧前缀。
  - P1 可以进一步改成：只有用户提到时间、日期、今天/明天/刚才/等会儿、作息、主动消息时才追加精确时间。
  - 好处：更稳定、更省 token。
  - 风险：模型在普通聊天里可能少一点“早晚语境”。

- [ ] 建立记忆优先级和 TTL。
  - `MEMORY_CORE.md` 继续长期保留。
  - `dm` 和 `TOMORROW_TOPICS` 的 prompt 视图按优先级筛选：明确偏好 > 正在发生的近期状态 > 未闭合问题 > 普通闲聊。
  - 过期内容不进入主 prompt，但仍保留在文件或归档中。
  - 好处：降低上下文污染。
  - 风险：低优先级旧话题可能不会被主动想起。

- [ ] 把 Markdown 记忆写入链路改成 append-only + 小 JSON 意图。
  - LLM 不再返回完整 Markdown 文件，只返回 `{section, text, ttl, source}` 这类小意图。
  - 代码负责去重、排序、状态更新、原子写入。
  - 好处：降低文件被 LLM 整体改坏的风险，也让 prompt 视图更可控。
  - 风险：需要一次比较完整的记忆模块改造。

- [ ] 会话摘要由代码管理版本。
  - P1 决策：约每 80 个可见气泡做一次 `conversation_checkpoint` 压缩；当前主流长上下文模型窗口足够大，该问题优先级低于 P0 append-only 修复。
  - 摘要只保留未来仍有用的信息，不复述完整聊天。
  - 好处：解决无限长聊天和最近 N 条滑窗的问题。
  - 风险：摘要质量会影响长期上下文，需要调试和人工查看。

- [ ] 建立 prompt cache 回归测试。
  - 固定一组模拟对话，连续构建多轮 messages。
  - 断言请求 N 的 messages 序列必须是请求 N+1 的前缀；新增内容只能 append 到尾部。
  - 单独记录 full_request_hash、prefix_hash、append_only_check，不再只看稳定块 hash。
  - 覆盖 COLD、HOT、表情包二轮、repair、超过 30 轮、跨天。
  - 好处：以后改 prompt 不会无意破坏 cache。

- [ ] 评估是否使用服务商显式 prompt cache 能力。
  - P0 工程决策：不绑定任何服务商的显式 cache 能力；先保证本地 messages 完整 append-only。
  - 如果当前模型/网关返回 cached tokens 统计，就记录到 debug log；如果不返回，不报错、不影响系统运行。
  - P1 再按具体服务商能力评估 cache key、cache control、cached tokens 等增强适配。
  - 风险：不同服务商能力不一致，不能把系统运行绑死在某一家能力上。
