# 技术架构

## 1. 当前结论

项目底层推荐采用：

```text
FastAPI
  + 自研实时事件门 EventGate
  + LangGraph
  + Pydantic BaseModel
  + SQLite first, Postgres later
  + SQLAlchemy / Alembic
  + Markdown 记忆文件
  + APScheduler first, Celery / Taskiq later
  + Vite / React / TypeScript WebUI
  + 本地表情包目录
  + 平台适配层
```

核心判断：

- `EventGate` 负责消息缓冲、single-flight、snapshot 过期作废、撤回、命令分流、主动消息撞车和发送幂等。
- `LangGraph` 负责主回复决策图、节点编排、断点恢复和可选内部检索循环。
- 自研 Action Harness 负责严格单 JSON 解析、协议校验、一次修复和最终可执行性校验；不依赖 LangChain structured output 或 provider `response_format` 作为可信边界。
- 可见性和风格预算网关负责发送前的产品体验约束，例如协议泄漏、过长回复、emoji 高频重复、meme 过度使用和同一表情反复发送；结构合法不等于产品可发送。
- 主聊天底层维护两份历史：用户可见历史只给产品展示、记忆、导出和普通接口使用；provider transcript 只给主 LLM 请求使用，用于 append-only、CoT/reasoning 回传、repair/meme 二轮和 prefix cache 命中。
- `Pydantic BaseModel` 只负责内部数据对象的定义、归一化和校验，不作为 Agent 框架使用。
- `FastAPI` 负责 WebUI、平台回调、调试接口和本地服务入口。
- 数据库负责机器可读运行数据，Markdown 文件负责用户可读记忆和角色配置。
- 定时任务系统负责记忆分析、凌晨整理和主动消息调度。
- QQ / 微信 / WebUI 都通过平台适配层接入，不侵入核心对话运行时。

防误用边界：

- 不要把 provider transcript 当成聊天记录使用。它可能包含系统提示词、runtime 状态、harness JSON、repair 指令、meme 检索结果、provider 原始 assistant message 和 `reasoning_content`。
- 聊天展示、记忆模块、用户历史查询、导出和普通 API 默认只能读取用户可见历史。
- provider transcript 只能在主 LLM 链路和开发调试中使用；调试页默认展示摘要，例如 role 序列、hash、append-only、cached tokens、raw output source，不默认暴露完整 CoT 或完整系统提示词。
- HOT 轮的 `SYSTEM_REMINDER / HOT_TURN_REMINDER` 是 provider transcript 内部系统消息，用于本轮 harness 与聊天风格约束；它不是用户消息，不进入用户可见历史、记忆、导出或普通 API。
- `/mem`、`/forget`、主动消息、定时记忆任务不读取 provider transcript；这些链路需要上下文时使用用户可见历史或单独设计的输入。

一句话原则：

```text
用 EventGate 管这条结果现在还能不能发，用 LangGraph 管主回复决策流程，用自研 Action Harness 管 LLM 原始输出能不能变成可执行动作，用自己的转发层管什么能被用户看到。
```

### 1.1 运行鲁棒性与 Provider Transcript

核心聊天体验不能被任何单一外部能力绑死。QQ / 微信平台事件、拍一拍 / 戳一戳、表情包发送、图片理解、语音能力、记忆检索和外部长期记忆后端都按软依赖处理。

能力不可用时：

- 不阻塞核心聊天。
- 不伪造不存在的平台事件。
- 不在自然聊天里报技术错误。
- 优先退回更基础的 `action`。
- 错误细节进入 raw log、调试界面或工程监控。

核心 action 降级方向：

```text
REACT -> LIGHT_ACK / REPLY -> WAIT
```

典型降级：

- 表情包不可用时，优先降级为短文本 `LIGHT_ACK` / `REPLY`；语境不适合文字时可 `WAIT`。
- 平台没有拍一拍 / 戳一戳事件时，不伪造事件，仍保留普通消息触发和主动消息机制。
- 图片理解不可用时，不胡乱描述图片，可以等待用户补充文字或轻问一句。
- 记忆检索不可用时，只使用 `MEMORY_CORE.md` 和近期 `dm/`，不在聊天里告诉用户“检索失败”。

Provider transcript 的 prompt cache 与 thinking 回传规则：

- 主回复使用完整 append-only provider transcript，不使用最近 30 条滑动窗口作为主链路。
- 请求 N+1 必须等于“请求 N + provider 返回的完整 assistant message + 本轮新增 runtime/system/user/reminder 消息”。不能只追加用户可见文本。
- 已进入历史前缀的内容不因为状态变化、时间变化或记忆文件变化而回写、删除、重排。
- 每轮 `runtime_context`、时间戳和最终输出提醒都是当轮固化事件，只追加到末尾。
- COLD -> HOT 不重建请求开头；HOT 每轮在本轮用户输入之后、最终输出提醒之前追加短 `role=system` 的 `SYSTEM_REMINDER / HOT_TURN_REMINDER`。
- 表情包二轮和协议修复沿用同一个 transcript：先追加模型真实返回的完整 assistant message，再追加本地检索结果、错误或修复要求，再追加二轮/修复返回的完整 assistant message。
- `search_meme` 是本地 harness 二轮能力，不是 provider-native tool call；不得伪造 `role=tool`、`tool_call_id` 或 provider-native tool result。
- `reasoning_content` / CoT 只作为 provider assistant message 字段和 debug 信息回传，不进入用户可见输出、长期记忆或普通聊天记录。
- usage、cached tokens、finish_reason、response id 等响应元信息只做 debug，不混入下一轮 messages。
- provider transcript 与当前 LLM identity 绑定；`base_url`、`model` 或 thinking 配置变化时清空 provider transcript，并用用户可见历史重建安全上下文。
- 本地 cache affinity 使用稳定 `cache_session_id`、请求头和 SDK 官方 `prompt_cache_key`；如果端点拒绝 `prompt_cache_key`，自动降级，不影响主链路运行。

## 2. 为什么主框架选 LangGraph

本项目的核心不是多 Agent 协作，也不是让模型自由调用工具完成任务，而是一个长期运行的陪伴运行时。

它需要处理：

- 消息缓冲。
- single-flight。
- snapshot 过期作废。
- 撤回静默取消。
- 发送幂等。
- 离线生活态和热聊在场态。
- 用户连续输入时的等待判断。
- `WAIT / REPLY / LIGHT_ACK / REACT / ENTER_CHAT / END_CHAT` 动作路由。
- `items:null` 或空时静默，不向用户发送任何内容。
- 拍一拍 / 戳一戳强制刷新注意力。
- `{"search_meme":"<category>:<keywords>"}` 到 `{"meme":"<file_stem>"}` 的内部检索闭环。
- `search_meme` 是本地 harness 二轮能力，不是 provider-native tool call；当前不引入完整 agent tool loop，也不伪造 `role=tool`。
- 发送前风格预算检查，防止模型把“积极使用表情包”退化成每轮固定 `REACT + meme + emoji` 模板。
- raw chat log 记录。
- 定时记忆分析。
- 凌晨整理线程。
- 主动消息候选读取和发送。
- 未来 QQ / 微信 / WebUI 多入口共存。

这些需求更接近状态机和事件流，而不是传统 chatbot 的单轮问答。

LangGraph 适合作为底层，是因为它可以承载：

- 显式状态。
- 节点化流程。
- checkpoint / persistence。
- 运行中断后的恢复。
- 长流程拆分。
- 后续增加人工介入、回放、调试和观测。

但本项目不应该直接套用 LangGraph 预置的 ReAct Agent 模式。我们只使用它的低层图编排能力。

同时，实时发送一致性不完全交给 LangGraph。MVP 需要一个自研 `EventGate`，它可以放在 LangGraph 外围，也可以实现为图前置层，但职责必须独立：

```text
EventGate：这次结果现在还能不能发
LangGraph：这次如果要处理，应该怎么思考和决策
```

## 3. Pydantic 的职责边界

这里建议使用的是 `Pydantic BaseModel`，不是把 `Pydantic AI` 作为主 Agent 框架。

当前落地是“自研 Action Harness + Pydantic 内部模型”：

1. Harness 严格解析单个 JSON object。
2. Harness 校验 `action + items` 协议，结构或协议错误时最多反馈给模型修复一次。
3. Pydantic 负责 `ActionDecision` / `SendItem` 的内部归一化和稳定数据对象。
4. 最终发送前仍要校验 `search_meme` 已被替换或丢弃、`meme` 的 `file_stem` 能真实命中。
5. 产品体验类约束由发送前网关承担，不塞进 Pydantic 数据模型。

示例：

```python
from enum import Enum
from pydantic import BaseModel, model_validator


class Action(str, Enum):
    WAIT = "WAIT"
    REPLY = "REPLY"
    LIGHT_ACK = "LIGHT_ACK"
    REACT = "REACT"
    ENTER_CHAT = "ENTER_CHAT"
    END_CHAT = "END_CHAT"


class SendItem(BaseModel):
    # LLM-facing item 是单键短格式：
    # {"text": "..."} / {"meme": "file_stem"} / {"search_meme": "category:keywords"}
    # 内部对象仍保留 type/content，方便路由、渲染和平台适配。
    type: str  # text / meme / search_meme
    content: str


class ActionDecision(BaseModel):
    action: Action
    items: list[SendItem] | None = None

    @model_validator(mode="after")
    def validate_action_items(self):
        if self.action in (Action.WAIT, Action.END_CHAT) and self.items:
            raise ValueError("WAIT/END_CHAT actions require no visible items")

        if self.action == Action.REPLY and not self.items:
            raise ValueError("REPLY action requires at least one item")

        if self.action == Action.LIGHT_ACK and not self.items:
            raise ValueError("LIGHT_ACK action requires short visible item")

        if self.action == Action.REACT and not any(item.type in ("meme", "search_meme") for item in self.items or []):
            raise ValueError("REACT action requires meme/search_meme item")

        return self
```

这样不会和 LangGraph 抢职责：

```text
LangGraph：什么时候运行、运行到哪一步、状态如何恢复
Action Harness：这一轮 LLM 原始输出是否能成为可执行 ActionDecision
Pydantic：ActionDecision / SendItem 的内部归一化和数据对象
```

## 4. 最小核心架构

### 4.1 运行入口

第一阶段可以只做 WebUI。

```text
WebUI
  ↓
FastAPI
  ↓
实时事件门 EventGate
  ↓
LangGraph 决策图
  ↓
转发层
```

第二阶段接入 QQ 时新增平台适配层：

```text
QQ / NapCat / OneBot
  ↓
平台适配层
  ↓
实时事件门 EventGate
  ↓
LangGraph 决策图
  ↓
平台适配层发送
```

`EventGate` 不关心消息来自 WebUI 还是 QQ，只接收统一事件。

### 4.2 统一事件结构

早期可以先保持简单：

```text
ChatEvent
  - event_id
  - platform
  - user_id
  - event_type
  - text
  - timestamp
  - raw
```

`event_type` 可以包含：

- `message.text`
- `message.image`
- `nudge`
- `recall`
- `command.mem`
- `command.forget`

第一阶段 WebUI 至少需要支持：

- 普通文本输入。
- 模拟拍一拍 / 戳一戳。
- 查看和编辑 `SOUL.md`。

### 4.3 实时事件门 EventGate

`EventGate` 是 MVP 的核心运行层之一。

它不是第二个聊天大脑，也不判断情绪、不改写文本、不做人造延迟。它只负责：

```text
这条回复现在还能不能发。
```

MVP 职责：

- 接收统一事件并写入 buffer。
- 维护 `buffer_version` 和 `snapshot_id`。
- 保证同一会话最多一个主回复任务在跑，即 single-flight。
- LLM 生成期间用户继续发消息时，标记旧任务 stale。
- LLM 返回后检查 `snapshot_id` 是否仍然有效。
- 用户撤回消息时移除对应 buffer 事件，并让相关 pending job 过期。
- `/mem`、`/forget` 等命令优先分流，不进入普通聊天 buffer。
- 主动消息准备发送时检查是否和用户新消息撞车。
- 发送前检查 `job_id` 和 `snapshot_id`，避免重复发送。

`EventGate` 输出给 LangGraph 的不是裸消息，而是一个稳定 snapshot：

```text
ConversationSnapshot
  - session_id
  - snapshot_id
  - buffer_version
  - status: COLD / HOT
  - events
  - cold_start_meta
```

`cold_start_meta` 可以包含 `timestamp`、`last_user_message_age`、`msg_index` 等给 LLM 看的语义提示，但这些字段不参与工程幂等。

### 4.4 LangGraph 主流程

推荐最小主图：

```text
receive_snapshot
  ↓
apply_interaction_flags
  ↓
build_context
  ↓
call_llm_for_decision
  ↓
parse_and_validate_action_protocol
  ├─ strict JSON parse
  ├─ protocol validate
  └─ repair once if needed
  ↓
if items 中存在 search_meme
  ↓
search_meme
  ↓
call_llm_with_meme_candidates
  ↓
validate_executable_decision
  ↓
visible_output_guard
  ↓
route_action
  ↓
persist_log
```

各节点职责：

- `receive_snapshot`：接收 `EventGate` 生成的稳定 snapshot。
- `apply_interaction_flags`：应用拍一拍 / 戳一戳、COLD / HOT 等交互状态；`/mem`、`/forget` 已由 `EventGate` 优先分流。
- `build_context`：读取 `SOUL.md`、`MEMORY_CORE.md`、当天 `dm` 摘要和消息缓冲。
- `call_llm_for_decision`：要求 LLM 输出 `ActionDecision` JSON。
- `parse_and_validate_action_protocol`：由自研 Action Harness 严格解析单个 JSON object，校验 `action + items` 协议；失败时反馈给模型修复一次，仍失败则安全兜底。
- `search_meme`：拦截内部 `{"search_meme":"<category>:<keywords>"}`，只返回候选 `file_stem`，不产生用户可见消息。
- `call_llm_with_meme_candidates`：在同一轮完整流程里让 LLM 从候选中选择精确 `{"meme":"<file_stem>"}`，一轮最多一次。
- `validate_executable_decision`：发送前禁止残留 `search_meme`，并确认 `meme` 的 `file_stem` 来自候选或能命中本地文件。
- `visible_output_guard`：集中检查用户可见内容是否泄漏协议、过长、空转、emoji/meme 重复或表情包使用过度；必要时触发一次内部修复或确定性降级。
- `route_action`：根据 `action` 决定静默、发文本、发表情或改变状态。
- `persist_log`：记录 raw chat log、状态变化和转发结果。

### 4.5 可见输出规则

转发层必须是独立模块。

```text
ActionDecision
  ↓
转发层
  ↓
用户可见消息
```

规则：

- `items:null` 或空时，不发送任何聊天内容。
- `items` 中存在可见发送单元时，才发送到 WebUI / QQ / 微信。
- `action` 不直接展示给用户。
- 内部状态不发成聊天消息。
- `REACT.items` 在 MVP 支持 `text`、`search_meme` 和 `meme`，但必须至少包含一个 `meme` 或 `search_meme`；emoji 作为普通 `text` 发送，单独 emoji 不构成 `REACT`。
- `search_meme` 是内部检索请求，必须拦截，不向用户发送。
- `meme` 只有精确命中本地图片时才渲染，命中失败时降级为短文本或静默。

## 5. 存储选型

### 5.1 Markdown 文件

继续保留当前设计：

```text
SOUL.md
MEMORY_CORE.md
TOMORROW_TOPICS.md
dm/
  YYYY-MM-DD.md
```

用途：

- `SOUL.md`：角色稳定偏好、边界和说话习惯。
- `MEMORY_CORE.md`：长期核心记忆。
- `dm/YYYY-MM-DD.md`：每日可读摘要。
- `TOMORROW_TOPICS.md`：明日主动消息候选。

这些文件是用户可读、可改、可审查的产品层记忆，不应该被数据库完全替代。

### 5.2 数据库

当前推荐：

```text
第一阶段默认 SQLite。
后续服务化、多用户或高并发后再切 Postgres。
```

选择 SQLite 的原因：

- 第一阶段主要是本地常驻、WebUI 验证和 QQ 接入探索。
- 当前核心可维护数据在 Markdown 文件里，数据库主要承担运行记录和状态持久化。
- raw log、事件流水、任务状态、checkpoint 这类写入量在早期 SQLite 完全够用。
- 不需要额外部署数据库服务，减少原型阶段的运维变量。
- 单用户个人机器人场景下，SQLite 更容易备份、迁移和排错。

SQLite 使用约束：

- 开启 WAL 模式，降低读写互相阻塞。
- 业务表建议使用 SQLAlchemy 2.x 管理。
- 通过 repository / storage 层访问数据库，不在业务节点中散落 SQL。
- 使用迁移工具管理表结构，例如 Alembic。
- 表结构和字段类型按未来 Postgres 迁移来设计，避免使用过多 SQLite 专有写法。
- LangGraph checkpoint 和业务表逻辑隔离，避免 checkpoint schema 影响产品数据模型。
- raw log、conversation events、任务状态和主动消息记录都通过统一存储接口写入。

切换到 Postgres 的条件：

- 需要多用户。
- 需要部署到服务器长期在线。
- WebUI、QQ adapter、后台任务分多进程或多机器运行。
- raw log 和 checkpoint 写入量明显增大。
- 需要更可靠的并发写、远程访问、权限管理、备份恢复。
- 后续需要数据看板、复杂查询或长期统计分析。

数据库负责：

- raw chat log。
- 事件流水。
- conversation events / transcript view。
- LangGraph checkpoint。
- 当前运行状态。
- 定时任务状态。
- 主动消息发送记录。
- 文件版本和写入锁状态。

不建议把 `MEMORY_CORE.md`、`SOUL.md` 这类用户可维护内容只存在数据库里。

### 5.3 raw chat log

raw chat log 推荐用 JSONL 或数据库表。

记录内容可以包括：

- 输入事件。
- 触发来源。
- 消息缓冲快照。
- LLM 原始输出。
- Action Harness 解析、修复和协议校验结果。
- 最终 `ActionDecision`。
- 实际转发结果。
- 错误信息。

raw chat log 只用于存档、审计、测评和回放，不作为常规记忆输入。

### 5.4 conversation events / transcript view

定时记忆分析任务需要读取“当天聊天上下文”，但这不等于直接读取 raw chat log。

MVP 建议在数据库里保留一个更干净的对话视图：

```text
conversation_events / transcript_view
```

它只保存记忆整理需要的最小上下文：

- 用户可见输入。
- 系统实际发出的可见输出。
- 关键平台事件，例如拍一拍 / 戳一戳。
- 时间戳。
- 会话 ID。
- 和 raw log 的关联 ID。

它不保存：

- LLM 原始中间输出。
- `search_meme` 内部检索过程。
- Action Harness 协议校验和修复错误细节。
- 发送重试细节。
- 工程异常堆栈。

这样可以保持分工清楚：

```text
raw chat log：完整审计、回放、排错
conversation_events：记忆整理和近期上下文
dm/YYYY-MM-DD.md：整理后的每日可读摘要
```

## 6. 定时任务选型

第一阶段可以用 `APScheduler`。

原因：

- 部署简单。
- 适合本地原型和单进程服务。
- 足够支持每日固定时间任务和少量周期任务。

后续如果需要更强可靠性，再切换：

- `Celery`：适合多 worker、任务队列、重试和分布式部署。
- `Taskiq`：适合更现代的 Python 异步任务队列。
- `Temporal / DBOS / Prefect`：适合更强 durable workflow，但复杂度更高。

当前建议：

```text
第一阶段：APScheduler
第二阶段：如果 QQ 常驻运行稳定后仍需要更强任务可靠性，再评估 Celery / Taskiq
不要过早引入 Temporal 级别的工作流系统
```

APScheduler 使用约束：

- 使用固定 `job_id` 注册定时任务。
- 重启时使用 `replace_existing=True`，避免重复注册同一任务。
- 任务运行记录写入 SQLite，包括开始时间、结束时间、状态、错误摘要和重试次数。
- 任务函数内部必须幂等，重复执行不能破坏 Markdown 文件。
- 定时任务只触发后台流程，不直接绕过 `EventGate` 给用户发消息。
- 主动消息发送任务在真正发送前仍要经过 `EventGate` 的主动消息撞车和发送幂等检查。

## 7. 后台任务划分

### 7.1 定时记忆分析任务

职责：

- 定时读取当天 `conversation_events / transcript_view`，不直接读取 raw chat log。
- 更新 `dm/YYYY-MM-DD.md`。
- 更新 `TOMORROW_TOPICS.md` 的未闭合话题。
- 不直接修改 `MEMORY_CORE.md`。

### 7.2 凌晨整理线程

职责：

- 凌晨 3-4 点运行。
- 从当天 `dm` 中提取少量长期价值内容。
- 更新 `MEMORY_CORE.md`。
- 为 `TOMORROW_TOPICS.md` 补充昨日记忆。
- 校验未闭合话题是否仍成立。
- 失败时保留原文件并重试。

### 7.3 主动消息调度

职责：

- 读取 `TOMORROW_TOPICS.md`。
- 判断是否适合发送主动消息。
- 控制频率、时间窗口、免打扰和未回复退避。
- 发送后记录状态。

主动消息调度属于系统层，不应该让 LLM 自己决定无限制主动联系用户。

主动消息真正发送前仍必须进入 `EventGate`：

```text
主动消息候选
  ↓
主动消息调度
  ↓
EventGate 检查用户是否有未处理输入、今日上限、候选状态和发送幂等
  ↓
LangGraph / LLM 生成自然表达
  ↓
EventGate 复核
  ↓
转发层发送
```

## 8. 文件写入和冲突处理

由于用户可能直接编辑 Markdown 文件，文件写入必须保守。

建议规则：

- 后台任务写入前读取文件最新内容。
- 写入前检查文件是否被修改。
- 写入失败时不强行覆盖。
- 如果文件被打开导致无法保存，停止当前写入并提示用户关闭文件后重试。
- 重要文件写入前保留备份或版本记录。
- 自动整理任务只做可解释的最小修改。

重点文件：

- `SOUL.md`
- `MEMORY_CORE.md`
- `TOMORROW_TOPICS.md`
- `dm/YYYY-MM-DD.md`

## 9. 平台适配层

平台适配层只负责把外部事件转换成统一事件，并把内部输出发送回平台。

```text
平台事件
  ↓
平台适配层
  ↓
ChatEvent
  ↓
EventGate
  ↓
ConversationSnapshot
  ↓
LangGraph 决策图
  ↓
ActionDecision
  ↓
EventGate 复核 snapshot 和发送幂等
  ↓
平台适配层
  ↓
平台可见消息
```

QQ 接入时可以优先调研：

- NapCat。
- OneBot 协议。
- QQ NT 事件结构。
- 戳一戳 / 拍一拍事件如何上报。
- 如何把 `items` 映射为平台文本、emoji、表情图片等消息段。

平台适配层不应该理解 `SOUL.md`、`MEMORY_CORE.md` 或记忆整理规则。

## 10. WebUI 定位

第一阶段 WebUI 是验证和观测入口。

技术栈建议：

```text
Vite + React + TypeScript
```

原因：

- 适合快速搭本地调试界面。
- 和 FastAPI 后端边界清楚。
- 表情包图片网格、状态面板和 Markdown 编辑区实现成本低。
- 后续需要更复杂交互时扩展空间足够。

最小功能：

- 对话窗口。
- 模拟拍一拍 / 戳一戳按钮。
- 查看和编辑 `SOUL.md`。
- 主动消息设定模块。
- 表情包 Tab：分类列表、图片缩略图、复制 `meme:<file_stem>`、删除单张。
- 展示最近一次 `ActionDecision`。
- 展示当前是否发出了可见文本。
- 展示最近一次 snapshot 是否被发送、静默或作废。

后续可以扩展：

- 查看 `MEMORY_CORE.md`。
- 查看当天 `dm/YYYY-MM-DD.md`。
- 查看 `TOMORROW_TOPICS.md`。
- 查看最近 action 决策。
- 查看 raw log。
- 查看后台任务运行状态。

WebUI 不应该在早期做成复杂后台产品，先服务最小闭环和可解释性。

## 11. MVP 技术栈定稿

当前 MVP 技术栈收敛为：

| 层 | 选择 |
| --- | --- |
| 后端语言 | Python 3.11+，优先 3.12 |
| API 服务 | FastAPI + Uvicorn |
| 实时事件门 | 自研 `EventGate` |
| 主回复编排 | LangGraph 低层 Graph API |
| 输出校验 | 自研 Action Harness + Pydantic v2 内部模型 |
| 数据库 | SQLite，开启 WAL |
| ORM / 迁移 | SQLAlchemy 2.x + Alembic |
| LangGraph checkpoint | SQLite checkpointer，和业务 repository 隔离 |
| 定时任务 | APScheduler |
| 前端 | Vite + React + TypeScript |
| 表情包 | 本地文件目录 + `memes_data.json` + 语义化文件名 |
| 文件记忆 | `SOUL.md`、`MEMORY_CORE.md`、`dm/`、`TOMORROW_TOPICS.md` |
| 测试 | pytest + pytest-asyncio，WebUI 后续加 Playwright |

MVP 暂不引入：

- Postgres。
- Celery / Taskiq。
- Temporal / DBOS / Prefect。
- LlamaIndex / 向量库 / BM25。
- Pydantic AI。
- Pi / CrewAI / AutoGen。
- 真实 QQ / 微信发图。
- 真实平台偷表情。

## 12. 暂不采用的主框架路线

### 12.1 不用 Pydantic AI 做主框架

原因：

- 它本身也是 Agent 框架。
- 如果和 LangGraph 同时作为主框架，会出现运行时职责重叠。
- 本项目只需要 Pydantic 的 schema 和校验能力。

可保留：

- 学习其结构化输出设计。
- 未来如果放弃 LangGraph，可以重新评估 Pydantic AI + durable execution 路线。

### 12.2 不用 Pi 做主框架

原因：

- Pi 更像 coding agent / session harness。
- 它的 session tree、history、context engineering 对开发和调试有价值，但不是 QQ / 微信陪伴机器人运行时的天然底座。
- 如果直接作为主框架，容易和本项目自己的消息缓冲、状态机、转发层、记忆文件和平台适配层抢职责。

可借鉴：

- session tree。
- 可分支回放。
- context 文件加载。
- skills / extensions 组织方式。

### 12.3 不用 CrewAI / AutoGen 做主框架

原因：

- 这类框架更偏多 Agent 协作和任务自动化。
- 本项目不是让多个角色协同完成工作。
- 引入后容易把产品带向“任务执行 Agent”，而不是“长期陪伴运行时”。

### 12.4 暂不使用 LlamaIndex 做主底座

原因：

- LlamaIndex 强项是索引、检索、RAG 和数据连接。
- 当前设计明确暂不优先引入 BM25 / 向量检索。

可保留：

- 未来当 `dm/` 历史文件太多、需要跨日期召回时，再把 LlamaIndex 或其他检索系统作为记忆检索组件接入。

## 13. 推荐第一阶段工程拆分

建议目录结构：

```text
app/
  main.py
  api/
  core/
    event_gate.py
    buffer.py
    snapshots.py
    graph.py
    state.py
    events.py
    decisions.py
    router.py
  llm/
    client.py
    prompts.py
  memory/
    files.py
    daily.py
    core_memory.py
    tomorrow_topics.py
  memes/
    catalog.py
    search.py
    renderer.py
  scheduler/
    jobs.py
  storage/
    db.py
    models.py
    repositories.py
    raw_log.py
    conversation_events.py
  adapters/
    webui.py
    qq.py
webui/
  src/
memes/
  memes_data.json
  image_dhash_index.json
  filtered.json
  assets/
```

第一阶段最小闭环：

1. WebUI 输入一句话。
2. 事件进入 `EventGate`。
3. `EventGate` 写入 buffer，生成 `snapshot_id`。
4. LangGraph 读取稳定 snapshot。
5. 读取 `SOUL.md`、`MEMORY_CORE.md` 和当天 `dm` 摘要。
6. 调用 LLM 输出 `ActionDecision`。
7. 自研 Action Harness 严格解析单 JSON、校验协议；失败时最多修复一次。
8. `items:null` 或空时静默，不清空未回应 buffer。
9. `items` 中存在可见发送单元时，由 `EventGate` 复核 snapshot 和发送幂等，再按顺序显示到 WebUI。
10. raw log 和 conversation events 分别记录。
11. 模拟拍一拍 / 戳一戳触发 `ENTER_CHAT` 或进入 HOT。
12. WebUI 表情包链路跑通 `{"search_meme":"<category>:<keywords>"}` 到 `{"meme":"<file_stem>"}`。

## 14. 当前选型原则

当前阶段最重要的不是框架能力越多越好，而是职责边界足够稳定。

底层框架必须服务以下产品原则：

- 允许不回复。
- 允许等待用户说完。
- 允许批量阅读多条消息后再回复。
- 允许用户用拍一拍 / 戳一戳强制刷新注意力。
- 允许用户直接查看和编辑记忆。
- 允许后台任务失败后可恢复。
- 允许后续接入 QQ / 微信，而不重写核心运行时。

因此当前推荐保持：

```text
EventGate 做实时发送权和一致性控制
LangGraph 做主回复决策图
自研 Action Harness 做协议校验和修复
Pydantic BaseModel 做内部数据对象
Markdown 做可维护记忆
SQLite 做运行记录和 checkpoint
FastAPI 做服务入口
Vite / React / TypeScript 做 WebUI
平台适配层做外部接入
```
