# 技术架构

## 1. 当前结论

项目底层推荐采用：

```text
FastAPI
  + 自研实时事件门 EventGate
  + LangGraph
  + Pydantic BaseModel
  + SQLite first, Postgres later
  + SQLAlchemy / Alembic
  + Markdown 记忆文件
  + APScheduler first, Celery / Taskiq later
  + Vite / React / TypeScript WebUI
  + 本地表情包目录
  + 平台适配层
```

核心判断：

- `EventGate` 负责消息缓冲、single-flight、snapshot 过期作废、撤回、命令分流、主动消息撞车和发送幂等。
- `LangGraph` 负责主回复决策图、节点编排、断点恢复和可选内部检索循环。
- 自研 Action Harness 负责解析主模型轻量文本协议、一次修复、归一化成内部 `ActionDecision / SendItem`，并做最终可执行性校验；不依赖 LangChain structured output 或 provider `response_format` 作为可信边界。
- 主聊天轻量协议可以产生不可见 side effect，例如主动消息时间设置；side effect 不等于可见气泡，必须在发送成功后由工程层复核并落配置。
- 可见性和风格预算网关负责发送前的产品体验约束，例如协议泄漏、过长回复、emoji 高频重复、meme 过度使用和同一表情反复发送；结构合法不等于产品可发送。
- 主聊天底层维护两份历史：用户可见历史只给产品展示、记忆、导出和普通接口使用；provider transcript 只给主 LLM 请求使用。当前落地版本把它作为 durable prefix，主要保存稳定 system、context checkpoint、真实 user/assistant 和少量跨轮内部 system reminder；本轮 runtime、HOT/FINAL reminder、media pending、repair 和 meme search 属于 ephemeral tail，不永久写入 durable transcript。
- `Pydantic BaseModel` 只负责内部数据对象的定义、归一化和校验，不作为 Agent 框架使用。
- `FastAPI` 负责 WebUI、平台回调、调试接口和本地服务入口。
- 数据库负责机器可读运行数据，Markdown 文件负责用户可读记忆和角色配置。
- 定时任务系统负责记忆分析、凌晨整理和主动消息调度。
- QQ / 微信 / WebUI 都通过平台适配层接入，不侵入核心对话运行时。

防误用边界：

- 不要把 provider transcript 当成聊天记录使用。它可能包含系统提示词、context checkpoint、跨轮内部 system reminder、provider 安全 assistant message 和 `reasoning_content`；完整本轮请求还会临时拼上 runtime 状态、repair 指令、meme 检索结果和 media pending payload。
- 聊天展示、记忆模块、用户历史查询、导出和普通 API 默认只能读取用户可见历史。
- provider transcript 只能在主 LLM 链路和开发调试中使用；调试页默认展示摘要，例如 role 序列、stable/full request hash、stable prefix append-only、cached tokens、raw output source，不默认暴露完整 CoT 或完整系统提示词。
- HOT 轮的 `SYSTEM_REMINDER / HOT_TURN_REMINDER` 是本轮请求内部 system message，用于本轮 harness 与聊天风格约束；它不是用户消息，不进入 durable transcript、用户可见历史、记忆、导出或普通 API。少量跨轮风格提醒可以通过 `append_internal_system_reminder` 追加到 durable transcript，但必须仍是 internal-only system message。
- `/mem`、`/forget`、主动消息、定时记忆任务不读取 provider transcript；这些链路需要上下文时使用用户可见历史或单独设计的输入。

多用户 session 隔离当前已作为底座能力落地：

- WebUI 默认 session 是 `webui_default`；QQ 私聊 session 是 `qq_private_<qq_user_id>`；群聊仍不进入当前版本范围。
- `SessionRegistry` 负责 session 命名、目录注册和元信息；`SessionRuntimeManager` 按 session 创建并持有 `EventGate`、`SnapshotManager`、`CompanionGraph` 和 session 版 `MemoryFileManager`。
- `SOUL.md` 和全局表情包库仍共享；`MEMORY_CORE.md`、`TOMORROW_TOPICS.md`、`dm/YYYY-MM-DD.md` 按 `memory/sessions/<session_id>/` 隔离。
- `ConversationEvent`、`RawChatLog`、`ScheduledJobLog` 都记录 `session_id`；查询、状态、清空、raw debug 和 WebUI 展示默认只看当前 session。
- provider transcript、stable/full request debug、reasoning 回传、repair/meme 二轮和 cache affinity 都随 `CompanionGraph` 按 session 隔离；每个 session 有独立的稳定 `cache_session_id`。
- 定时记忆分析、凌晨整理和主动消息候选扫描全量 session；单个 session 失败不阻断其他 session。QQ 主动消息仍默认关闭。
- 媒体理解结果按 session 回灌后续 prompt；偷到的表情包仍进入全局 meme 库。

一句话原则：

```text
用 EventGate 管这条结果现在还能不能发，用 LangGraph 管主回复决策流程，用自研 Action Harness 管 LLM 轻量输出能不能变成内部可执行动作，用自己的转发层管什么能被用户看到。
```

### 1.1 运行鲁棒性与 Provider Transcript

核心聊天体验不能被任何单一外部能力绑死。QQ / 微信平台事件、拍一拍 / 戳一戳、表情包发送、图片理解、语音能力、记忆检索和外部长期记忆后端都按软依赖处理。

能力不可用时：

- 不阻塞核心聊天。
- 不伪造不存在的平台事件。
- 不在自然聊天里报技术错误。
- 优先退回更基础的内部动作或静默。
- 错误细节进入 raw log、调试界面或工程监控。

核心降级方向：

```text
带表情自然回复 -> 去掉 marker 后发文本 / 短自然回复 -> WAIT
```

典型降级：

- 表情包不可用时，删除不可见 marker，优先继续发送自然文本；语境不适合文字时可 `WAIT`。
- 平台没有拍一拍 / 戳一戳事件时，不伪造事件，仍保留普通消息触发和主动消息机制。
- 图片理解不可用时，不胡乱描述图片，可以等待用户补充文字或轻问一句。
- 记忆检索不可用时，只使用 `MEMORY_CORE.md` 和近期 `dm/`，不在聊天里告诉用户“检索失败”。

Provider transcript 的 prompt cache 与 thinking 回传规则：

- 主回复不使用最近 30 条滑动窗口作为主链路；当前请求由 durable prefix 复制后拼接 ephemeral tail 组成。
- durable prefix 只保存稳定 system、context checkpoint、checkpoint 后的真实 user/assistant、已确认安全的 assistant 可见文本，以及少量跨轮 internal-only system reminder。
- ephemeral tail 每轮重新构造，包含 `runtime_context`、本轮 media pending payload、HOT 轮 `SYSTEM_REMINDER / HOT_TURN_REMINDER`、`FINAL_ACTION_OUTPUT_REMINDER`、repair tail 和 meme search tail。它每轮完整发送给 provider，但不永久 append 到 durable transcript。
- 缓存健康判断以 `stable_prefix_append_only_check` 为主；`full_request_append_only_check` 只说明完整请求尾部是否变化，不作为缓存是否健康的唯一判断。
- 请求 N+1 的 stable prefix 必须以请求 N 的 stable prefix 为前缀；完整请求允许因 runtime、HOT reminder、media、repair、meme search 等当轮尾巴变化而不是 append-only。
- COLD -> HOT 不因为状态变化回写旧前缀；HOT 每轮只在本轮请求尾部追加 HOT reminder。
- 表情包二轮和协议修复仍沿用同一套主聊天上下文，但本地检索结果、错误和修复要求属于本轮 ephemeral tail；不会伪造 `role=tool`、`tool_call_id` 或 provider-native tool result。
- `&&category:keywords&&` marker 会被系统转成内部 `search_meme` 二轮能力；它不是 provider-native tool call。
- MiMo 偶发输出的 `||category:keywords||` 只作为兼容输入被协议层识别，并归一化成同一类 `search_meme`；提示词仍只推荐 `&&category:keywords&&`，未解析 marker 仍由 bubble guard 拦截。
- provider raw response 先归一化为 `NormalizedLLMOutput`，只有通过 `classify_visible_text(mode="model_raw")` 和主聊天轻量协议解析的内容才进入可见解析；不安全 content 不进入 durable transcript。
- `reasoning_content` / CoT 只作为 hidden reasoning 和 debug 信息；仅在 content 为空且可抽取出安全最终回复时，`reasoning_content_rescue` 才作为受控 fallback，仍要经过可见安全分类。
- usage、cached tokens、finish_reason、response id 等响应元信息只做 debug，不混入下一轮 messages。
- provider transcript 与当前 LLM identity 绑定；`base_url`、`model` 或 thinking 配置变化时清空 provider transcript，并用 context checkpoint 和用户可见历史重建安全上下文。
- 本地 cache affinity 使用稳定 `cache_session_id`、请求头和 SDK `prompt_cache_key`；如果端点拒绝 `prompt_cache_key`，自动降级，不影响主链路运行。MiniMax M3 当前不发送 `extra_body.user_id`。

### 1.2 已落地底层稳定性选型

用户可见输出边界采用统一隔离链路：provider raw response 先归一为 `NormalizedLLMOutput`，再进入主聊天轻量协议解析、repair 和发送层 `bubble` 安全检查；`reasoning_content`、tool/function call、provider 残片和内部 marker 不直接进入用户可见文本、历史或 checkpoint。发送、切分、WebUI 和 OneBot 共享同一最终可见文本闸门。

主聊天主动消息时间设置采用不可见 marker + side effect：当且仅当用户本轮明确要求某个时间主动联系、提醒、叫他或每天固定联系时，主模型可以在自然确认文本末尾附一个 `&&next:YYYY-MM-DD HH:mm&&` 或 `&&daily:HH:mm&&`。协议层会把 marker 从可见文本中剥离，归一化为 `ActionDecision.side_effects.active_message_setting`；发送层只有在本轮可见回复成功发送并提交后，且当前 snapshot 的用户文本再次通过“主动消息设置请求”意图判断时，才调用现有配置写入逻辑。marker 本身禁止进入用户气泡、provider durable transcript、记忆和 checkpoint；不合法、多重或畸形 marker 走 repair / WAIT。

主聊天 prompt 采用 durable prefix + ephemeral tail：稳定 system、真实 user/assistant、checkpoint 属于 durable；runtime context、HOT reminder、FINAL reminder、media pending、repair tail、meme search tail 只属于本轮请求。观测口径同时保留 `stable_prefix_append_only_check` 和 `full_request_append_only_check`，缓存判断优先看稳定前缀，不再为了完整 request append-only 让一次性 system 消息滞留。

Provider 调用采用 `ProviderRuntime` 集中管理配置快照、runtime id、identity hash、共享 `AsyncOpenAI` transport 和 transient retry 观测。`SessionRuntimeManager` 明确三类 scope：`chat_session` 用于主聊天，保留 provider transcript、cache affinity 和 session 级 provider identity；`internal_session` 用于 `/mem`、`/forget`、主动消息和媒体理解，按 session 缓存但禁用 cache affinity / provider identity；`background_batch` 用于定时批处理，单 session 失败不阻断其他 session。所有 scope 共享底层 runtime，但内部线程不读取、不写入主聊天 provider transcript。

每个 session 的 provider identity 由 `SessionRegistry` 保存随机 seed，并通过安装级 secret 做 HMAC 派生，生成稳定且不可反推原始用户号的 `u_<hash>`。主聊天 `chat_session` client 会携带该 `provider_user_id`；实际请求写入哪个字段由 provider profile 决定：DeepSeek 和普通 OpenAI-compatible profile 使用 `extra_body.user_id`；MiniMax M3 官方 OpenAI-compatible 参数表未列该字段，当前按兼容性策略不发送；Kimi profile 使用 `safety_identifier`，并通过模型名包含 `kimi` 识别，不依赖官方 URL。

Provider 差异只存在于 `LLMClient` / `ProviderRuntime` adapter 层，业务层消费统一的 `NormalizedLLMOutput`：

- MiniMax M3：保持当前 OpenAI-compatible 配置，thinking 自适应，`reasoning_split=True`，不发送 provider `user_id`。
- MiMo V2.5：按 OpenAI-compatible chat completions 形态调用；可配置 `mimo_web_search_mode=off/adaptive/force`，默认 `off`。只有 `chat_session` 会注入 MiMo 原生 `web_search` tool，`adaptive` 对应 `force_search=false`，`force` 对应 `force_search=true`；联网 annotations、error_message 和 web_search usage 只进入审计/debug，不写入聊天历史。
- Kimi：模型名包含 `kimi` 时启用 Kimi profile；thinking 使用 Kimi `thinking.type`，请求中不发送 `temperature`，provider identity 字段为 `safety_identifier`。
- DeepSeek / 其它兼容端点：保持默认 OpenAI-compatible profile；是否真正支持 provider identity、thinking 或 cache 字段以 adapter 实测和文档为准。

## 2. 为什么主框架选 LangGraph

本项目的核心不是多 Agent 协作，也不是让模型自由调用工具完成任务，而是一个长期运行的陪伴运行时。

它需要处理：

- 消息缓冲。
- single-flight。
- snapshot 过期作废。
- 撤回静默取消。
- 发送幂等。
- 离线生活态和热聊在场态。
- 用户连续输入时的等待判断。
- `WAIT`、`ENTER_CHAT:`、普通自然语言和 `&&category:keywords&&` 的轻量输出解析；`||category:keywords||` 仅作为 provider marker 漂移兼容。
- `&&next:YYYY-MM-DD HH:mm&&` / `&&daily:HH:mm&&` 到主动消息时间 side effect 的解析、剥离和发送后落配置。
- 内部 `WAIT / REPLY / LIGHT_ACK / REACT / ENTER_CHAT / END_CHAT` 动作路由。
- `items:null` 或空时静默，不向用户发送任何内容。
- 拍一拍 / 戳一戳强制刷新注意力。
- `&&category:keywords&&` 到内部 `search_meme`，再到二轮 `:meme:<file_stem>` 的精确选图闭环；兼容 `||category:keywords||` 但不主动提示模型使用。
- 表情二轮是本地 harness 能力，不是 provider-native tool call；当前不引入完整 agent tool loop，也不伪造 `role=tool`。
- 发送前风格预算检查，防止模型把“积极使用表情包”退化成每轮固定 marker 或固定 emoji 模板。
- raw chat log 记录。
- 定时记忆分析。
- 凌晨整理线程。
- 主动消息候选读取和发送。
- WebUI 与 QQ / NapCat 私聊多入口共存；微信和群聊后置。

这些需求更接近状态机和事件流，而不是传统 chatbot 的单轮问答。

LangGraph 适合作为底层，是因为它可以承载：

- 显式状态。
- 节点化流程。
- checkpoint / persistence。
- 运行中断后的恢复。
- 长流程拆分。
- 后续增加人工介入、回放、调试和观测。

但本项目不应该直接套用 LangGraph 预置的 ReAct Agent 模式。我们只使用它的低层图编排能力。

同时，实时发送一致性不完全交给 LangGraph。MVP 需要一个自研 `EventGate`，它可以放在 LangGraph 外围，也可以实现为图前置层，但职责必须独立：

```text
EventGate：这次结果现在还能不能发
LangGraph：这次如果要处理，应该怎么思考和决策
```

## 3. Pydantic 的职责边界

这里建议使用的是 `Pydantic BaseModel`，不是把 `Pydantic AI` 作为主 Agent 框架。

当前落地是“轻量文本协议解析 + Pydantic 内部模型”：

1. 主聊天 LLM 面向协议只允许 `WAIT`、`ENTER_CHAT: <自然语言>`、普通自然语言和 `&&category:keywords&&` marker；`||category:keywords||` 是运行时兼容输入，不是推荐输出格式。
2. Harness 把轻量输出归一化为内部 `ActionDecision` / `SendItem`；结构或协议错误时最多反馈给模型修复一次。
3. Pydantic 负责内部对象的稳定数据结构，不要求主聊天 LLM 直接输出这些对象。
4. `&&category:keywords&&` 和兼容的 `||category:keywords||` 会被解析成内部 `search_meme` item，并在发送层按位置触发二轮选图。
5. 二轮表情选择只允许输出 `:meme:<file_stem>`；最终发送前仍要校验 `file_stem` 来自候选且能真实命中。
6. 产品体验类约束由发送前网关承担，不塞进 Pydantic 数据模型。

示例：

```python
from enum import Enum
from pydantic import BaseModel, model_validator


class Action(str, Enum):
    WAIT = "WAIT"
    REPLY = "REPLY"
    LIGHT_ACK = "LIGHT_ACK"
    REACT = "REACT"
    ENTER_CHAT = "ENTER_CHAT"
    END_CHAT = "END_CHAT"


class SendItem(BaseModel):
    # 内部对象保留 type/content，方便路由、渲染和平台适配。
    # 主聊天 LLM 不直接输出 SendItem；它只输出自然语言和 marker。
    type: str  # text / meme / search_meme
    content: str


class ActionDecision(BaseModel):
    action: Action
    items: list[SendItem] | None = None

    @model_validator(mode="after")
    def validate_action_items(self):
        if self.action in (Action.WAIT, Action.END_CHAT) and self.items:
            raise ValueError("WAIT/END_CHAT actions require no visible items")

        if self.action == Action.REPLY and not self.items:
            raise ValueError("REPLY action requires at least one item")

        if self.action == Action.LIGHT_ACK and not self.items:
            raise ValueError("LIGHT_ACK action requires short visible item")

        if self.action == Action.REACT and not any(item.type in ("meme", "search_meme") for item in self.items or []):
            raise ValueError("REACT action requires meme/search_meme item")

        return self
```

这样不会和 LangGraph 抢职责：

```text
LangGraph：什么时候运行、运行到哪一步、状态如何恢复
Action Harness：这一轮 LLM 轻量输出是否能成为内部可执行 ActionDecision
Pydantic：ActionDecision / SendItem 的内部数据对象
```

## 4. 最小核心架构

### 4.1 运行入口

当前 WebUI 与 QQ / NapCat 私聊主链路都已接通；核心运行时统一接收平台无关的 `ChatEvent`。

```text
WebUI
  ↓
FastAPI
  ↓
实时事件门 EventGate
  ↓
LangGraph 决策图
  ↓
转发层
```

QQ / NapCat / OneBot 通过平台适配层接入：

```text
QQ / NapCat / OneBot
  ↓
平台适配层
  ↓
实时事件门 EventGate
  ↓
LangGraph 决策图
  ↓
平台适配层发送
```

`EventGate` 不关心消息来自 WebUI 还是 QQ，只接收统一事件。

### 4.2 统一事件结构

早期可以先保持简单：

```text
ChatEvent
  - event_id
  - platform
  - user_id
  - event_type
  - text
  - timestamp
  - raw
```

`event_type` 可以包含：

- `message.text`
- `message.image`
- `nudge`
- `recall`
- `command.mem`
- `command.forget`

当前 WebUI 至少需要支持：

- 普通文本输入。
- 模拟拍一拍 / 戳一戳。
- 查看和编辑 `SOUL.md`。

### 4.3 实时事件门 EventGate

`EventGate` 是 MVP 的核心运行层之一。

它不是第二个聊天大脑，也不判断情绪、不改写文本、不做人造延迟。它只负责：

```text
这条回复现在还能不能发。
```

MVP 职责：

- 接收统一事件并写入 buffer。
- 维护 `buffer_version` 和 `snapshot_id`。
- 保证同一会话最多一个主回复任务在跑，即 single-flight。
- LLM 生成期间用户继续发消息时，标记旧任务 stale。
- LLM 返回后检查 `snapshot_id` 是否仍然有效。
- 用户撤回消息时移除对应 buffer 事件，并让相关 pending job 过期。
- `/mem`、`/forget` 等命令优先分流，不进入普通聊天 buffer。
- 主动消息准备发送时检查是否和用户新消息撞车。
- 发送前检查 `job_id` 和 `snapshot_id`，避免重复发送。

`EventGate` 输出给 LangGraph 的不是裸消息，而是一个稳定 snapshot：

```text
ConversationSnapshot
  - session_id
  - snapshot_id
  - buffer_version
  - status: COLD / HOT
  - events
  - cold_start_meta
```

`cold_start_meta` 可以包含 `timestamp`、`last_user_message_age`、`msg_index` 等给 LLM 看的语义提示，但这些字段不参与工程幂等。

### 4.4 LangGraph 主流程

推荐最小主图：

```text
receive_snapshot
  ↓
apply_interaction_flags
  ↓
build_context
  ↓
call_llm_for_decision
  ↓
parse_and_validate_main_output
  ├─ WAIT / ENTER_CHAT / natural text / marker parse
  ├─ protocol validate
  └─ repair once if needed
  ↓
validate_executable_decision
  ↓
visible_output_guard
  ↓
route_action
  ↓
persist_log
```

各节点职责：

- `receive_snapshot`：接收 `EventGate` 生成的稳定 snapshot。
- `apply_interaction_flags`：应用拍一拍 / 戳一戳、COLD / HOT 等交互状态；`/mem`、`/forget` 已由 `EventGate` 优先分流。
- `build_context`：读取 `SOUL.md`、`MEMORY_CORE.md`、当天 `dm` 摘要和消息缓冲。
- `call_llm_for_decision`：要求 LLM 输出轻量文本协议，而不是 JSON 主协议。
- `parse_and_validate_main_output`：由自研 Action Harness 解析 `WAIT`、`ENTER_CHAT:`、自然文本、推荐 `&&category:keywords&&` 和兼容 `||category:keywords||`；失败时反馈给模型修复一次，仍失败则安全兜底。
- `validate_executable_decision`：发送前确认内部动作可执行，并保留 `search_meme` 作为发送层待解析 marker。
- `visible_output_guard`：集中检查用户可见内容是否泄漏协议、过长、空转、emoji/meme 重复或表情包使用过度；必要时触发一次内部修复或确定性降级。
- `route_action`：根据内部 `action` 决定静默、发文本、发表情或改变状态。
- `send_visible_items`：按顺序发送；遇到内部 `search_meme` 时，先确保前置文本已发出，再检索候选并调用二轮模型选择 `:meme:<file_stem>`，失败则删除 marker 并继续发送后缀。
- `persist_log`：记录 raw chat log、状态变化和转发结果。

### 4.5 可见输出规则

转发层必须是独立模块。

```text
内部 ActionDecision
  ↓
转发层
  ↓
用户可见消息
```

规则：

- 内部 `items:null` 或空时，不发送任何聊天内容。
- 内部 `items` 中存在可见发送单元时，才发送到 WebUI / QQ / 微信。
- 内部 `action` 不直接展示给用户。
- 内部状态不发成聊天消息。
- 主模型输出中的 `&&category:keywords&&` 以及兼容 `||category:keywords||` 不进入用户可见历史、记忆或普通 conversation events。
- 内部 `search_meme` 是检索请求，必须拦截，不向用户发送。
- `meme` 只有精确命中本地图片时才渲染，命中失败时降级为短文本或静默。

## 5. 存储选型

### 5.1 Markdown 文件

继续保留当前设计：

```text
SOUL.md
MEMORY_CORE.md
TOMORROW_TOPICS.md
dm/
  YYYY-MM-DD.md
```

用途：

- `SOUL.md`：角色稳定偏好、边界和说话习惯。
- `MEMORY_CORE.md`：长期核心记忆。
- `dm/YYYY-MM-DD.md`：每日可读摘要。
- `TOMORROW_TOPICS.md`：明日主动消息候选。

这些文件是用户可读、可改、可审查的产品层记忆，不应该被数据库完全替代。

### 5.2 数据库

当前推荐：

```text
当前默认 SQLite。
后续服务化、跨进程多 worker 或高并发后再切 Postgres。
```

选择 SQLite 的原因：

- 当前主要是本地常驻、WebUI 验证和 QQ / NapCat 私聊运行。
- 当前核心可维护数据在 Markdown 文件里，数据库主要承担运行记录和状态持久化。
- raw log、事件流水、任务状态、checkpoint 这类写入量在早期 SQLite 完全够用。
- 不需要额外部署数据库服务，减少原型阶段的运维变量。
- 单用户个人机器人场景下，SQLite 更容易备份、迁移和排错。

SQLite 使用约束：

- 开启 WAL 模式，降低读写互相阻塞。
- 业务表建议使用 SQLAlchemy 2.x 管理。
- 通过 repository / storage 层访问数据库，不在业务节点中散落 SQL。
- 使用迁移工具管理表结构，例如 Alembic。
- 表结构和字段类型按未来 Postgres 迁移来设计，避免使用过多 SQLite 专有写法。
- LangGraph checkpoint 和业务表逻辑隔离，避免 checkpoint schema 影响产品数据模型。
- raw log、conversation events、任务状态和主动消息记录都通过统一存储接口写入。

切换到 Postgres 的条件：

- 需要多用户。
- 需要部署到服务器长期在线。
- WebUI、QQ adapter、后台任务分多进程或多机器运行。
- raw log 和 checkpoint 写入量明显增大。
- 需要更可靠的并发写、远程访问、权限管理、备份恢复。
- 后续需要数据看板、复杂查询或长期统计分析。

数据库负责：

- raw chat log。
- 事件流水。
- conversation events / transcript view。
- LangGraph checkpoint。
- 当前运行状态。
- 定时任务状态。
- 主动消息发送记录。
- 文件版本和写入锁状态。

不建议把 `MEMORY_CORE.md`、`SOUL.md` 这类用户可维护内容只存在数据库里。

### 5.3 raw chat log

raw chat log 推荐用 JSONL 或数据库表。

记录内容可以包括：

- 输入事件。
- 触发来源。
- 消息缓冲快照。
- LLM 原始输出。
- Action Harness 轻量协议解析、修复和归一化结果。
- 最终 `ActionDecision`。
- 实际转发结果。
- 错误信息。

raw chat log 只用于存档、审计、测评和回放，不作为常规记忆输入。

### 5.4 conversation events / transcript view

定时记忆分析任务需要读取“当天聊天上下文”，但这不等于直接读取 raw chat log。

MVP 建议在数据库里保留一个更干净的对话视图：

```text
conversation_events / transcript_view
```

它只保存记忆整理需要的最小上下文：

- 用户可见输入。
- 系统实际发出的可见输出。
- 关键平台事件，例如拍一拍 / 戳一戳。
- 时间戳。
- 会话 ID。
- 和 raw log 的关联 ID。

它不保存：

- LLM 原始中间输出。
- `search_meme` 内部检索过程。
- Action Harness 协议解析和修复错误细节。
- 发送重试细节。
- 工程异常堆栈。

这样可以保持分工清楚：

```text
raw chat log：完整审计、回放、排错
conversation_events：记忆整理和近期上下文
dm/YYYY-MM-DD.md：整理后的每日可读摘要
```

## 6. 定时任务选型

当前使用 `APScheduler`。

原因：

- 部署简单。
- 适合本地原型和单进程服务。
- 足够支持每日固定时间任务和少量周期任务。

后续如果需要更强可靠性，再切换：

- `Celery`：适合多 worker、任务队列、重试和分布式部署。
- `Taskiq`：适合更现代的 Python 异步任务队列。
- `Temporal / DBOS / Prefect`：适合更强 durable workflow，但复杂度更高。

当前建议：

```text
当前：APScheduler
后续：如果 QQ / NapCat 常驻运行稳定后仍需要更强任务可靠性，再评估 Celery / Taskiq
不要过早引入 Temporal 级别的工作流系统
```

APScheduler 使用约束：

- 使用固定 `job_id` 注册定时任务。
- 重启时使用 `replace_existing=True`，避免重复注册同一任务。
- 任务运行记录写入 SQLite，包括开始时间、结束时间、状态、错误摘要和重试次数。
- 任务函数内部必须幂等，重复执行不能破坏 Markdown 文件。
- 定时任务只触发后台流程，不直接绕过 `EventGate` 给用户发消息。
- 主动消息发送任务在真正发送前仍要经过 `EventGate` 的主动消息撞车和发送幂等检查。

## 7. 后台任务划分

### 7.1 定时记忆分析任务

职责：

- 定时读取当天 `conversation_events / transcript_view`，不直接读取 raw chat log。
- 更新 `dm/YYYY-MM-DD.md`。
- 更新 `TOMORROW_TOPICS.md` 的未闭合话题。
- 不直接修改 `MEMORY_CORE.md`。

### 7.2 凌晨整理线程

职责：

- 凌晨 3-4 点运行。
- 从当天 `dm` 中提取少量长期价值内容。
- 更新 `MEMORY_CORE.md`。
- 为 `TOMORROW_TOPICS.md` 补充昨日记忆。
- 校验未闭合话题是否仍成立。
- 失败时保留原文件并重试。

### 7.3 主动消息调度

职责：

- 读取 `TOMORROW_TOPICS.md`。
- 判断是否适合发送主动消息。
- 控制频率、时间窗口、免打扰和未回复退避。
- 发送后记录状态。

主动消息调度属于系统层，不应该让 LLM 自己决定无限制主动联系用户。

用户显式要求“明天几点来找我 / 每天几点叫我”这类时间设置时，有两条兼容入口：

- 文本直设入口会先尝试专用解析，再按规则写入 `next_active_at` 或每日主动消息时间。
- 主聊天入口允许 LLM 在自然确认文本后附不可见 `&&next:...&&` / `&&daily:...&&` marker；Action Harness 将其转成 `active_message_setting` side effect。该 side effect 必须等可见确认消息成功发送并提交后才落配置，且落配置前仍要复核本轮用户输入确实像主动消息时间设置请求。

这类时间设置只改变主动消息调度配置，不写入 `TOMORROW_TOPICS.md`，也不把“提醒事项正文”当成明日话题候选。

主动消息真正发送前仍必须进入 `EventGate`：

```text
主动消息候选
  ↓
主动消息调度
  ↓
EventGate 检查用户是否有未处理输入、今日上限、候选状态和发送幂等
  ↓
LangGraph / LLM 生成自然表达
  ↓
EventGate 复核
  ↓
转发层发送
```

### 7.4 媒体后台队列

职责：

- 处理 QQ 表情包 / 贴纸的偷取、去重、入库慢任务。
- 为普通图片提供主线程 inline 理解能力和短期 media cache。
- 下载或复制平台媒体到 `data/onebot_media`。
- 调用视觉模型生成 `image_understanding_result`。
- 对表情包执行静默偷取链路：详细分析、分类、语义化命名、dHash 去重、保存到 `memes/assets`、同步 `image_dhash_index.json`。
- 写入 `RawChatLog` 和 `/api/status.media_jobs`，用于调试。

边界：

- 不占用 `EventGate.pending_job_id`。
- 普通图片不再依赖后台 followup 发送引用回复；主聊天请求前置执行 inline 图片理解，把 `image_understanding_result` 作为本轮 ephemeral system payload 注入主状态机。
- 普通图片理解结果写入 session 级短期 media cache；如果看图期间用户继续输入导致旧 job stale，旧 job 不发送、不清 buffer，只保留 cache，由最新 snapshot 重跑并复用图片理解结果。
- 表情包 / 贴纸仍只向主聊天注入 `sticker_pending`，不把表情包画面分析直接作为主聊天可见回复依据；偷取、命名、去重和入库继续在后台静默完成。
- `meme_intake_result` 只服务入库、debug 和未来检索，不作为可见分析话术来源。
- 图片读写、dHash、索引同步等阻塞型操作必须放入线程池或等价后台执行，不能卡住 asyncio 事件循环。

## 8. 文件写入和冲突处理

由于用户可能直接编辑 Markdown 文件，文件写入必须保守。

建议规则：

- 后台任务写入前读取文件最新内容。
- 写入前检查文件是否被修改。
- 写入失败时不强行覆盖。
- 如果文件被打开导致无法保存，停止当前写入并提示用户关闭文件后重试。
- 重要文件写入前保留备份或版本记录。
- 自动整理任务只做可解释的最小修改。

重点文件：

- `SOUL.md`
- `MEMORY_CORE.md`
- `TOMORROW_TOPICS.md`
- `dm/YYYY-MM-DD.md`

## 9. 平台适配层

平台适配层只负责把外部事件转换成统一事件，并把内部输出发送回平台。

```text
平台事件
  ↓
平台适配层
  ↓
ChatEvent
  ↓
EventGate
  ↓
ConversationSnapshot
  ↓
LangGraph 决策图
  ↓
内部 ActionDecision
  ↓
EventGate 复核 snapshot 和发送幂等
  ↓
平台适配层
  ↓
平台可见消息
```

QQ 接入时可以优先调研：

- NapCat。
- OneBot 协议。
- QQ NT 事件结构。
- 戳一戳 / 拍一拍事件如何上报。
- 如何把内部 `SendItem` 映射为平台文本、emoji、表情图片等消息段。

平台适配层不应该理解 `SOUL.md`、`MEMORY_CORE.md` 或记忆整理规则。

## 10. WebUI 定位

WebUI 是验证和观测入口。

技术栈建议：

```text
Vite + React + TypeScript
```

原因：

- 适合快速搭本地调试界面。
- 和 FastAPI 后端边界清楚。
- 表情包图片网格、状态面板和 Markdown 编辑区实现成本低。
- 后续需要更复杂交互时扩展空间足够。

当前基础功能：

- 对话窗口。
- 模拟拍一拍 / 戳一戳按钮。
- 查看和编辑 `SOUL.md`。
- Provider API 配置：API Key、base URL、model、thinking、temperature；模型名包含 `kimi` 时 WebUI 使用 Kimi 温度范围提示，MiMo 额外提供 `off/adaptive/force` 联网模式配置。
- 主动消息和记忆分析时间设定。
- Context checkpoint 阈值配置。
- 表情包 Tab：分类列表、图片缩略图、复制 `meme:<file_stem>`、删除单张、过滤记录计数。
- 会话切换、清空和删除当前 session 私有数据。
- Debug 状态条：最近主模型输出、解析状态、内部 `ActionDecision`、raw log 和后台状态摘要。

后续可扩展但不阻塞主链路：

- 直接查看和编辑 session 级 `MEMORY_CORE.md`、当天 `dm/YYYY-MM-DD.md`、`TOMORROW_TOPICS.md`。
- 更完整的后台任务列表、历史运行结果和失败重放。
- 更复杂的表情包批量管理、移动、人工打标和 metadata 编辑。

WebUI 不应该膨胀成复杂后台产品，当前优先服务主链路验证、配置和可解释性。

## 11. MVP 技术栈定稿

当前 MVP 技术栈收敛为：

| 层 | 选择 |
| --- | --- |
| 后端语言 | Python 3.11+，优先 3.12 |
| API 服务 | FastAPI + Uvicorn |
| 实时事件门 | 自研 `EventGate` |
| 主回复编排 | LangGraph 低层 Graph API |
| 输出校验 | 轻量文本 Action Harness + Pydantic v2 内部模型 |
| 数据库 | SQLite，开启 WAL |
| ORM / 迁移 | SQLAlchemy 2.x + Alembic |
| LangGraph checkpoint | SQLite checkpointer，和业务 repository 隔离 |
| 定时任务 | APScheduler |
| 前端 | Vite + React + TypeScript |
| 表情包 | 本地文件目录 + `memes_data.json` + 语义化文件名 |
| 文件记忆 | `SOUL.md`、`MEMORY_CORE.md`、`dm/`、`TOMORROW_TOPICS.md` |
| 测试 | Python 单元测试为主，WebUI 后续加 Playwright |

MVP 暂不引入或仍后置：

- Postgres。
- Celery / Taskiq。
- Temporal / DBOS / Prefect。
- LlamaIndex / 向量库 / BM25。
- Pydantic AI。
- Pi / CrewAI / AutoGen。
- 微信接入和微信群聊。
- 人工审核式偷表情、云同步和复杂表情资产管理。

## 12. 暂不采用的主框架路线

### 12.1 不用 Pydantic AI 做主框架

原因：

- 它本身也是 Agent 框架。
- 如果和 LangGraph 同时作为主框架，会出现运行时职责重叠。
- 本项目只需要 Pydantic 的 schema 和校验能力。

可保留：

- 学习其结构化输出设计。
- 未来如果放弃 LangGraph，可以重新评估 Pydantic AI + durable execution 路线。

### 12.2 不用 Pi 做主框架

原因：

- Pi 更像 coding agent / session harness。
- 它的 session tree、history、context engineering 对开发和调试有价值，但不是 QQ / 微信陪伴机器人运行时的天然底座。
- 如果直接作为主框架，容易和本项目自己的消息缓冲、状态机、转发层、记忆文件和平台适配层抢职责。

可借鉴：

- session tree。
- 可分支回放。
- context 文件加载。
- skills / extensions 组织方式。

### 12.3 不用 CrewAI / AutoGen 做主框架

原因：

- 这类框架更偏多 Agent 协作和任务自动化。
- 本项目不是让多个角色协同完成工作。
- 引入后容易把产品带向“任务执行 Agent”，而不是“长期陪伴运行时”。

### 12.4 暂不使用 LlamaIndex 做主底座

原因：

- LlamaIndex 强项是索引、检索、RAG 和数据连接。
- 当前设计明确暂不优先引入 BM25 / 向量检索。

可保留：

- 未来当 `dm/` 历史文件太多、需要跨日期召回时，再把 LlamaIndex 或其他检索系统作为记忆检索组件接入。

## 13. 当前工程拆分参考

建议目录结构：

```text
app/
  main.py
  api/
  core/
    event_gate.py
    buffer.py
    snapshots.py
    graph.py
    state.py
    events.py
    decisions.py
    router.py
  llm/
    client.py
    prompts.py
  memory/
    files.py
    daily.py
    core_memory.py
    tomorrow_topics.py
  memes/
    catalog.py
    search.py
    renderer.py
  scheduler/
    jobs.py
  storage/
    db.py
    models.py
    repositories.py
    raw_log.py
    conversation_events.py
  adapters/
    webui.py
    qq.py
webui/
  src/
memes/
  memes_data.json
  image_dhash_index.json
  filtered.json
  assets/
```

当前主闭环：

1. WebUI 或 QQ / NapCat 私聊输入进入平台适配层。
2. 平台事件归一化为 `ChatEvent` 后进入 `EventGate`。
3. `EventGate` 写入 buffer，生成 `snapshot_id`，并保证同一 session single-flight。
4. LangGraph 读取稳定 snapshot。
5. 读取 `SOUL.md`、session 级 `MEMORY_CORE.md`、当天 `dm` 摘要、`TOMORROW_TOPICS.md` 和 context checkpoint。
6. 复制 durable provider transcript，并拼接本轮 runtime、media、HOT/FINAL reminder 等 ephemeral tail。
7. 调用 LLM，先把 provider raw response 归一化为 `NormalizedLLMOutput`。
8. 自研 Action Harness 解析安全 `visible_text` 中的 `WAIT`、`ENTER_CHAT:`、自然文本和表情 marker；失败时最多修复一次。
9. 归一化后的内部 `items:null` 或空时静默，不清空未回应 buffer。
10. 内部 `items` 中存在可见发送单元时，由 `EventGate` 复核 snapshot 和发送幂等，再按顺序发送到 WebUI / QQ。
11. raw log、conversation events、prompt observability 和 LLM call debug 分别记录。
12. 拍一拍 / 戳一戳触发强制关注，用户可通过 WebUI 模拟或 QQ 平台事件进入同一套链路。
13. 表情包链路支持 `&&category:keywords&&` 到二轮 `:meme:<file_stem>` 再到 WebUI / QQ 可见图片；`||category:keywords||` 仅作为 MiMo 等 provider 偶发格式漂移的兼容入口。

## 14. 当前选型原则

当前阶段最重要的不是框架能力越多越好，而是职责边界足够稳定。

底层框架必须服务以下产品原则：

- 允许不回复。
- 允许等待用户说完。
- 允许批量阅读多条消息后再回复。
- 允许用户用拍一拍 / 戳一戳强制刷新注意力。
- 允许用户直接查看和编辑记忆。
- 允许后台任务失败后可恢复。
- 允许已接入的 QQ / NapCat 和后续微信、群聊共享核心运行时。

因此当前推荐保持：

```text
EventGate 做实时发送权和一致性控制
LangGraph 做主回复决策图
自研 Action Harness 做轻量协议解析、修复和内部归一化
Pydantic BaseModel 做内部数据对象
Markdown 做可维护记忆
SQLite 做运行记录和 checkpoint
FastAPI 做服务入口
Vite / React / TypeScript 做 WebUI
平台适配层做外部接入
```
