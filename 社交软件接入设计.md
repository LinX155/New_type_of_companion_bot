# QQ 调研

## 1. 调研结论摘要

**推荐方案：NapCatQQ + OneBot v11 协议**，通过 reverse WebSocket 接入我们的 FastAPI 平台适配层。

不采用 Hermes Agent / QQ 官方 Bot API 路线。核心原因：官方 API 不支持表情包发送、戳一戳事件、消息撤回事件 —— 这三项是我们产品设计框架中 `REACT` 动作、强制刷新注意力和撤回静默取消的必要依赖。官方的 Bot API 本质是"轻量消息通道"，而我们需要的是"完整聊天能力"。

**一句话结论**：NapCat 通过直接调用 QQ NT 的 Node.js 底层模块接口，以 OneBot v11 协议对外暴露了接近完整 QQ 客户端能力的 API 和事件流，是我们 MVP 阶段的正确选择。

**第一阶段范围**：仅支持私聊（C2C），不做群聊。原因有二：(1) 群聊涉及 @提及、多用户上下文、权限管理等额外复杂度，会拖慢核心闭环验证；(2) 陪伴机器人的核心体验是 1v1 关系感，私聊是最自然的起点。群聊支持延后到后续版本。


## 2. 方案对比

### 2.1 主方案 vs 备选方案

| 维度 | NapCatQQ | Hermes Agent (QQ 官方 API) | LLOneBot |
|---|---|---|---|
| **接入原理** | 调用 QQ NT Node 模块底层接口 | QQ 开放平台 WebSocket + HTTP REST | 类似 NapCat，注入 Electron |
| **协议** | OneBot v11（ws / reverse-ws / http） | QQ 官方 Bot API v2 | OneBot v11 |
| **文本收发** | ✅ | ✅ | ✅ |
| **图片发送** | ✅（file:// / url / base64） | ❌（仅接收图片） | ✅ |
| **表情包发送** | ✅（mface / image 混合） | ❌ | ✅ |
| **戳一戳接收** | ✅（notice/notify/poke） | ❌ | ✅ |
| **戳一戳发送** | ✅（扩展 API: `friend_poke`, `group_poke`） | ❌ | ✅ |
| **消息撤回事件** | ✅（friend_recall / group_recall） | ❌ | ✅ |
| **群聊支持** | ✅ 完整 | ✅ 仅 @提及 | ✅ |
| **消息历史** | ✅（get_msg, get_group_msg_history） | ❌ | ✅ |
| **部署复杂度** | 中（需安装指定版本 QQ NT） | 低（纯云端 API） | 中 |
| **Windows 支持** | ✅ OneKey 一键包 | ✅ | ✅ |
| **风控风险** | ⚠️ 中等（非官方协议，有封号风险） | ✅ 低（官方 API） | ⚠️ 中等 |
| **审核要求** | 无 | ✅ 需提交审核发布 | 无 |
| **项目活跃度** | 🟢 活跃（v4.15.1, 2025年持续更新） | 🟢 活跃 | 🟡 中等 |
| **GitHub** | [NapNeko/NapCatQQ](https://github.com/NapNeko/NapCatQQ) | [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent) | [LLOneBot/LLOneBot](https://github.com/LLOneBot/LLOneBot) |

### 2.2 官方 API 的能力鸿沟

QQ 官方 Bot API 的限制是结构性的，不是暂时的：

- **只能发文本和 Markdown** —— 没有图片/表情包发送 API
- **没有戳一戳事件** —— 官方 WebSocket 事件列表不含 poke/notify
- **没有撤回事件** —— 无法感知用户撤回
- **私聊需沙箱审核** —— 测试阶段只能在沙箱频道收发消息
- **群聊仅有 @提及** —— 不能监听完整的群聊上下文

这些限制直接阻断了我们产品设计的三个核心机制：`REACT`（表情包发送）、戳一戳强制刷新、撤回静默取消。


## 3. 推荐方案：NapCatQQ + OneBot v11

### 3.1 架构图

```
┌─────────────────────────────────────────────────────┐
│                     Windows 11                       │
│                                                      │
│  ┌──────────────┐          ┌────────────────────┐   │
│  │  QQ NT 客户端 │ ◀──调用── │   NapCatQQ Shell   │   │
│  │  (Node 模块)  │          │  (TypeScript 运行时) │   │
│  │  ≥ 40768     │          │  内置 WebUI :6099   │   │
│  └──────────────┘          └──────────┬─────────┘   │
│                                       │              │
│                              reverse WebSocket       │
│                              (OneBot v11 JSON)       │
│                                       │              │
│                              ┌────────▼─────────┐   │
│                              │  QQ Adapter       │   │
│                              │  (Python/FastAPI) │   │
│                              │  端口: 8080       │   │
│                              └────────┬─────────┘   │
│                                       │              │
│                              统一 ChatEvent          │
│                                       │              │
│                              ┌────────▼─────────┐   │
│                              │   EventGate       │   │
│                              └──────────────────┘   │
└─────────────────────────────────────────────────────┘
```

关键点：
- NapCat 和 QQ NT 运行在同一台 Windows 机器上
- NapCat 通过 **reverse WebSocket**（被动连接模式）向我们的 FastAPI 服务推送事件
- 我们的 Python adapter 也通过同一个 WebSocket 连接调用 OneBot API
- 所有通信走 localhost，零网络延迟，无外网暴露

### 3.2 NapCat 工作原理

```
QQ 用户操作 → QQ NT 客户端
                    │
         ┌──────────┼──────────┐
         │          │          │
    Electron     Node 模块   渲染层
    (UI壳)       (核心逻辑)  (Chromium)
                    │
              NapCat 在此层调用
              主动 invoke QQ 的 Node 模块接口
              不需要 Hook 或注入 Electron
```

NapCat 不重新实现 QQ 协议（不像早期 Mirai/Android 协议），也不向 Electron 注入代码（不像某些 Hook 方案）。它直接调用 QQ 自身暴露给客户端的 Node.js 模块接口 —— 这意味着它天然能用 QQ 所有已实现的能力，不需要自己维护协议适配。

### 3.3 连接方式选择：Reverse WebSocket

OneBot v11 支持四种连接方式。我们选择 **reverse WebSocket**：

| 方式 | 方向 | 适用场景 |
|---|---|---|
| HTTP | 我们 → NapCat | 简单但无事件推送 |
| 正向 WS | 我们 → NapCat | NapCat 作为 WS Server |
| **反向 WS（Reverse WS）** | **NapCat → 我们** | **我们作为 WS Server，NapCat 主动连接** |
| HTTP POST | NapCat → 我们 | 事件上报用 HTTP |

选择 Reverse WS 的原因：

- 我们的 FastAPI 已经是 HTTP Server，加上 WebSocket endpoint 即可
- NapCat 主动连接我们，重启/断连后自动重连
- 不需要为 NapCat 开额外的公网端口

NapCat WebUI 配置：
```
连接方式：反向 WebSocket
连接地址：ws://127.0.0.1:8080/qq/ws
Token：（可选，用于验证连接来源）
```

### 3.3.1 后端需要做什么：WebSocket 双向通道

Reverse WS 模式下，NapCat 作为客户端主动连接我们的 FastAPI。整个 QQ 通信就靠**这一条 WebSocket 连接**——它既是事件上报通道（NapCat → 我们），也是 API 调用通道（我们 → NapCat）。后端只需要做三件事：

**① 注册一个 WebSocket 路由**

在 FastAPI 中挂载 WebSocket endpoint，等 NapCat 连接进来：

```python
# app/main.py
from fastapi import FastAPI, WebSocket
from app.adapters.qq import QQAdapter

app = FastAPI()
qq_adapter = QQAdapter(event_gate)

@app.websocket("/qq/ws")
async def qq_websocket(ws: WebSocket):
    await qq_adapter.handle_ws(ws)
```

不需要额外的 HTTP 路由、不需要消息队列、不需要 redis —— 一个 WebSocket 路由就够。

**② 接收事件（读方向：NapCat → 我们）**

连接建立后，NapCat 持续推送 JSON 事件。我们的 adapter 循环读取、解析、转发给 EventGate：

```
NapCat WS Client                    FastAPI WS Server
      │                                    │
      │── connect ──────────────────────▶  │  ws.accept()
      │                                    │
      │── {"post_type":"message", ...} ──▶ │  ws.iter_json()
      │── {"post_type":"notice", ...} ───▶ │  → parse → EventGate
      │── {"post_type":"message", ...} ──▶ │  ...
```

**③ 调用 API（写方向：我们 → NapCat）**

当 ActionDecision 需要发消息时，adapter 通过**同一条** WebSocket 连接把 OneBot API 请求发回去：

```python
# 发送方向：我们 → NapCat（同一条 WebSocket 连接）
await ws.send_json({
    "action": "send_private_msg",
    "params": {
        "user_id": 123456789,
        "message": [{"type": "text", "data": {"text": "来了"}}]
    },
    "echo": "unique_request_id"
})
```

这就是 NapCat + OneBot 的完整通信模型：**一条 WebSocket = 事件流 + API 调用**。没有额外的 HTTP 调用，没有轮询，不需要消息队列。

### 3.3.2 为什么不需要消息队列

常见的 IM 接入方案会引入消息队列（如 Redis pub/sub）来解耦"接收消息的 WS 连接"和"发送消息的 HTTP API"。但 OneBot reverse WS 模式下不需要：

- 接收和发送走**同一条 TCP 连接**，天然串行，不存在多连接竞争
- 我们的 EventGate 已经做了 single-flight 控制，同会话不会并发发送
- 所有通信发生在 localhost 上，网络延迟可忽略，不需要异步解耦

因此后端只需维护一个 `WebSocket` 对象引用，不需要额外基础设施。

### 3.4 OneBot v11 消息段格式

OneBot v11 的消息以**消息段数组（Message Segment Array）**表示，每个段是一个 JSON 对象：

```json
// 纯文本
{ "type": "text", "data": { "text": "来了。怎么了？" } }

// 图片（支持本地路径、URL、Base64）
{ "type": "image", "data": { "file": "file:///C:/companion_bot/memes/assets/helpless/helpless_miku_cry_floor.webp" } }

// QQ 自带 emoji 表情（Face ID）
{ "type": "face", "data": { "id": "178" } }

// 商城表情/动态表情（我们发自定义表情包走 image 更可靠）
{ "type": "image", "data": { "file": "file:///C:/companion_bot/memes/assets/helpless/helpless_miku_cry_floor.webp" } }
```

**发送一条完整的消息示例**（文本 + 表情图）：

```json
{
  "action": "send_private_msg",
  "params": {
    "user_id": 123456789,
    "message": [
      { "type": "text", "data": { "text": "我也无语了" } },
      { "type": "image", "data": { "file": "file:///C:/companion_bot/memes/assets/helpless/helpless_miku_cry_floor.webp" } }
    ]
  }
}
```


## 4. 关键能力映射

### 4.1 QQ 事件 → ChatEvent 映射

根据 OneBot v11 事件规范，我们需要处理的映射关系：

| QQ 事件（OneBot） | 触发条件 | → ChatEvent.event_type |
|---|---|---|
| 私聊消息 `message.private` | 好友发来文本/图片/表情 | `TEXT` / `IMAGE` / `STICKER` |
| 群聊消息 `message.group` | 群内有人发言（含 @机器人） | `TEXT` / `IMAGE` / `STICKER` |
| 好友戳一戳 `notice.notify.poke`（含 `sender_id`） | 好友戳了机器人 | `POKE` |
| 群戳一戳 `notice.notify.poke`（含 `group_id`） | 群内有人戳机器人 | `POKE` |
| 好友撤回 `notice.friend_recall` | 好友撤回消息 | `RECALL` |
| 群撤回 `notice.group_recall` | 群内有人撤回消息 | `RECALL` |

### 4.2 消息类型解析细节

**区分 TEXT / IMAGE / STICKER**：

```python
def parse_message_event(event: dict) -> ChatEvent:
    message = event.get("message", [])

    segments = {
        "text": [],
        "image": [],
        "face": [],
        "mface": [],
    }

    for seg in message:
        if seg["type"] == "text":
            segments["text"].append(seg["data"]["text"])
        elif seg["type"] == "image":
            segments["image"].append(seg["data"])
        elif seg["type"] in ("face", "mface"):
            segments["mface"].append(seg["data"])

    # 判断主事件类型
    if segments["mface"] and not segments["text"]:
        event_type = "STICKER"  # 纯表情
    elif segments["image"] and not segments["text"]:
        event_type = "IMAGE"    # 纯图片
    else:
        event_type = "TEXT"     # 有文本，或混合内容

    text = "".join(segments["text"]) if segments["text"] else None

    return ChatEvent(
        event_type=event_type,
        text=text,
        # ... 其他字段
    )
```

### 4.3 ActionDecision → QQ 消息发送

我们的 action 系统到 OneBot API 的映射：

```
ActionDecision { action, text }
         │
         ▼
    QQ 发送适配器
         │
    ┌────┼────────────────────────────┐
    │    │                            │
  WAIT  REPLY / LIGHT_ACK           REACT
    │    │                            │
    │    ▼                            ▼
    │  send_private_msg           判断 text 前缀：
    │  message=[{text}]           │
    │                             ├─ "meme:<file_stem>"
    │                             │   → send_private_msg
    │  不发送                      │     message=[{image: file://path}]
    │  （核心：                  │
    │   text=null                ├─ "emoji:<code>"
    │   什么都不发）               │   → send_private_msg
    │                             │     message=[{face: id}]
                                  │
                                  └─ 其他
                                      → send_private_msg
                                        message=[{text}]（降级）
```

伪代码实现：

```python
async def send_action_decision(user_id: int, decision: ActionDecision):
    if decision.action == "WAIT" or decision.text is None:
        return  # 核心：text=null 不发任何消息

    message_segments = build_message_segments(decision)

    if not message_segments:
        return  # 没有有效消息段

    await call_onebot_api("send_private_msg", {
        "user_id": user_id,
        "message": message_segments,
    })


def build_message_segments(decision: ActionDecision) -> list[dict]:
    if decision.action == "REACT":
        return build_react_segments(decision.text)
    else:
        # REPLY, LIGHT_ACK, ENTER_CHAT
        return [{"type": "text", "data": {"text": decision.text}}]


def build_react_segments(text: str) -> list[dict]:
    if text.startswith("meme:"):
        file_stem = text[5:]  # 去掉 "meme:"
        file_path = resolve_meme_path(file_stem)
        if file_path:
            return [{"type": "image", "data": {"file": f"file:///{file_path}"}}]
        else:
            return []  # 文件不存在，降级静默

    if text.startswith("emoji:"):
        # QQ Face ID 映射
        emoji_code = text[6:]
        face_id = EMOJI_TO_QQ_FACE.get(emoji_code)
        if face_id:
            return [{"type": "face", "data": {"id": str(face_id)}}]

    # 纯文本 emoji（如 "🙂"）→ 直接发文本
    return [{"type": "text", "data": {"text": text}}]
```


## 5. 技术细节：戳一戳 / 拍一拍

### 5.1 NapCat 的 poke 事件格式

当用户戳机器人时，OneBot 推送的 **notice 事件**：

**群聊戳一戳：**
```json
{
  "post_type": "notice",
  "notice_type": "notify",
  "sub_type": "poke",
  "time": 1700000000,
  "self_id": 1234567890,
  "user_id": 111111111,
  "target_id": 222222222,
  "group_id": 987654321,
  "raw_info": {}
}
```

**好友（私聊）戳一戳：**
```json
{
  "post_type": "notice",
  "notice_type": "notify",
  "sub_type": "poke",
  "time": 1700000000,
  "self_id": 1234567890,
  "user_id": 111111111,
  "target_id": 222222222,
  "sender_id": 111111111,
  "raw_info": {}
}
```

### 5.2 解析与映射

```python
def parse_poke_event(event: dict) -> ChatEvent:
    # 只有当 target_id == self_id 时，才是"用户戳了机器人"
    if event["target_id"] != event["self_id"]:
        return None  # 不是戳机器人，忽略

    platform = "qq_group" if "group_id" in event else "qq_private"
    user_id = event["user_id"]
    group_id = event.get("group_id")

    return ChatEvent(
        event_id=f"poke_{event['time']}_{user_id}",
        platform=platform,
        user_id=str(user_id),
        group_id=str(group_id) if group_id else None,
        event_type="POKE",
        text=None,
        timestamp=event["time"],
        raw=event,
    )
```

### 5.3 戳一戳 → EventGate 行为

根据产品设计框架 §3.5.11：

- `POKE` 事件进入 EventGate 后，**立即刷新注意力**：将当前 buffer 提交为 snapshot 并启动主回复 LLM
- 触发进入 `HOT` 状态（热聊在场态，持续 10-30 分钟）
- LLM 仍然可以输出 WAIT/REPLY/REACT，戳一戳不强制必须回复
- 工程层做重复事件去重：同一用户短时间内多次 poke 合并为一次触发

### 5.4 主动发送戳一戳（扩展能力）

NapCat 实现了 go-cqhttp 兼容的扩展 API，支持机器人主动戳用户：

```json
// 好友戳一戳
{ "action": "friend_poke", "params": { "user_id": 123456, "target_id": 123456 } }

// 群戳一戳
{ "action": "group_poke", "params": { "group_id": 987654321, "user_id": 123456 } }
```

MVP 阶段暂不使用主动戳一戳能力（避免骚扰感），但 API 可用。


## 6. 技术细节：仅传输 text 字段

### 6.1 问题定义

开发计划中的核心问题：
> "如何传输且仅传输 text 字段"

我们的架构中，ActionDecision 有两个字段：
- `action`：系统用，"WAIT" / "REPLY" / "REACT" 等 —— **绝不发送给用户**
- `text`：用户可见，唯一能转化为 QQ 聊天内容的东西

### 6.2 发送适配器的核心规则

```python
class QQMessageSender:
    """QQ 发送适配器 —— 核心约束：只发 text 转化的内容，action 永远不泄露"""

    async def send(self, user_id: int, decision: ActionDecision):
        # 规则 1：action 不进入消息
        # 规则 2：text=null 不发送任何消息
        # 规则 3：search_meme:* 是内部协议，拦截
        # 规则 4：meme:* 转图片，emoji:* 转 Face，其他发文本

        text = decision.text

        if text is None:
            return  # WAIT 或 text:null → 静默

        if text.startswith("search_meme:"):
            return  # 内部检索协议，永远不发

        segments = self._text_to_segments(text)
        if segments:
            await self._api("send_private_msg", {
                "user_id": user_id,
                "message": segments,
            })

    def _text_to_segments(self, text: str) -> list[dict] | None:
        """将 ActionDecision.text 转换为 OneBot 消息段"""
        if text.startswith("meme:"):
            return self._meme_to_image(text)
        if text.startswith("emoji:"):
            return self._emoji_to_face(text)
        return [{"type": "text", "data": {"text": text}}]
```

### 6.3 为什么 action 不会泄露

action 字段**不在 `text` 字段中**。我们的 JSON 格式保证了这一点：

```json
{
  "action": "ENTER_CHAT",
  "text": "来了。怎么了？"
}
```

发送适配器只读取 `text`。用户看到的是 "来了。怎么了？"，完全不知道背后有 `ENTER_CHAT` 这个动作。这就是"双输出通道"在发送层的落地。


## 7. 部署方案

### 7.1 Windows 11 环境要求

| 组件 | 要求 |
|---|---|
| 操作系统 | Windows 11 x64 |
| QQ NT | ≥ 9.9.26-44343，推荐 ≥ 40768 |
| VC++ 运行库 | Visual C++ Redistributable 2015-2022 x64 |
| Node.js | NapCat OneKey 包已内置，无需单独安装 |
| Python | 3.11+（已有） |
| 磁盘 | ~2GB（QQ + NapCat + 表情包素材） |

### 7.2 安装步骤

**Step 1：安装 NapCat**

1. 从 [NapCatQQ Releases](https://github.com/NapNeko/NapCatQQ/releases) 下载 `NapCat.Shell.Windows.OneKey.zip`
2. 解压到不含中文/空格的目录，如 `C:\NapCat\`
3. 双击 `launcher.bat` 启动
4. 控制台输出 WebUI 密钥（随机密码）
5. 浏览器打开 `http://localhost:6099`，输入密钥
6. 扫码登录 QQ

**Step 2：配置 WebSocket 连接**

在 NapCat WebUI (localhost:6099) 中：

```
网络服务 → 添加 WebSocket 服务
  服务类型：反向 WebSocket
  连接地址：ws://127.0.0.1:8080/qq/ws
  Token：（可选，建议设置一个随机 Token）
  启用：✅
```

**Step 3：实现 FastAPI WebSocket endpoint**

```python
# app/adapters/qq.py
from fastapi import WebSocket

class QQAdapter:
    def __init__(self, event_gate: EventGate):
        self.event_gate = event_gate
        self.ws: WebSocket | None = None

    async def handle_ws(self, ws: WebSocket):
        """接收 NapCat 的 reverse WebSocket 连接"""
        await ws.accept()
        self.ws = ws

        async for raw in ws.iter_json():
            event = self.parse_onebot_event(raw)
            if event:
                await self.event_gate.submit(event)

    def parse_onebot_event(self, raw: dict) -> ChatEvent | None:
        post_type = raw.get("post_type")

        if post_type == "message":
            return self._parse_message(raw)
        elif post_type == "notice":
            return self._parse_notice(raw)
        return None

    async def call_api(self, action: str, params: dict):
        """调用 OneBot API"""
        if not self.ws:
            raise ConnectionError("WebSocket not connected")

        request = {
            "action": action,
            "params": params,
            "echo": str(uuid.uuid4()),
        }
        await self.ws.send_json(request)
```

**Step 4：启动顺序**

```
1. 启动 QQ NT（或由 NapCat launcher.bat 自动拉起）
2. 启动 NapCat（launcher.bat），扫码登录
3. NapCat WebUI 确认 WebSocket 连接状态为"已连接"
4. 启动我们的 FastAPI 服务
5. NapCat 自动通过 reverse WS 连接到 ws://127.0.0.1:8080/qq/ws
```

### 7.3 完整环境拓扑

```
┌─────────────────────────────────────────┐
│              localhost 环境               │
│                                          │
│  QQ NT (:4301) ←── NapCat Shell (:6099)  │
│                          │               │
│                   reverse WS             │
│                          │               │
│                  FastAPI (:8080)          │
│                   /qq/ws                 │
│                   /webui                 │
│                                          │
│  所有服务本地运行，无外网依赖               │
└─────────────────────────────────────────┘
```


## 8. 风险与限制

### 8.1 NapCat 核心风险

| 风险 | 等级 | 说明 | 缓解措施 |
|---|---|---|---|
| **账号风控/封号** | 🔴 高 | 非官方协议，腾讯可能检测并封禁 | 使用老号（注册 1 年+）；避免高频操作；不用于群发广告 |
| **QQ 版本升级破坏** | 🟡 中 | QQ NT 更新可能改变底层模块接口 | 锁定 QQ 版本（关闭自动更新）；关注 NapCat 发版节奏 |
| **项目停更风险** | 🟢 低 | NapCat 当前活跃（v4.15.1），社区活跃 | 备用方案：LLOneBot 或 Lagrange |
| **Windows 特定问题** | 🟡 中 | QQ 在某些 Windows 版本上不稳定 | 使用 Win11，定期重启 |
| **WebSocket 断连** | 🟢 低 | 网络异常时 WS 连接断开 | Reverse WS 自带重连；添加健康检查 |

### 8.2 腾讯风控特别说明

从安全角度需要注意：

- **扫码登录** —— NapCat 使用手机 QQ 扫码登录，本质是真实的 QQ 客户端
- **行为特征** —— 避免在极短时间内发送大量相同消息（像群发广告）
- **新号风险** —— 新注册 QQ 号比老号更容易被风控
- **Docker 限制** —— Docker 部署无法接收语音和文件（但本地 Windows OneKey 包无此问题）

### 8.3 降级方案

如果 NapCat 不可用（封号、版本不兼容、项目停更），可降级为：

1. **LLOneBot** —— 与 NapCat 同样是 NTQQ → OneBot 方案，架构相似，可作为备用
2. **Lagrange** —— C# 实现的 NTQQ 协议客户端，不依赖 QQ 主程序
3. **Hermes Agent QQ 官方 API** —— 退回到仅文本聊天（丢失表情包和戳一戳能力）

降级场景下，鲁棒性设计文档中的降级规则生效：
- `REACT` → 降级为 `LIGHT_ACK` / `REPLY` / `WAIT`
- 戳一戳不可用 → 保留普通消息触发和主动消息机制


## 9. 开发路线

### Phase 1：基础连通（预计 1-2 天）— 仅私聊
- [ ] 安装 NapCat OneKey 包，完成扫码登录
- [ ] 配置 reverse WebSocket → FastAPI `/qq/ws`
- [ ] 实现 OneBot 事件解析器：仅处理 `message.private`（私聊），忽略 `message.group`
- [ ] 实现 OneBot API 调用：仅 `send_private_msg`
- [ ] 验证：在 QQ 中给机器人发 "你好"，收到文本回复

### Phase 2：戳一戳支持（预计 0.5 天）— 仅私聊戳一戳
- [ ] 解析 `notice.notify.poke` 事件
- [ ] 第一阶段仅处理好友戳一戳（含 `sender_id`），暂忽略群戳一戳（含 `group_id`）
- [ ] 集成 EventGate 的强制刷新注意力逻辑
- [ ] 验证：在 QQ 好友聊天中戳机器人，收到 "来了。怎么了？"

### Phase 3：表情包发送（预计 1 天）
- [ ] 实现 `meme:<file_stem>` → OneBot image 消息段
- [ ] 本地图片路径 → `file:///` URL
- [ ] `emoji:<code>` → OneBot face 消息段（可选）
- [ ] 验证：对话中收到表情包

### Phase 4：表情包接收 + 偷表情（预计 2 天）
- [ ] 解析 `message.private` 中的图片消息（仅私聊）
- [ ] 下载/缓存接收到的图片
- [ ] 接入 `steal_meme` 工具链：LLM 判断 → 多模态生成文件名 → 入库
- [ ] 撤回事件解析（仅 `friend_recall`，暂不处理群撤回）
- [ ] 集成 EventGate 的撤回静默取消逻辑

### Phase 5：完善与压测（预计 1 天）
- [ ] WebSocket 断连重连稳定性
- [ ] 长对话连续性测试
- [ ] 表情包发送成功率统计
- [ ] 压力测试：短时间大量消息、异常断连恢复

### 第二阶段（延后）：群聊支持
- [ ] 群聊消息事件解析（`message.group`）
- [ ] 群聊 @提及触发
- [ ] 群戳一戳事件（`notice.notify.poke` 含 `group_id`）
- [ ] 群撤回事件（`group_recall`）

---

## 附录 A：OneBot v11 完整事件类型速查

| post_type | 具体事件 | 方向 | 我们的用途 |
|---|---|---|---|
| `message` | `message.private` | 接收 | 私聊文本/图片/表情 |
| `message` | `message.group` | 接收 | 群聊文本/图片/表情 |
| `notice` | `notify.poke` | 接收 | 戳一戳 → 强制刷新注意力 |
| `notice` | `friend_recall` | 接收 | 好友撤回 → 静默取消 pending msg |
| `notice` | `group_recall` | 接收 | 群撤回 → 静默取消 |
| `notice` | `friend_add` | 接收 | 新增好友（可选） |
| `notice` | `group_increase` | 接收 | 新成员入群（可选） |

## 附录 B：关键 API 速查

| API | 用途 |
|---|---|
| `send_private_msg` | 发送私聊消息 |
| `send_group_msg` | 发送群聊消息 |
| `send_msg` | 根据上下文自动选择私聊/群聊 |
| `delete_msg` | 撤回机器人自己发的消息 |
| `get_msg` | 获取历史消息详情 |
| `friend_poke` | 主动戳好友（扩展 API） |
| `group_poke` | 主动戳群成员（扩展 API） |
| `get_image` | 下载图片文件 |
| `get_login_info` | 获取机器人 QQ 号 |
| `get_version_info` | 获取 NapCat 版本 |
| `set_restart` | ⚠️ NapCat 不支持此 API |

## 附录 C：参考资料

- [NapCatQQ GitHub](https://github.com/NapNeko/NapCatQQ)
- [NapCat 官方文档](https://doc.napneko.icu)
- [NapCat OneBot 事件系统](https://doc.napneko.icu/onebot/event)
- [NapCat API 兼容列表](https://napneko.github.io/develop/api)
- [OneBot v11 协议规范](https://11.onebot.dev)
- [Hermes Agent QQ Bot 文档](https://www.majiabin.com/hermes/user-guide/messaging/qqbot/)
- [作为参考调研的 Hermes Agent 接入指南](https://gitcode.csdn.net/69e82a600a2f6a37c5a16d71.html)

---

# 微信调研

## 1. 调研结论摘要

**当前没有完美的微信接入方案。** 微信的封闭程度远高于 QQ。市面上有两类路线：

| 路线 | 代表方案 | 风险 | 拍一拍 | 表情包 | 撤回 |
|---|---|---|---|---|---|
| **官方 iLink Bot API** | Hermes Agent 原生 / OpenClaw-weixin（腾讯官方） | 🟢 零风险 | ❌ | ❌ | ❌ |
| **Hook 注入** | WeChatFerry | 🔴🔴 极高（>80%） | ✅ | ✅ | ✅ |

两条路线都不完美：官方路线安全但能力缺失（和 QQ 官方 API 一样的问题），Hook 路线能力强但封号风险极高（和 QQ 的 NapCat 不同——NapCat 不走 DLL 注入，风险可控得多）。

对比 QQ 有 NapCat（调用 NT Node 模块，风险可控），微信生态中没有一个"既安全又能满足刚需"的中间方案。

**当前建议**：第一阶段优先完成 QQ 接入，微信接入暂缓。如果必须接入微信，WeChatFerry 是目前唯一能满足"拍一拍 + 表情包 + 撤回"三项刚需的方案，但必须用小号且承受高风险。


## 2. 官方路线：iLink Bot API（Hermes / OpenClaw）

### 2.1 这是什么

从 2026 年 4 月起，Hermes Agent v0.9.0 和 OpenClaw（通过腾讯官方插件 `@tencent-weixin/openclaw-weixin`）都原生支持了微信个人号接入。底层都是腾讯的 **iLink Bot API** —— 一种官方开放的个人微信号 Bot 协议。

接入方式极简：扫码即可，不需要公网 IP、不需要企业认证、不需要装额外客户端。

```
Hermes Agent:   hermes gateway setup → 选择 Weixin → 扫码 → 完成
OpenClaw:       openclaw plugins install "@tencent-weixin/openclaw-weixin" → 扫码 → 完成
```

### 2.2 iLink Bot API 能力

| 能力 | 支持 | 说明 |
|---|---|---|
| 文本收发 | ✅ | 自动分片（4000 字符限制） |
| 图片收发 | ✅ | AES-128-ECB 加密的 CDN 上传/下载 |
| 语音接收 | ✅ | 微信内置 ASR 转文字 |
| 语音发送 | ❌ | iLink 未开放语音发送接口 |
| 视频接收 | ✅ | 下载为 .mp4 |
| 文件收发 | ✅ | 支持任意格式 |
| **表情包/贴纸** | ❌ | "iLink protocol does not open sending interfaces for these types"，收到表情包时静默忽略 |
| **拍一拍** | ❌ | 不在 iLink 支持的消息类型中 |
| **撤回** | ❌ | 撤回通知存在但 adapter 不处理；已发消息无法撤回 |
| Markdown | ❌ | 微信客户端不渲染，adapter 自动剥离格式 |
| 群聊 | ⚠️ | 群聊消息可接收但 @提及逻辑未完成，无法区分是否 @机器人 |
| 朋友圈 | ❌ | iLink 不支持 |

### 2.3 本质限制

iLink Bot 是一个**特殊的 Bot 账号**，不是真实的微信个人号。它的能力边界和 QQ 官方 Bot API 几乎一模一样 —— 只能收发基础消息类型。表情包、拍一拍、撤回这三个我们产品设计的核心能力，iLink 都不支持。

这也解释了为什么 Hermes Agent 的 PR #7166 专门强调"no buttons, inline keyboards, card messages, or quick replies" —— iLink 就是一条基础消息通道。

### 2.4 结论

iLink 方案适合只需要文本+图片对话的场景，不适合我们。它和 QQ 官方 Bot API 一样，无法支撑 `REACT`、戳一戳强制刷新、撤回静默取消三个核心机制。


## 3. Hook 路线：WeChatFerry

### 3.0 为什么微信比 QQ 难接入

QQ 的 NapCat 方案之所以可行，核心在于：
- QQ NT 的 Node 模块接口是**内部可调用的 API**，NapCat 调用这些接口不修改 QQ 进程本身
- 风险可控：扫码登录真实 QQ 客户端，行为特征正常

微信的 WeChatFerry 方案则完全不同：
- 直接向微信进程**注入 DLL**，读写微信内存空间
- 这是典型的恶意软件行为，Windows Defender / 微信自身都可能在更新中检测到
- 一旦被检测，封号率 >80%（来源：社区反馈）

### 3.1 架构与能力详情

#### 架构图

```
┌─────────────────────────────────────────────────────┐
│                     Windows 11                       │
│                                                      │
│  ┌──────────────────┐    DLL注入 + Hook              │
│  │  微信 PC 客户端    │ ◀────────── │                │
│  │  v3.9.2~3.9.10   │             │                │
│  └──────────────────┘    ┌─────────┴──────────┐    │
│                           │  WeChatFerry (WCF) │    │
│                           │  C++ Hook 核心      │    │
│                           │  + Python SDK       │    │
│                           │  (wcferry)         │    │
│                           └────────┬───────────┘    │
│                                    │                │
│                         NanoRPC + HTTP 回调          │
│                                    │                │
│                           ┌────────▼───────────┐    │
│                           │  WeChat Adapter    │    │
│                           │  (Python/FastAPI)  │    │
│                           └────────┬───────────┘    │
│                                    │                │
│                            统一 ChatEvent            │
│                                    │                │
│                           ┌────────▼───────────┐    │
│                           │   EventGate         │    │
│                           └────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

与 QQ + NapCat 方案的关键区别：
- WCF 不是独立进程，它通过 DLL **进入微信进程**后才工作
- 消息回调通过 HTTP POST 推送给我们的 FastAPI（而不是 WebSocket 双向通道）
- API 调用走 NanoRPC（类似的 RPC 协议），不是 OneBot v11

#### 关键能力映射

#### 拍一拍（接收）

WCF 将拍一拍作为消息类型 `922746929` 上报，包含 XML 详情：

```python
# WCF 消息回调
@app.post("/wcf/callback")
async def on_wcf_message(msg: WcfMsg):
    if msg.type == 922746929:  # 拍一拍
        # msg.xml 示例：
        # <sysmsg type="pat">
        #   <pat>
        #     <fromusername>wxid_abc</fromusername>    ← 谁拍的
        #     <pattedusername>wxid_robot</pattedusername> ← 被拍的是谁
        #     <template>某某 拍了拍 我</template>
        #   </pat>
        # </sysmsg>

        # 只有被拍的是机器人自己时才触发
        if is_patted_self(msg.xml):
            return ChatEvent(event_type="POKE", ...)
```

与 QQ 的对应关系：
```
QQ:  notice.notify.poke  → target_id == self_id → ChatEvent(POKE)
微信: WCF msg type 922746929 → parse xml → pattedusername == self → ChatEvent(POKE)
```

#### 拍一拍（发送）

```python
# WCF 主动发起拍一拍
wcf.send_pat_msg(roomid="群ID", wxid="要拍的人的wxid")
```

但 MPV 阶段和 QQ 一样，不使用主动拍一拍能力。

#### 表情包发送

```python
# WCF 发送 GIF/表情（即我们的自定义表情包）
# 需要先把本地图片转为 GIF 或直接用图片发送
wcf.send_image(path="C:/companion_bot/memes/assets/helpless/helpless_miku_cry_floor.webp")

# 或通过 send_gif 发送 GIF 动图
wcf.send_gif(path="C:/companion_bot/memes/assets/smiling_kirby_soft_smile.gif")
```

微信的表情包发送与 QQ 的区别：
- 微信没有 OneBot 那样的 `mface` 消息段类型
- 表情包以**图片消息**形式发送（GIF 或静态图）
- 这意味着我们收到微信表情时，它表现为普通图片（`IMAGE` 事件类型），无法区分"用户是故意发表情还是发了一张图" —— 这对偷表情策略有影响

#### 消息撤回

```python
# WCF API
wcf.revoke_msg(msg_id=123456)
```

收到撤回事件同样通过消息类型判断（具体 type ID 视 WCF 版本而定），与 QQ 的 `friend_recall` / `group_recall` 对应。

#### 与 QQ 方案的工程差异

| 维度 | QQ (NapCat + OneBot) | 微信 (WeChatFerry) |
|---|---|---|
| **通信方式** | 单一 WebSocket（双向） | NanoRPC（调用）+ HTTP POST（接收回调） |
| **连接方向** | NapCat → 我们（reverse WS） | 我们 → WCF（主动连接）+ WCF → 我们（回调） |
| **事件格式** | OneBot v11 JSON | 自定义 WcfMsg JSON |
| **消息段模型** | 消息段数组（text/image/face/...） | 单 type + content/xml |
| **平台适配器复杂度** | 中等（OneBot 有规范） | 较高（需理解 WCF 专有格式 + XML 解析） |
| **表情包** | image 或 mface 消息段 | 统一为 image 消息（无法区分表情/图片） |

### 3.2 风险评估

#### 封号风险

| 风险因素 | 说明 |
|---|---|
| **DLL 注入检测** | 微信的反作弊系统可以检测到非授权模块注入进程 |
| **版本不匹配** | WCF 必须严格匹配特定微信版本才能正常工作；微信自动更新可能导致 WCF 失效 |
| **异常行为模式** | 高频消息、非常规操作触发风控 |
| **同一设备登录** | WCF 必须和微信客户端在同一台 Windows 机器上运行 |

#### 风险缓解措施（如果决定使用）

- 使用**微信小号**，绝不用主号
- 锁定微信版本，**禁止自动更新**
- 消息频率控制在 **5条/分钟** 以内
- 短信间隔加随机延迟（1-5秒）
- 不使用群发功能
- 定期检查 WCF 版本更新

#### 结论 & 建议

微信接入的"拍一拍 + 表情包 + 撤回"三项刚需，WeChatFerry 是市面上唯一能满足的方案。但代价是极高的封号风险和工程复杂度（DLL 注入 + 双通道通信 + XML 解析）。

**当前建议优先级**：
1. 先完成 QQ 接入，跑通完整陪伴闭环
2. 微信接入作为长期探索项，不阻塞 MVP 交付
3. 如果未来微信必须接入，用小号 + WeChatFerry 方案，做好随时被封的准备


## 附录：微信调研参考资料

- [WeChatFerry GitHub](https://github.com/lich0821/WeChatFerry) — Hook 方案，功能最完整，当前唯一活跃的选择
- [WeChatFerry 消息类型文档](https://github.com/lich0821/WeChatFerry/blob/master/README.MD)
- [企业微信开放平台](https://work.weixin.qq.com/api/doc) — 官方 API，安全但功能受限
- [微信机器人方案对比参考](https://blog.csdn.net/weixin_38861218/article/details/146997227)

