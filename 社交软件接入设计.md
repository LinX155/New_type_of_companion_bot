# 社交软件接入设计

本文档是社交软件接入的统一设计入口，已整合原 `社交软件接入设计.md` 与 `QQ对接设计.md` 的非重复信息。

## 前置项：以onebot能力为上限的扩展项设计，暂未在WEBUI中实现

消息级动作属于 QQ / OneBot 接入层能力，不进入当前底层 `action + items` 主协议。

当前主协议只表达本轮宏观意图和可见发送单元：

```json
{
  "action": "WAIT | REPLY | LIGHT_ACK | REACT | ENTER_CHAT | END_CHAT",
  "items": []
}
```

QQ 接入层负责把 `items[]` 和运行时上下文翻译成 OneBot / NapCat 可执行动作。不要为了 QQ 平台能力，在核心协议中新增 `QUOTE_REPLY`、`RECALL_AND_RESEND`、`REACTION_TO_MESSAGE` 等平台化 action。

已调研结论：

| 能力 | OneBot / NapCat 支持情况 | 设计归属 |
| --- | --- | --- |
| 文本发送 | OneBot 标准 `text` 消息段 | `SendItem(type=text)` 映射 |
| 图片 / 本地表情包发送 | OneBot 标准 `image` 消息段；NapCat 支持本地路径或文件引用，具体格式需实测 | QQ adapter 映射 |
| QQ 原生表情 | OneBot 标准 `face` 消息段 | QQ adapter 可选映射 |
| QQ 商城 / 收藏表情 | NapCat 支持 `mface` 消息段；接收时可作为表情包识别和偷取候选 | QQ adapter + 表情包系统 |
| 引用回复 | OneBot 标准 `reply` 消息段，使用被回复消息的 `id` | QQ adapter 增强能力，优先级最高 |
| 撤回自己消息 | OneBot / NapCat 支持 `delete_msg` API | QQ adapter 后置能力，需要保存已发送 `message_id` |
| 用户戳一戳 / 拍一拍 | OneBot notice / NapCat 相关事件可映射到内部 `NUDGE` | QQ adapter 事件映射 |
| 机器人主动戳一戳 | NapCat 存在 `friend_poke`、`group_poke`、`send_poke` 等扩展 API | QQ adapter 后置能力，不进入主协议 |
| 对某条消息点表情回应 | NapCat 存在 `set_msg_emoji_like` 扩展 API，不属于 OneBot v11 标准核心能力 | QQ adapter 后置增强能力 |
| 多条连续回复 | 当前项目已由 `items[]` 支持；QQ adapter 按 item 顺序调用发送 | 核心协议已支持，平台层执行 |

设计原则：

- OneBot / NapCat 能力是 QQ 接入层扩展能力的上限；WebUI 阶段暂不实现这些平台动作。
- 核心聊天链路保持平台无关，只输出 `ActionDecision -> SendItem[]`。
- QQ adapter 只做平台事件转换、消息段转换、API 调用和消息 ID 记录，不重新决定回复内容。
- 引用回复、撤回、点表情回应、主动戳一戳等动作需要依赖 OneBot `message_id`，因此必须在 raw log / conversation event / 发送记录中保留平台原始 ID。
- 对用户发来的 `image`、`face`、`mface` 等消息，进入内部上下文时应转换为轻量占位或表情包候选，不把完整 OneBot JSON 塞进 LLM prompt。
- `search_meme` 仍是内部检索项，任何情况下不能直接发送到 OneBot。
- 如果 OneBot / QQ 对单条消息长度、文件大小、格式或频率有限制，由 QQ adapter 做平台保护性降级，并写入日志；不反向污染核心产品协议。

优先级建议：

1. **引用回复**：最能提升 QQ 多消息场景的自然感。用户连续发多条时，adapter 可以引用其中一条精准回应。
2. **表情包接收与复用**：识别用户发来的 `image` / `mface`，下载到本地待处理区，进入表情包系统。
3. **戳一戳 / 拍一拍映射**：用户触发后转成内部 `NUDGE`，强制刷新注意力和进入热聊在场状态。
4. **撤回自己消息**：需要保存机器人发送消息的 OneBot `message_id`，后置实现。
5. **对消息点表情回应**：依赖 NapCat 扩展 `set_msg_emoji_like`，后置实现，不作为 MVP 主链路。

参考资料：

- [OneBot v11 协议规范](https://11.onebot.dev)
- [OneBot v11 消息段](https://github.com/botuniverse/onebot-11/blob/master/message/segment.md)
- [OneBot v11 公开 API](https://github.com/botuniverse/onebot-11/blob/master/api/public.md)
- [OneBot v11 通知事件](https://github.com/botuniverse/onebot-11/blob/master/event/notice.md)
- [NapCat API 兼容列表](https://napneko.github.io/develop/api)
- [NapCat 消息段说明](https://napneko.github.io/onebot/segment)
- [NapCat API 文档](https://napneko.github.io/develop/api/doc)

当前阶段不追求一次性完成 OneBot 完整适配，先按可逐步实现的基础架构推进。第一阶段优先 QQ 私聊；微信暂缓。

## 1. 当前结论

### 1.1 第一阶段优先 QQ

第一阶段选择 QQ 私聊作为主要落地方向。

推荐路线：

- NapCatQQ
- OneBot v11
- reverse WebSocket
- 本地 FastAPI adapter

理由：

- QQ 私聊最接近情感陪伴机器人的一对一使用场景。
- NapCat / OneBot 能覆盖文本、图片、表情包、戳一戳、撤回等关键能力。
- QQ 官方 Bot API 的表情包、戳一戳、撤回能力不足，不适合作为当前核心方案。

### 1.2 微信暂缓

微信接入暂不进入 MVP。

原因：

- 官方路线能力不足，无法稳定支持表情包、拍一拍、撤回等关键交互。
- Hook / DLL 注入路线风险过高，不适合当前阶段作为主线推进。
- 微信接入会引入新的平台协议、风控和部署复杂度，容易拖慢 QQ 闭环。

后续如果必须推进微信，应新建独立文档重新调研，不复用 QQ 方案。

## 2. 当前已废弃的旧假设

以下内容曾在旧调研中出现，但已经不再作为开发依据。

### 2.1 不再使用 `action + text` 作为主协议

当前底层协议已经改为：

```text
action + items
```

其中 `items[]` 表示本轮按顺序发送的文本、emoji、表情包等可见发送单元。

旧 `text` 只作为兼容入口，内部应统一归一化为 typed send items。

### 2.2 戳一戳不再叫 `POKE`

内部事件统一使用：

```text
EventType.NUDGE
```

QQ / 微信平台上的戳一戳、拍一拍类动作都应该转换为 `NUDGE`，表示用户强制刷新注意力。

### 2.3 当前不做每会话运行时隔离

当前产品假设是每个用户本地部署、私聊使用。

因此 MVP 阶段保持：

- 单个 `EventGate`
- 单个 `CompanionGraph`
- 单个 HOT/COLD 状态机
- 单个主动消息状态机
- `session_id = default` 仅作为日志兼容字段

QQ Adapter 不创建或查找独立运行时。

未来如果进入群聊、多用户托管、多人共享同一后端实例，再重新讨论状态机隔离。

### 2.4 WebUI 不只是验证层

WebUI 不是一次性验证工具。

它在后续可能继续承担：

- 配置入口
- 调试入口
- 本地可视化状态面板
- QQ 接入前后的验证入口

因此 QQ 接入设计不应假设 WebUI 会被完全替换。

## 3. USER_COMPOSING：用户正在输入 / 有话没说完

目标：当用户正在输入下一条消息时，助手不要等到用户第二条消息真正发出后才 stale/drop 上一轮回复，而是在发送前直接进入 pending，避免打断用户还没说完的话。

核心抽象：

```text
USER_COMPOSING
```

含义：用户正在组织输入。该事件不进入聊天 buffer，不参与记忆，不增加 msg_index，只影响发送权。

期望流程：

```text
用户发第一条 -> LLM 开始生成
用户开始输入第二条 -> 标记 composing
LLM 完成 -> 不立刻发送，进入 deferred/pending
用户发出第二条 -> 旧回复 stale/drop，合并 buffer 重新跑
用户停止输入且没有发新消息 -> 释放 deferred 回复，正常发送
```

后端基础设计：

- 在 `EventType` 中增加 `USER_COMPOSING`。
- 在 `EventGate` 增加 composing 状态，例如 `user_composing_until`。
- visible send 前检查 composing 状态；若仍在 composing，则暂缓发送。
- WebUI 可以显式发送 `composing=true/false`，QQ/NapCat 侧可以用 TTL 兜底。
- 该机制应属于基础架构层，不应写死为 WebUI 专用逻辑。

当前实现状态：

- 已新增 `EventType.USER_COMPOSING`。
- 已新增 `POST /api/input-status`。
- 已在 `EventGate` 中加入 `user_composing_until` 和发送前门禁。
- 已在 WebUI 输入框接入 `composing=true/false` 上报。
- 已在右侧状态栏原始会话信息中暴露 `gate.user_composing`。
- 已预留 `app/adapters/onebot11`，可把 NapCat `input_status` notice 转成内部 `USER_COMPOSING` 事件。

待讨论 / 待实现：

- 当前 MVP 采用发送任务内等待，不单独持久化 deferred 回复对象；后续如需要可升级为显式 deferred queue。
- 用户停止输入后已做 snapshot current 检查再释放。
- TTL 初始值：WebUI 暂用 3 秒，QQ/NapCat 预留 8 秒。
- 如果 composing 期间收到 nudge/戳一戳，是否强制打断 composing 并立即进入热聊发送。

## 4. WebUI 输入状态事件

目标：先在 WebUI 阶段验证 `USER_COMPOSING` 基础机制。

初步方案：

- 输入框内容变化时，上报 `composing=true`。
- 点击发送、回车发送、输入框清空、blur 时，上报 `composing=false`。
- 前端只负责上报用户正在输入，不直接控制 LLM 或消息发送。

待讨论 / 待实现：

- 已新增接口：`POST /api/input-status`。
- 已加入 250ms 节流，避免每个键盘事件都立即打后端。
- 已把 composing 状态显示到右侧调试面板的 `会话` 原始 JSON 中。

## 5. NapCat 当前架构调研结论

本地路径：

```text
C:\Users\19533\Desktop\Astrobot\NapCat.Shell
```

当前配置启用的是 OneBot11 反向 WebSocket：

```text
config/onebot11_3665612616.json
```

关键配置：

```json
{
  "url": "ws://127.0.0.1:6199/ws",
  "messagePostFormat": "array",
  "reportSelfMessage": false
}
```

结论：

- NapCat 作为 WebSocket client 连接我们的后端。
- 我们未来需要提供 OneBot11 reverse WebSocket 服务端。
- `messagePostFormat` 当前为 `array`，消息段应按数组格式处理。
- `reportSelfMessage=false`，默认不会回传机器人自己发出的消息。

## 6. NapCat 输入状态事件

调研发现 NapCat 打包文件中存在输入状态 notice：

```text
notice_type = "notify"
sub_type = "input_status"
status_text = "对方正在输入..."
event_type = 1
```

并且存在 `onInputStatusPush`，会把 QQ kernel 的输入状态推送转换成 OneBot notice 事件。

初步适配方向：

```json
{
  "post_type": "notice",
  "notice_type": "notify",
  "sub_type": "input_status",
  "user_id": 123456,
  "event_type": 1,
  "status_text": "对方正在输入..."
}
```

转换为内部事件：

```text
ChatEvent(event_type=USER_COMPOSING, platform=qq, user_id=...)
```

待讨论 / 待实现：

- 确认实际运行时 NapCat 上报字段是否与打包代码一致。
- 明确 `event_type` 的取值含义：开始输入、停止输入、其他状态。
- QQ 输入状态可能没有可靠的“停止输入”事件，因此需要 TTL。
- 先按私聊 C2C 支持设计；群聊是否支持输入状态暂不假设。

## 7. NapCat 设置机器人输入状态

NapCat 中存在扩展 API：

```text
set_input_status
```

该 API 作用是让机器人向用户展示“正在输入”等输入状态，不是检测用户正在输入。

当前判断：

- 这属于“助手存在感 / 正在输入提示”能力。
- 不应和 `USER_COMPOSING` 混在一起。
- 未来可以在 LLM 真正开始生成时调用，让 QQ 侧显示机器人正在输入。

待讨论 / 待实现：

- 是否在 `llm_started` 时调用 `set_input_status`。
- 多消息发送中是否需要维持输入状态。
- 平台是否允许频繁调用，是否需要节流。

## 8. OneBot11 接入基础层

目标：未来将 QQ 接入作为 adapter，而不是改动核心聊天逻辑。

待讨论 / 待实现：

- 新增 `app/adapters/onebot11` 或同级模块。
- 提供 reverse WebSocket server：`ws://127.0.0.1:6199/ws`。
- 解析 OneBot 事件：
  - 私聊文本消息 -> `EventType.TEXT`
  - 图片 / 表情 -> `EventType.IMAGE` / `EventType.STICKER`
  - 戳一戳 -> `EventType.NUDGE`
  - 撤回 -> `EventType.RECALL`
  - 输入状态 -> `EventType.USER_COMPOSING`
- 发送层把内部 `SendItem` 转成 OneBot action：
  - text -> `send_private_msg` / `send_msg`
  - emoji/meme -> 后续结合表情包系统细化
- 先支持私聊，再考虑群聊。

## 9. OneBot 安全与配置

当前 NapCat 配置中带有 token。未来后端服务端需要校验连接来源和 token。

待讨论 / 待实现：

- OneBot reverse WebSocket token 校验。
- 端口配置：默认 `6199`，允许用户在配置层修改。
- 是否把 NapCat 配置写入项目设置页。
- 不在日志或前端状态栏暴露 token。

## 10. OneBot reverse WebSocket 接收入口

目标：让 NapCat 作为 OneBot11 client 连接本项目后端。后端需要成为 reverse WebSocket server，而不是主动去轮询 QQ。

本地调研依据：

- 当前 NapCat 配置使用 `url = ws://127.0.0.1:6199/ws`。
- 当前 NapCat 配置使用 `messagePostFormat = array`。
- NapCat 打包文件中存在 `send_private_msg`、`send_group_msg`、`send_msg`、`mark_private_msg_as_read`、`set_input_status` 等 action。

待调研 / 待实现 TODO：

- [ ] 新增 OneBot reverse WebSocket 服务端入口，例如 `/onebot/ws` 或兼容 NapCat 当前配置的 `/ws`。
- [ ] 建立 `OneBotConnectionManager`，负责连接生命周期、心跳、断线清理和当前连接状态。
- [ ] 支持 OneBot action response 的 `echo` 关联：发送 action 时生成 `echo`，收到 response 后匹配回等待中的任务。
- [ ] 连接时做 token 校验；token 不写入前端调试面板，不写入普通日志。
- [ ] 接收事件时保留原始 OneBot JSON，供调试和后续测评使用。
- [ ] 第一阶段只保证私聊 C2C；群聊、频道、临时会话先保留扩展位。

## 11. OneBot 事件接收映射

目标：把 OneBot/NapCat 的平台事件转换成项目内部 `ChatEvent`，核心聊天逻辑不直接依赖 OneBot 字段。

待调研 / 待实现 TODO：

- [ ] `post_type=message` + `message_type=private` + `message[]` 文本段：转换为 `EventType.TEXT`。
- [ ] `message[]` 中连续多个 `text` 段：合并为同一个用户消息文本；如果中间夹杂图片/表情，则保留段顺序给多模态/占位处理。
- [ ] `message[]` 中 `image` 段：转换为 `EventType.IMAGE`，并保留 `file`、`url`、`file_id`、`summary` 等平台字段。
- [ ] `message[]` 中 `face` 段：转换为 `EventType.STICKER` 或轻量文本占位，例如 `[QQ表情: face:<id>]`。
- [ ] `message[]` 中 `mface` 段：转换为 `EventType.STICKER`，优先进入表情包识别和偷取链路。
- [ ] `message[]` 中 `reply` 段：记录被回复的 OneBot `message_id`，后续用于上下文引用；第一阶段可先转成轻量占位。
- [ ] `message[]` 中 `record`、`file`、`video`：第一阶段只保留原始事件和占位，不进入核心聊天理解。
- [ ] `notice_type=notify` + `sub_type=input_status`：转换为 `EventType.USER_COMPOSING`。
- [ ] `notice_type=notify` + 戳一戳/拍一拍类事件：转换为 `EventType.NUDGE`。
- [ ] `notice_type=friend_recall` / `group_recall`：转换为 `EventType.RECALL`。
- [ ] 每个转换后的内部事件必须带 `platform=qq`，并在 `raw` / 日志中保留 `qq_user_id`、`qq_group_id`、`onebot_message_id` 等可追踪字段。

## 12. 本地单实例状态机与未来隔离预留

这里的状态机指 `EventGate`、HOT/COLD、snapshot、msg_index、USER_COMPOSING、active message 等运行状态，不是新增情绪状态机。

当前产品假设是“每个人部署在自己的本地”，第一阶段也是 QQ 私聊陪伴场景。因此 MVP 不引入按 `session_id` 拆分的 `EventGate` / `CompanionGraph` 运行时隔离。

设计取向：

- WebUI 可以同时承担配置、调试、验证入口，不把它降级为一次性验证层。
- QQ 私聊是主要聊天入口时，仍使用本地唯一状态机；`snapshot.session_id = default` 只作为日志兼容字段，不作为运行时路由键。
- OneBot 的 `user_id`、`group_id`、`message_id` 等平台标识进入 `raw` 和日志，用于审计、撤回、表情包偷取、排错，不驱动多会话调度。
- 如果未来要做群聊、多用户托管、多人共享同一后端实例，再重新讨论状态机隔离。

待调研 / 待实现 TODO：

- [ ] 第一阶段保持单个 `EventGate`、单个 `CompanionGraph`、单个主动消息状态机。
- [ ] QQ Adapter 只做 OneBot 事件和内部 `ChatEvent` 的转换，不创建或查找 per-session runtime。
- [ ] `ConversationEvent.session_id` 和 `RawChatLog.session_id` 暂时固定为 `default`，用于兼容已有表结构。
- [ ] `message_id` 映射、撤回、表情包偷取等能力依赖 OneBot 原始字段，不依赖 `session_id`。
- [ ] 未来如果进入群聊、多用户托管或同一后端连接多个 QQ 账号，再重新设计会话隔离。

## 13. SendItem 到 OneBot 发送映射

目标：让现有 `action + items` 协议无损落到 QQ。OneBot adapter 只负责平台发送，不重新决定回复内容。

核心原则：

- `search_meme` 是内部检索项，绝不能发送到 OneBot。
- `SendItem` 是可见发送单元；多条 `items` 就是多次连续发消息。
- 不设置产品硬上限，但受 OneBot、QQ、网络和文件大小等工程边界限制。

待调研 / 待实现 TODO：

- [ ] `type=text`：发送 OneBot `text` 消息段。
- [ ] `type=emoji`：优先作为普通 Unicode 文本发送；如果未来建立 QQ face 映射表，可转换为 `face` 段。
- [ ] `type=meme`：根据 `meme:<file_stem>` 找到本地表情文件，转换为 OneBot `image` 或适配后的表情段。
- [ ] `type=search_meme`：在 LangGraph / meme resolver 阶段完成解析，进入 adapter 前必须替换为 `meme` 或降级删除。
- [ ] `REPLY.items = text + meme + text` 在 QQ 中默认发送为三条连续消息，而不是压成一条图文混合消息。
- [ ] `REACT.items = meme + emoji` 作为纯表情连续发送，不混入普通文本。
- [ ] 每个 item 发送前继续执行 snapshot stale 检查；用户补消息、撤回、戳一戳、输入状态打断时，剩余 item 停止发送。
- [ ] 幂等键继续使用 `job_id:snapshot_id:send_index`，OneBot action 的 `echo` 可以携带该键。
- [ ] 发送失败只影响当前 item；同轮其他 item 是否继续发送需要按鲁棒性规则决定，不能把技术错误暴露给用户。

## 14. text 分段发送与多消息协议

目标：QQ 侧保留“对方可以连续发多条”的真实聊天感，同时不让 adapter 擅自拆句。

已定原则：

- 多消息由 LLM 在 `items[]` 中显式决定。
- 发送层不按标点、长度或换行自动拆成多条。
- 除非平台硬限制，否则 adapter 不改写 item 数量。

待调研 / 待实现 TODO：

- [ ] `text` item 原样作为一个 QQ 消息发送。
- [ ] 如果 QQ/OneBot 对单条文本长度有限制，再在 adapter 层做平台保护性拆分，并记录为技术降级。
- [ ] 技术性拆分不能改变 `send_index` 的产品语义；需要额外内部 `platform_part_index`。
- [ ] QQ 连续发送需要串行队列，保证 item 顺序，不并发发送。
- [ ] MVP 本地单用户使用单发送队列即可；如果未来支持多用户或群聊，再按目标对象隔离发送队列，避免不同对象互相阻塞。

## 15. 接收用户表情包与偷取到本地

目标：用户在 QQ 发来的表情包不能只被语言描述，应进入“理解 / 识别 / 可能偷取”的表情系统链路。

本地调研依据：

- NapCat 支持 OneBot `face`、`mface`、`image` 消息段。
- NapCat 存在 `downloadMedia`、`downloadRawMsgMedia`、`download_file`、`get_file` 等媒体下载能力。
- NapCat 存在 `fetch_custom_face`、`fetch_custom_face_detail`、`add_custom_face`、`delete_custom_face`、`set_custom_face_desc` 等自定义表情相关 action。

待调研 / 待实现 TODO：

- [ ] 识别 `mface` 为 QQ 商城/收藏表情优先候选，进入 `EventType.STICKER`。
- [ ] 识别 `image` 为普通图片；是否当作表情包交给 LLM/工具判断，不在 adapter 里硬判。
- [ ] 识别 `face` 为 QQ 内置小表情，优先转轻量占位，不进入偷表情链路。
- [ ] 为每个候选媒体建立内部 `MediaRef`：

```text
source=qq
session_id=default
qq_user_id=...
qq_group_id=...
onebot_message_id=...
segment_type=image|mface|face
file_id=...
url=...
local_path=...
hash=...
```

- [ ] 下载用户发来的 `image` / `mface` 到本地临时目录。
- [ ] 使用 dHash / md5 与现有 `image_dhash_index.json` 去重。
- [ ] 新表情先进入表情系统的待处理入口，不直接污染正式可检索表情库。
- [ ] 偷表情保持静默，不主动向用户发“已偷取”。
- [ ] 入库时复用表情包系统设计：多模态生成分类和语义化 `save_name`，最终文件 stem 才能作为 `meme:<file_stem>` 发送。
- [ ] 原始来源只用于调试和去重，不进入对话 prompt，不暴露给用户。

## 16. QQ 发送表情包与本地文件适配

目标：把本地 `meme:<file_stem>` 稳定发到 QQ，同时为动画表情和 QQ 自定义表情预留位置。

待调研 / 待实现 TODO：

- [ ] 从表情系统拿到本地文件绝对路径。
- [ ] Windows 路径转换为 OneBot/NapCat 可接受的文件引用格式，例如 `file:///C:/...` 或 NapCat 支持的本地路径格式。
- [ ] 验证 NapCat 对 png、jpg、gif、webp、apng 的真实发送表现。
- [ ] 如果某些格式 QQ 端不稳定，adapter 层做格式转换或降级。
- [ ] 动态表情是否保持动画，需要单独验证。
- [ ] 是否使用 QQ 自定义表情能力 `add_custom_face` 作为发送前置缓存，后续再讨论；MVP 先走图片消息。
- [ ] 发送失败时记录表情日志，但不向用户解释技术原因。

## 17. 戳一戳、撤回、已读与输入状态

目标：把 QQ 平台特有动作接到现有实时调度层，而不是只当普通文本。

待调研 / 待实现 TODO：

- [ ] 戳一戳 / 拍一拍接收：映射为 `EventType.NUDGE`，用于强制进入热聊在场状态。
- [ ] 是否支持机器人主动戳一戳：NapCat 存在 `friend_poke`、`group_poke`、`send_poke`，但是否纳入产品动作后续讨论。
- [ ] 撤回接收：映射为 `EventType.RECALL`。
- [ ] OneBot `message_id` 需要映射到内部 `event_id`，否则撤回无法定位被撤回内容。
- [ ] 已读能力：NapCat 存在 `mark_private_msg_as_read`、`mark_group_msg_as_read`，后续决定是在收到消息后标记，还是发送回复后标记。
- [ ] 机器人输入状态：`set_input_status` 只表示“机器人正在输入”，与 `USER_COMPOSING` 分离；后续可在 LLM 生成开始时调用。

## 18. 日志、历史回灌与审计

目标：QQ 接入后既保留可审计原始数据，又不污染 LLM 上下文。

待调研 / 待实现 TODO：

- [ ] Raw log 保存完整 OneBot 原始事件 JSON。
- [ ] 单条发送记录保存 `send_key`、`session_id=default`、`onebot_echo`、`onebot_message_id`、`item_type`、`content`。
- [ ] 单条发送记录还应保存 QQ 目标信息，例如 `qq_user_id` / `qq_group_id`，但不把这些平台字段作为核心运行时路由键。
- [ ] `ConversationEvent` 中 QQ 文本按原文回灌给 LLM。
- [ ] 用户发来的 `face`、`mface`、`image` 回灌时使用轻量占位，例如 `[表情: mface]`、`[图片]`，避免把平台 JSON 塞进 prompt。
- [ ] 助手发出的 `meme` 回灌为 `[表情: meme:<file_stem>]`。
- [ ] 表情下载、检索、二轮选择、发送失败要进入表情日志，方便前端状态栏或后续调试查看。
- [ ] 日志中敏感配置、token、本地完整隐私路径要做展示层脱敏。

## 19. 记忆与平台接入边界

目标：QQ 接入不能破坏现有 `MEMORY_CORE.md`、`dm`、`TOMORROW_TOPICS.md` 的简洁结构。

待调研 / 待实现 TODO：

- [ ] 明确 WebUI 与 QQ 是否共享同一个用户身份和记忆文件。
- [ ] 如果未来支持多个 QQ 用户，需要设计记忆命名空间；MVP 先按单用户私聊处理。
- [ ] 记忆分析只读取归一化后的对话事件，不直接读取 OneBot 原始 JSON。
- [ ] 用户发送表情包可以作为“互动风格”被摘要，但不要把表情文件路径写入核心记忆。
- [ ] `/mem` 在 QQ 中是否通过文字命令触发，后续单独讨论。

## 20. 主动消息与 QQ 发送

目标：现有主动消息能力接入 QQ 时仍然走统一发送协议和状态机。

待调研 / 待实现 TODO：

- [ ] 主动消息目标必须绑定具体 QQ 私聊对象，例如 OneBot `user_id` 或 adapter 当前目标；MVP 本地单用户可先只允许一个绑定目标，不能默认发到所有已连接 QQ 对象。
- [ ] 主动消息发送前检查本地唯一状态机的 HOT/COLD、未回复退避、USER_COMPOSING、近期用户消息状态。
- [ ] 主动消息内容进入同一个 `ActionDecision -> SendItem -> OneBotAdapter` 流程。
- [ ] 主动消息是否允许复杂表情混发，后续再单独细化；MVP 可先支持 text item。

## 21. QQ 接入 MVP 边界

目标：防止 OneBot 完整适配一次性膨胀，先打通和现有设计最相关的路径。

第一阶段建议范围：

- [ ] OneBot reverse WebSocket 接入。
- [ ] 私聊文本接收。
- [ ] 私聊文本发送。
- [ ] USER_COMPOSING 接收和 TTL 兜底。
- [ ] 多 `items` 串行发送。
- [ ] `meme:<file_stem>` 作为 QQ 图片发送。
- [ ] 用户 `image` / `mface` 下载到本地待处理区。
- [ ] 戳一戳接收为 `NUDGE`。
- [ ] 撤回接收为 `RECALL`。
- [ ] 明确保持本地单实例状态机，不做每会话隔离。

暂不纳入第一阶段：

- [ ] 群聊完整体验。
- [ ] QQ 自定义表情收藏管理。
- [ ] 机器人主动戳一戳。
- [ ] 语音、视频、文件理解。
- [ ] 多用户记忆命名空间。
- [ ] 完整 OneBot API 管理页。

## 22. QQ 方案选型与部署调研归档

本节保留旧 `社交软件接入设计.md` 中仍有价值的 QQ 调研信息。这里是选型背景和部署参考，不覆盖前文的当前开发规格。

### 22.1 方案对比

| 维度 | NapCatQQ | Hermes Agent / QQ 官方 API | LLOneBot |
|---|---|---|---|
| 接入原理 | 调用 QQ NT Node 模块底层接口 | QQ 开放平台 WebSocket + HTTP REST | 类似 NapCat，注入 Electron |
| 协议 | OneBot v11（ws / reverse-ws / http） | QQ 官方 Bot API v2 | OneBot v11 |
| 文本收发 | 支持 | 支持 | 支持 |
| 图片发送 | 支持（file / url / base64） | 不支持或能力受限 | 支持 |
| 表情包发送 | 支持（mface / image 混合） | 不支持 | 支持 |
| 戳一戳接收 | 支持（notice / notify / poke） | 不支持 | 支持 |
| 戳一戳发送 | 支持扩展 API（`friend_poke`、`group_poke`） | 不支持 | 支持 |
| 消息撤回事件 | 支持（friend_recall / group_recall） | 不支持 | 支持 |
| 群聊支持 | 完整 | 仅 @ 提及 | 完整 |
| 消息历史 | 支持（get_msg、get_group_msg_history） | 不支持 | 支持 |
| 部署复杂度 | 中，需要安装指定版本 QQ NT | 低，纯云端 API | 中 |
| Windows 支持 | 支持 OneKey 一键包 | 支持 | 支持 |
| 风控风险 | 中等，非官方协议 | 低，官方 API | 中等 |
| 审核要求 | 无 | 需要提交审核发布 | 无 |
| 项目活跃度 | 活跃 | 活跃 | 中等 |

当前不采用 QQ 官方 API / Hermes Agent 路线。核心原因是官方 API 不支持表情包发送、戳一戳事件、消息撤回事件。这三项分别对应我们产品中的 `REACT`、强制刷新注意力和撤回静默取消。

LLOneBot 可作为备用方案，但第一阶段不作为主线。

### 22.2 QQ 官方 API 的能力鸿沟

QQ 官方 Bot API 的限制是结构性的，不是暂时的：

- 只能发文本和 Markdown，缺少真实图片 / 表情包发送能力。
- 没有戳一戳事件，官方 WebSocket 事件列表不含 poke / notify。
- 没有撤回事件，无法感知用户撤回。
- 私聊需要沙箱审核，测试阶段只能在沙箱频道收发消息。
- 群聊仅有 @ 提及，不能监听完整群聊上下文。

这些限制会直接阻断 `REACT`、戳一戳强制刷新、撤回静默取消三个核心机制。

### 22.3 NapCat 工作原理

NapCat 不重新实现 QQ 协议，也不向 Electron 注入代码。它直接调用 QQ NT 客户端暴露给内部 Node.js 模块的接口。

```text
QQ 用户操作 -> QQ NT 客户端
                    |
         +----------+----------+
         |          |          |
    Electron     Node 模块   渲染层
    UI 壳        核心逻辑    Chromium
                    |
              NapCat 在此层调用
              QQ 自身 Node 模块接口
```

这个机制和早期 Mirai / Android 协议路线不同，也和注入 Electron 的 Hook 方案不同。它的优势是能复用 QQ 客户端已经实现的能力，不需要自己维护底层 QQ 协议。

### 22.4 Reverse WebSocket 选择理由

OneBot v11 常见连接方式：

| 方式 | 方向 | 适用场景 |
|---|---|---|
| HTTP | 我们 -> NapCat | 简单 API 调用，但没有事件推送 |
| 正向 WebSocket | 我们 -> NapCat | NapCat 作为 WebSocket Server |
| 反向 WebSocket | NapCat -> 我们 | 我们作为 WebSocket Server，NapCat 主动连接 |
| HTTP POST | NapCat -> 我们 | 只做事件上报 |

当前选择 reverse WebSocket：

- FastAPI 已经是 HTTP Server，加一个 WebSocket endpoint 即可。
- NapCat 主动连接我们，重启或断连后可自动重连。
- 不需要为 NapCat 暴露额外公网端口。
- 接收事件和调用 API 可以复用同一条 WebSocket 连接。

旧调研中建议的 NapCat WebUI 配置：

```text
连接方式：反向 WebSocket
连接地址：ws://127.0.0.1:8080/qq/ws
Token：（可选，用于验证连接来源）
```

当前本机调研到的实际配置是 `ws://127.0.0.1:6199/ws`，以后实现时应优先兼容当前本机配置，并允许配置层修改端口和路径。

### 22.5 WebSocket 双向通道

Reverse WS 模式下，NapCat 作为客户端连接我们的 FastAPI。整条 QQ 通信可以只依赖这一条 WebSocket：

- 读方向：NapCat -> 我们，持续推送 OneBot JSON 事件。
- 写方向：我们 -> NapCat，通过同一条连接发送 OneBot action。
- API response 通过 `echo` 关联回等待中的请求。

示例 action：

```json
{
  "action": "send_private_msg",
  "params": {
    "user_id": 123456789,
    "message": [
      { "type": "text", "data": { "text": "来了" } }
    ]
  },
  "echo": "unique_request_id"
}
```

旧调研认为第一阶段不需要消息队列：

- 接收和发送走同一条 TCP 连接，连接模型简单。
- `EventGate` 已经做 single-flight 控制。
- 所有通信发生在 localhost，网络延迟可忽略。

如果未来同一后端连接多个 QQ 账号、群聊或多用户托管，再重新评估队列和运行时隔离。

### 22.6 OneBot v11 消息段格式

OneBot v11 的消息以消息段数组表示，每个段是一个 JSON 对象。

纯文本：

```json
{ "type": "text", "data": { "text": "来了。怎么了？" } }
```

图片：

```json
{ "type": "image", "data": { "file": "file:///C:/companion_bot/memes/assets/helpless/example.webp" } }
```

QQ 内置表情：

```json
{ "type": "face", "data": { "id": "178" } }
```

发送一条 OneBot 消息时可以包含多个段，但当前产品协议更倾向于把 `items[]` 映射为多条连续消息，而不是压成一条图文混合消息。除非后续产品重新决定，否则 `REPLY.items = text + meme + text` 默认发送为三条 QQ 消息。

### 22.7 Windows 部署参考

环境要求：

| 组件 | 要求 |
|---|---|
| 操作系统 | Windows 11 x64 |
| QQ NT | >= 9.9.26-44343，旧调研推荐 >= 40768 |
| VC++ 运行库 | Visual C++ Redistributable 2015-2022 x64 |
| Node.js | NapCat OneKey 包已内置，无需单独安装 |
| Python | 3.11+ |
| 磁盘 | 约 2GB（QQ + NapCat + 表情包素材） |

安装步骤参考：

1. 从 NapCatQQ Releases 下载 `NapCat.Shell.Windows.OneKey.zip`。
2. 解压到不含中文 / 空格的目录，例如 `C:\NapCat\`。
3. 双击 `launcher.bat` 启动。
4. 控制台输出 WebUI 密钥。
5. 浏览器打开 `http://localhost:6099`，输入密钥。
6. 扫码登录 QQ。
7. 在 NapCat WebUI 中添加反向 WebSocket 服务，指向本项目后端。

建议启动顺序：

```text
1. 启动 QQ NT（或由 NapCat launcher.bat 自动拉起）
2. 启动 NapCat，扫码登录
3. 确认 NapCat WebUI 的 WebSocket 连接状态
4. 启动本项目 FastAPI 服务
5. NapCat 通过 reverse WS 连接后端
```

本地拓扑：

```text
QQ NT -> NapCat Shell -> reverse WS -> FastAPI / OneBot adapter -> EventGate
```

### 22.8 QQ 风险与降级方案

NapCat 核心风险：

| 风险 | 说明 | 缓解措施 |
|---|---|---|
| 账号风控 / 封号 | 非官方协议可能触发风控 | 使用老号；避免高频操作；不做群发广告 |
| QQ 版本升级破坏 | QQ NT 更新可能改变内部模块接口 | 锁定 QQ 版本；关注 NapCat 发版 |
| 项目停更 | 上游不可控 | 准备 LLOneBot / Lagrange 备用 |
| Windows 稳定性 | QQ 在某些 Windows 环境上不稳定 | 使用 Win11，必要时定期重启 |
| WebSocket 断连 | 本地服务重启或连接异常 | reverse WS 重连，增加健康检查 |

风控注意：

- NapCat 使用手机 QQ 扫码登录，本质是真实 QQ 客户端。
- 避免极短时间内发送大量相同消息。
- 新号比老号更容易风控。
- Docker 部署可能无法接收语音和文件，本地 Windows OneKey 包更适合当前阶段。

降级方案：

1. LLOneBot：与 NapCat 同样是 NTQQ -> OneBot 方案，架构相似。
2. Lagrange：C# 实现的 NTQQ 协议客户端，不依赖 QQ 主程序。
3. QQ 官方 API / Hermes Agent：退回到仅文本聊天，会丢失表情包和戳一戳能力。

降级时，鲁棒性规则生效：

- `REACT` 可以降级为 `LIGHT_ACK` / `REPLY` / `WAIT`。
- 戳一戳不可用时，保留普通消息触发和主动消息机制。

### 22.9 旧调研开发路线参考

Phase 1：基础连通，仅私聊。

- 安装 NapCat OneKey 包，完成扫码登录。
- 配置 reverse WebSocket。
- 实现 OneBot 事件解析器，仅处理 `message.private`。
- 实现 `send_private_msg`。
- 验证 QQ 中发送“你好”后收到文本回复。

Phase 2：戳一戳支持。

- 解析 `notice.notify.poke`。
- 第一阶段仅处理好友戳一戳。
- 集成 `EventType.NUDGE` 的强制刷新注意力逻辑。

Phase 3：表情包发送。

- 实现 `meme:<file_stem>` -> OneBot image 消息段。
- 本地图片路径转 `file:///` URL 或 NapCat 支持的格式。
- `emoji:<code>` 可选映射为 OneBot face。

Phase 4：表情包接收 + 偷表情。

- 解析私聊图片和 `mface`。
- 下载 / 缓存接收到的图片。
- 接入表情包系统的判断、命名、入库链路。
- 解析好友撤回事件。

Phase 5：完善与压测。

- WebSocket 断连重连稳定性。
- 长对话连续性。
- 表情包发送成功率。
- 短时间大量消息、异常断连恢复。

第二阶段延后：

- 群聊消息事件解析。
- 群聊 @ 提及触发。
- 群戳一戳事件。
- 群撤回事件。

### 22.10 OneBot API 速查

| API | 用途 |
|---|---|
| `send_private_msg` | 发送私聊消息 |
| `send_group_msg` | 发送群聊消息 |
| `send_msg` | 根据上下文自动选择私聊 / 群聊 |
| `delete_msg` | 撤回机器人自己发的消息 |
| `get_msg` | 获取历史消息详情 |
| `friend_poke` | 主动戳好友（扩展 API） |
| `group_poke` | 主动戳群成员（扩展 API） |
| `get_image` | 下载图片文件 |
| `get_login_info` | 获取机器人 QQ 号 |
| `get_version_info` | 获取 NapCat 版本 |
| `set_restart` | NapCat 不支持 |

参考资料：

- [NapCatQQ GitHub](https://github.com/NapNeko/NapCatQQ)
- [NapCat 官方文档](https://doc.napneko.icu)
- [NapCat OneBot 事件系统](https://doc.napneko.icu/onebot/event)
- [NapCat API 兼容列表](https://napneko.github.io/develop/api)
- [OneBot v11 协议规范](https://11.onebot.dev)
- [Hermes Agent QQ Bot 文档](https://www.majiabin.com/hermes/user-guide/messaging/qqbot/)

## 23. 微信接入调研归档

本节保留旧微信调研中的非重复信息。当前结论仍然是：微信接入暂缓，不进入 MVP。

### 23.1 路线对比

| 路线 | 代表方案 | 风险 | 拍一拍 | 表情包 | 撤回 |
|---|---|---|---|---|---|
| 官方 iLink Bot API | Hermes Agent 原生 / OpenClaw-weixin | 低 | 不支持 | 不支持 | 不支持 |
| Hook 注入 | WeChatFerry | 极高 | 支持 | 支持 | 支持 |

微信的封闭程度远高于 QQ。QQ 有 NapCat 这种调用 NT Node 模块的中间路线，微信生态里暂时没有同时满足“风险可控”和“能力完整”的中间方案。

### 23.2 官方 iLink Bot API

从旧调研记录看，Hermes Agent v0.9.0 和 OpenClaw 通过腾讯官方插件 `@tencent-weixin/openclaw-weixin` 支持微信个人号接入，底层是腾讯 iLink Bot API。

接入方式：

```text
Hermes Agent: hermes gateway setup -> 选择 Weixin -> 扫码 -> 完成
OpenClaw: openclaw plugins install "@tencent-weixin/openclaw-weixin" -> 扫码 -> 完成
```

能力边界：

| 能力 | 支持 | 说明 |
|---|---|---|
| 文本收发 | 支持 | 可自动分片，旧调研记录限制为 4000 字符 |
| 图片收发 | 支持 | AES-128-ECB 加密的 CDN 上传 / 下载 |
| 语音接收 | 支持 | 微信内置 ASR 转文字 |
| 语音发送 | 不支持 | iLink 未开放语音发送接口 |
| 视频接收 | 支持 | 下载为 mp4 |
| 文件收发 | 支持 | 支持任意格式 |
| 表情包 / 贴纸 | 不支持 | 收到表情包时可能静默忽略 |
| 拍一拍 | 不支持 | 不在 iLink 支持的消息类型中 |
| 撤回 | 不支持 | 撤回通知存在但 adapter 不处理；已发消息无法撤回 |
| Markdown | 不支持 | 微信客户端不渲染，adapter 自动剥离格式 |
| 群聊 | 部分支持 | 群聊消息可接收，但 @ 提及逻辑不完整 |
| 朋友圈 | 不支持 | iLink 不支持 |

结论：iLink 适合只需要文本 + 图片对话的场景，不适合当前产品。它和 QQ 官方 API 一样，无法支撑 `REACT`、拍一拍 / 戳一戳强制刷新、撤回静默取消。

### 23.3 WeChatFerry 路线

WeChatFerry 是能力最完整但风险最高的路线。

它和 QQ + NapCat 的关键区别：

- QQ NapCat 调用 QQ NT 内部 Node 模块，不修改 QQ 进程本身。
- WeChatFerry 直接向微信进程注入 DLL，读写微信内存空间。
- 这类行为可能被 Windows Defender 或微信自身检测。
- 旧调研记录的社区反馈封号风险非常高。

架构差异：

| 维度 | QQ（NapCat + OneBot） | 微信（WeChatFerry） |
|---|---|---|
| 通信方式 | 单一 WebSocket 双向通道 | NanoRPC 调用 + HTTP POST 回调 |
| 连接方向 | NapCat -> 我们（reverse WS） | 我们 -> WCF 主动连接，WCF -> 我们回调 |
| 事件格式 | OneBot v11 JSON | 自定义 WcfMsg JSON |
| 消息段模型 | 消息段数组（text / image / face 等） | 单 type + content / xml |
| 平台适配复杂度 | 中等，OneBot 有规范 | 较高，需要理解 WCF 专有格式和 XML |
| 表情包 | image 或 mface 消息段 | 统一表现为 image，难区分表情和普通图片 |

拍一拍接收：

- WCF 将拍一拍作为消息类型 `922746929` 上报。
- 详情在 XML 中，例如 `fromusername`、`pattedusername`、`template`。
- 只有 `pattedusername` 是机器人自己时才转换为内部 `EventType.NUDGE`。

拍一拍发送：

```python
wcf.send_pat_msg(roomid="群ID", wxid="要拍的人的wxid")
```

MVP 阶段和 QQ 一样，不使用主动拍一拍能力。

表情包发送：

```python
wcf.send_image(path="C:/companion_bot/memes/assets/helpless/example.webp")
wcf.send_gif(path="C:/companion_bot/memes/assets/smiling/example.gif")
```

微信没有 OneBot 那样的 `mface` 消息段类型。表情包以图片消息形式发送，这意味着收到微信表情时，它可能只表现为普通 `IMAGE` 事件，偷表情策略需要额外判断。

消息撤回：

```python
wcf.revoke_msg(msg_id=123456)
```

收到撤回事件同样需要通过 WCF 消息类型判断，具体 type ID 取决于 WCF 版本。

### 23.4 微信风险与建议

风险因素：

| 风险因素 | 说明 |
|---|---|
| DLL 注入检测 | 微信反作弊系统可检测非授权模块注入 |
| 版本不匹配 | WCF 必须匹配特定微信版本，自动更新可能导致失效 |
| 异常行为模式 | 高频消息、非常规操作会触发风控 |
| 同一设备登录 | WCF 必须和微信客户端在同一 Windows 机器运行 |

如果未来必须使用 WeChatFerry：

- 使用微信小号，绝不用主号。
- 锁定微信版本，禁止自动更新。
- 消息频率控制在 5 条 / 分钟以内。
- 不做群发。
- 定期检查 WCF 版本更新。
- 做好随时失效或封号的准备。

当前建议优先级：

1. 先完成 QQ 接入，跑通完整陪伴闭环。
2. 微信接入作为长期探索项，不阻塞 MVP。
3. 如果未来微信必须接入，用小号 + WeChatFerry 方案单独开设计文档。

参考资料：

- [WeChatFerry GitHub](https://github.com/lich0821/WeChatFerry)
- [WeChatFerry 消息类型文档](https://github.com/lich0821/WeChatFerry/blob/master/README.MD)
- [企业微信开放平台](https://work.weixin.qq.com/api/doc)
- [微信机器人方案对比参考](https://blog.csdn.net/weixin_38861218/article/details/146997227)

## 24. 文档维护规则

- 本文是社交软件接入与 QQ/NapCat/OneBot 的统一设计入口。
- `QQ对接设计.md` 只保留跳转说明，避免重复维护两份 QQ 规格。
- 微信重新启动前，新建独立调研文档，不在本文继续扩写。
- 如果某项旧调研内容没有迁移到当前规格文档，就不应作为开发依据。
