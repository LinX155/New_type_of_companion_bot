# 社交软件接入调研归档

本文档保留 QQ / 微信 / OneBot / NapCat 接入的调研结论、平台能力差异和历史方案背景。当前可执行工程项归入 `开发计划.md`；核心运行时职责以 `技术架构.md` 为准。

这里的内容不作为主产品协议事实源。如果和 `产品设计.md`、`技术架构.md` 或 `开发计划.md` 冲突，以后者为准。

## 1. 当前结论

### 1.1 第一阶段优先 QQ + NapCat

当前社交软件接入主线：

```text
QQ NT -> NapCat -> OneBot v11 reverse WebSocket -> FastAPI adapter -> EventGate -> CompanionGraph
```

选择理由：

- QQ 私聊最接近当前情感陪伴机器人的一对一使用场景。
- NapCat / OneBot 覆盖文本、图片、表情包、引用、输入状态、撤回、戳一戳等关键能力。
- QQ 官方 Bot API 的表情包、戳一戳、撤回能力不足，不适合作为当前核心方案。
- reverse WebSocket 适合本地部署：NapCat 主动连后端，读事件和写 action 可复用同一条连接。

当前 QQ 私聊主链路已接通；未完成项以 `开发计划.md` 为准。

### 1.2 微信暂缓

微信接入不进入当前阶段。

原因：

- 官方路线能力不足，无法稳定支持表情包、拍一拍、撤回等关键交互。
- Hook / DLL 注入路线风险过高，不适合当前阶段作为主线推进。
- 微信会引入新的平台协议、风控和部署复杂度，容易拖慢 QQ 闭环。

后续如果必须推进微信，应新建独立文档重新调研，不复用 QQ 方案。

## 2. 官方能力核对

官方文档核对日期：2026-06-21。

### 2.1 网络连接

NapCat 官方基础配置说明中，WebSocket 客户端即反向 WS，是“既能主动推送事件也能接收请求”的双工模型。配置项包括：

- `url`：上报地址。
- `messagePostFormat`：消息上报格式，支持 `string` / `array`。
- `reportSelfMessage`：是否上报自身消息。
- `token`：鉴权密钥。
- `heartInterval` / `reconnectInterval`：心跳和重连。

因此第一阶段应实现 OneBot reverse WebSocket 服务端，而不是让项目去轮询 NapCat。

参考：[NapCat WebUI 基础配置](https://napneko.github.io/config/basic)。

### 2.2 API 能力

NapCat 官方 API 兼容列表显示以下能力可用：

| 能力 | API | 阶段归属 |
| --- | --- | --- |
| 私聊发送 | `send_private_msg` / `send_msg` | 第一阶段 |
| 撤回机器人消息 | `delete_msg` | 第三阶段 |
| 获取消息详情 | `get_msg` | 媒体 fallback 和引用回复已接入 |
| 获取图片 | `get_image` | 已接入 |
| 获取登录号信息 | `get_login_info` | 第一阶段调试 |
| 设置输入状态 | `set_input_status` | 已接入 |
| 标记私聊已读 | `mark_private_msg_as_read` | 第三阶段 |
| 收藏表情相关 | `fetch_custom_face` 等 | 第二阶段调研，不作为第一版依赖 |
| 戳一戳 | `friend_poke` / `group_poke` / `send_poke` | 第三阶段 |
| 消息表情回应 | `set_msg_emoji_like` | 第三阶段 |

参考：[NapCat 请求接口兼容情况](https://napneko.github.io/develop/api)。

### 2.3 事件能力

NapCat 官方事件兼容列表显示以下事件可用：

| 能力 | 事件 | 阶段归属 |
| --- | --- | --- |
| 私聊消息 | `message.private` | 第一阶段 |
| 群聊消息 | `message.group` | 后置 |
| 私聊消息撤回 | `notice.friend_recall` | 第三阶段 |
| 群聊消息撤回 | `notice.group_recall` | 后置 |
| 戳一戳 / 拍一拍接收 | `notice.notify.poke` | 已接入 |
| 用户输入状态 | `notice.notify.input_status` | 第一阶段 |
| 消息表情回应 | `notice.group_msg_emoji_like` | 后置 |

参考：[NapCat 上报事件兼容情况](https://napneko.github.io/develop/event)。

### 2.4 消息段能力

NapCat 官方消息格式列表显示：

- `text`：收发均支持。
- `image`：收发均支持。
- `face`：QQ 内置表情，收发均支持。
- `reply`：引用回复，收发均支持。
- `mface`：QQ 表情包，收发均支持；官方说明收到时可能以 `image` 消息段上报，并通过子类型区分。
- `poke`：群聊戳一戳可收发，但官方说明事件上报和接口调用不通过普通消息。

OneBot v11 标准图片段也支持用 `file:///...` 绝对路径、网络 URL 或 base64 发送图片，因此第一阶段本地 meme 发送应优先验证 `image.file = file:///...`。

参考：[NapCat 消息格式兼容情况](https://napneko.github.io/develop/msg)，[OneBot v11 消息段规范](https://github.com/botuniverse/onebot-11/blob/master/message/segment.md)。

### 2.5 文件处理

NapCat 从 v4.8.115 起提供 Stream API，官方推荐用于大文件传输和跨设备部署。第一阶段本地私聊图片 / 表情发送使用普通 OneBot 图片能力；第二阶段偷表情如果遇到大文件、跨设备部署或普通 API 不稳定，再补 Stream API。

参考：[NapCat 文件处理框架指南](https://napneko.github.io/develop/file)。

## 3. 本地 `.research` 调研结论

本地资料目录实际为 `.research`。

### 3.1 NapCat.Shell

本地配置文件：

```text
.research/NapCat.Shell/config/onebot11_3665612616.json
```

关键信息：

```json
{
  "websocketClients": [
    {
      "enable": true,
      "name": "情感陪伴",
      "url": "ws://127.0.0.1:6199/ws",
      "reportSelfMessage": false,
      "messagePostFormat": "array",
      "heartInterval": 30000,
      "reconnectInterval": 30000
    }
  ]
}
```

结论：

- 当前本机 NapCat 已按 reverse WebSocket client 思路配置。
- `messagePostFormat=array`，adapter 应以消息段数组为主。
- `reportSelfMessage=false`，默认不会把机器人自己发出的消息再作为普通消息回传。
- 配置中有 token，文档和日志中不得明文记录。
- `6199/ws` 仅作为早期 raw event 抓包配置保留在本地资料中；正式接入不使用该端口。
- 当前正式 OneBot reverse WebSocket 地址固定为 `ws://127.0.0.1:8000/onebot/ws`。NapCat WebUI 中应把反向 WS 地址改到该 endpoint。

### 3.2 当前项目状态

当前已存在：

```text
app/adapters/onebot11/events.py
app/adapters/onebot11/client.py
app/api/routes.py:/onebot/ws
```

能力：

- OneBot reverse WebSocket 服务端。
- 连接状态、token 校验、心跳元信息。
- 同一条 WS 上发送 OneBot action，并通过 `echo` 关联 response。
- 私聊文本消息映射为内部 `EventType.TEXT`。
- QQ 私聊按 `qq_private_<user_id>` 自动创建并隔离 session；不同 QQ 用户拥有独立主对话运行态、历史、记忆和调试日志。
- QQ `image` / `mface` / `face` 消息段映射为轻量占位和 media refs。
- QQ `/mem` / `/forget` 指令分流，不进入主聊天 harness。
- 本地 meme 到 OneBot `image` 消息段的发送层。
- QQ 输入状态 `notice.notify.input_status` 转内部 `USER_COMPOSING`，带 TTL 兜底。
- QQ 拍一拍 / 戳一戳转内部 `EventType.NUDGE`。
- QQ 图片理解、静默偷表情和索引同步走后台媒体队列。
- QQ 引用回复：接收侧把 `reply` 消息段还原为轻量引用上下文；发送侧可前置 OneBot `reply` 段，图片理解异步补发会引用原图片消息。
- 主动消息可按 `qq_private_<user_id>` 绑定目标发送，默认仍由配置控制是否启用。
- 平台路由保护：只有本轮最新输入来自 QQ 时才回发 QQ。

运行侧补充：

- WebUI 通过 `/ws` 收到 `conversation_changed` 后自动刷新 `/api/conversation`，QQ 新消息不需要手动刷新页面。
- WebUI 对话框展示 NapCat 连接状态灯，状态来自 `/api/status.onebot.connected`。
- WebUI 对话栏右上角提供 session 下拉选择，默认只展示当前选中 session；清空对话和 raw debug 查询也默认只作用于当前 session。
- `start.bat` 启动前会清理本项目残留的 8000 端口后端进程，避免重复启动时端口占用。

### 3.3 表情包参考项目

`.research/astrbot_plugin_angel_smile` 的可复用结论：

- 偷表情工具以图片引用为输入，可支持本地路径、`file:///` 和 `http(s)` URL。
- 入库前做 dHash 近似去重，重复时跳过保存。
- 保存结果是工具内部结果，不需要主动打断用户。

当前项目已经有自己的表情包系统和 `image_dhash_index.json` 派生缓存，因此 QQ 第二阶段不需要照搬第三方插件，只需要复用这些设计思路：

- 平台 adapter 负责下载媒体到临时目录。
- 表情系统负责判断、命名、去重、入库和索引同步。
- 聊天侧只看到轻量占位，不直接看到平台 JSON 或文件细节。

## 4. Adapter 边界

QQ 接入层负责把内部 `SendItem[]` 和运行时上下文翻译成 OneBot / NapCat 可执行动作。不要为了 QQ 平台能力，在核心协议中新增 `QUOTE_REPLY`、`RECALL_AND_RESEND`、`REACTION_TO_MESSAGE` 等平台化 action。

核心原则：

- 主 harness 保持平台无关；主 LLM 输出轻量文本协议，核心层归一化成内部 `ActionDecision -> SendItem[]`。
- QQ adapter 只做平台事件转换、消息段转换、API 调用和消息 ID 记录，不重新决定回复内容。
- `search_meme` 是内部检索项，任何情况下不能直接发送到 OneBot。
- 用户发来的 `image`、`face`、`mface` 等消息进入内部上下文时应转换为轻量占位或表情包候选，不把完整 OneBot JSON 塞进 LLM prompt。
- 如果 OneBot / QQ 对单条消息长度、文件大小、格式或频率有限制，由 QQ adapter 做平台保护性降级，并写入日志；不反向污染核心产品协议。
- `message_id` 是 QQ 平台能力的关键元信息，引用回复、撤回、主动消息发送审计都依赖它。

## 5. 当前边界

已接入：

- OneBot reverse WebSocket、私聊文本收发、多气泡串行发送、本地 meme 图片发送。
- `/mem` / `/forget` 分流、输入状态检测、机器人输入状态展示、拍一拍接收。
- QQ 图片理解、静默偷表情、媒体下载 fallback、引用回复、全局表情索引同步。
- 多 session 隔离和按 QQ 私聊目标发送主动消息。

待补：

- 撤回接收。

后置：

- 已读回执、群聊、QQ 表情回应、主动戳一戳、语音 / 视频 / 文件理解。

## 7. 风险与降级

| 风险 | 说明 | 降级 |
| --- | --- | --- |
| 账号风控 | NapCat 是非官方 QQ 接入方式 | 使用低风险账号；避免高频、群发、重复内容 |
| QQ / NapCat 版本变更 | 上游字段或内部接口可能变化 | 保留 raw log；adapter 做字段兼容 |
| 本地图片发送失败 | Windows 路径、file URI、格式支持需要实测 | 尝试 file URI、绝对路径、base64；失败则删表情 item |
| 输入状态无停止事件 | QQ 可能只上报开始输入 | TTL 兜底 |
| `mface` 字段不稳定 | 官方说明可能以 image 上报并用子类型区分 | 同时兼容 `mface` 段和 image 子类型 |

## 8. 参考资料

- [NapCat WebUI 基础配置](https://napneko.github.io/config/basic)
- [NapCat 请求接口兼容情况](https://napneko.github.io/develop/api)
- [NapCat 上报事件兼容情况](https://napneko.github.io/develop/event)
- [NapCat 消息格式兼容情况](https://napneko.github.io/develop/msg)
- [NapCat 文件处理框架指南](https://napneko.github.io/develop/file)
- [OneBot v11 协议规范](https://11.onebot.dev)
- [OneBot v11 消息段规范](https://github.com/botuniverse/onebot-11/blob/master/message/segment.md)
