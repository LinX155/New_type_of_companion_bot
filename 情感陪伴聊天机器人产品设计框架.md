# 情感陪伴聊天机器人设计框架

## 0. 文档状态

本文档是当前头脑风暴阶段的共识版设计指南。

核心准则：**我们已经讨论形成共识的细节必须清晰落盘，不得遗漏。** 暂缓选型和需要调研参考项目的内容统一放到 `开发计划.md` 的 TODO 中，不再和当前设计共识混写。已经形成共识的内容吸收到正式章节；未采纳内容不继续保留在主设计文档中。

当前真正成型的模块：

- 回复冲动抑制：同一个 LLM 输出 `action + items`，转发层只把可见发送单元转发给用户。
- 实时事件门 / 发送权层：只处理发送一致性、版本有效性、撤回、命令分流和主动消息撞车，不替代 LLM 判断是否回复。
- 存在感系统：不在微信/QQ 里直接发状态消息，而是通过行为表现。
- 强制看手机：用“拍一拍 / 戳一戳”触发即时关注。
- 记忆系统：`MEMORY_CORE.md + dm/YYYY-MM-DD.md` 的两层 Markdown 方案。
- 原始日志：保留机器友好的 raw chat log，用于存档、审计、测评和回放，不作为记忆整理输入。
- 记忆维护：`/mem` 和 `/forget` 作为显式身份切换入口。
- 角色灵魂：由 `SOUL.md` 承载稳定偏好、边界和说话习惯。
- 主动消息：以 `TOMORROW_TOPICS.md` 维护明日话题，主要来自未闭合话题和昨日记忆；生活感消息备选默认不自动维护。
- 轻交互：`REACT` 必须存在，指向表情包系统；表情系统选型见 `开发计划.md` TODO / Delay。
- 情绪安全与回应边界：当前只做系统提示词级别约束，不设计单独状态机，也不设计独立情绪回复模块。
- 测评：暂不展开。

## 1. 核心问题判断

现有很多“LLM + 接口”的情感陪伴机器人，本质仍然是把助手模型包装成朋友、恋人或虚拟角色。问题不只是 prompt 不够好，而是底层行为倾向不对。

LLM 的训练方向天然偏“助手”：

- 看见用户输入，就倾向于生成一个合格答复。
- 倾向于完整、正确、礼貌、可执行。
- 倾向于服务用户，而不是形成平等关系。
- 很难自然地沉默、等待、走神、暂缓、短句回应。

因此，靠 prompt 约束 LLM “不要像助手”很容易让模型变得畏手畏脚，反而更像被调教过的客服。

我们的方向不是让 LLM 自己学会沉默，而是让系统架构允许：

- 有时不对外发送任何文本。
- 有时等待用户说完。
- 有时批量阅读多条消息后再回复。
- 有时需要用户用社交动作把它叫回来。
- 记忆维护不伪装成自然聊天，而是显式切换到维护模式。

## 2. 总体架构共识

当前推荐架构：

```text
平台事件
  ↓
实时事件门 / 发送权层
  ├─ 命令分流：/mem、/forget
  ├─ buffer / snapshot / 版本有效性
  ├─ 撤回、幂等、主动消息撞车检查
  ↓
启动主回复 LLM
  ├─ COLD：附带冷启动元信息
  └─ HOT：只传普通聊天上下文
  ↓
同一个思考模型输出 JSON：决策 + 可选发送单元
  ↓
实时事件门复核 snapshot 和发送幂等
  ↓
items 为 null 或空：用户侧无可见输出
items 非空：按顺序发送到微信/QQ 聊天界面
```

这不是“多模型复杂架构”，而是“同一次调用，拆分输出通道”。

关键点：

- 决策和文本可以由同一个 LLM 在同一个决策时刻生成。
- 不要求单独做情绪判断模型。
- 不要求把判断模块和回复模块拆成两次调用。
- 思考模型的 CoT 默认隐藏，用户看不到内部判断。
- 用户只看到 `items` 中可见发送单元被转发后的结果。

基础 JSON 采用 `action + items`，`text` 仅作为旧协议兼容入口：

```json
{
  "action": "REPLY",
  "items": [
    {"type": "text", "content": "来了。怎么了？"}
  ],
  "text": null
}
```

字段类型：

- `action`：string，表示本轮输出动作。
- `items`：array 或 null，表示本轮按顺序发送的文本、emoji、表情包或内部表情检索项。
- `text`：string、string[] 或 null，仅用于兼容旧格式；内部会被归一化为 `items`。

`items` 单元类型：

- `text`：普通聊天文本。
- `emoji`：emoji 协议，例如 `emoji:🙂`。
- `meme`：精确表情包资源，例如 `meme:helpless_cat_facepalm`。
- `search_meme`：内部表情检索请求，例如 `search_meme:helpless:tired facepalm`，不会直接发给用户。

当前 action 集：

- `WAIT`：等待用户继续说，`items:null`。
- `REPLY`：普通文字回复，通常以 `text` 为主；若本轮包含表情包或表情检索，优先使用 `REACT`。
- `LIGHT_ACK`：轻回应，`items` 应短、轻、少，由 prompt 约束，不做硬数量截断。
- `REACT`：表情参与型回应，`items` 可以混合 text、emoji、meme 或 search_meme，但必须至少包含一个 emoji、meme 或 search_meme。
- `ENTER_CHAT`：进入热聊在场态的动作信号，`items` 可为 null 或短回复。
- `END_CHAT`：结束热聊在场态、回到离线生活态的动作信号，通常 `items:null`。

约束：

- `WAIT` 时，`items:null`。
- 混合“文字 + 表情包 + 文字”优先使用 `REACT.items`，由 LLM 在同一决策时刻决定，不由发送层按标点拆分。
- `LIGHT_ACK` 仍应短，不能展开成完整回复。
- `REACT` 必须产生可见表情内容；平台一定需要支持表情包系统。
- 运行时热聊状态由实时事件门维护；状态迁移来源包括平台强触发和 LLM 的 `ENTER_CHAT` / `END_CHAT` 动作信号。
- 不额外设置 `hot_chat`、`status` 等 LLM 输出字段。
- 热聊持续时间、snapshot、幂等字段等工程信息由外部 envelope 维护，不进入当前 JSON。

等待示例：

```json
{
  "action": "WAIT",
  "items": null
}
```

普通回复示例：

```json
{
  "action": "REPLY",
  "items": [
    {"type": "text", "content": "来了。怎么了？"}
  ]
}
```

轻回应示例：

```json
{
  "action": "LIGHT_ACK",
  "items": [
    {"type": "text", "content": "嗯，你说。"}
  ]
}
```

转发规则：

- `items:null` 或空时，不向用户发送任何聊天内容。
- `items` 非空时，按顺序转发其中可见发送单元。
- `search_meme` 是内部检索单元，必须先被替换为 `meme` 或被丢弃，不能直接发给用户。
- `action` 给系统和转发层使用，不直接展示给用户。
- 不把“当前状态”“正在等待”“进入热聊态”等内部信息发成聊天消息。
- 实时 JSON 不负责生成记忆候选；普通聊天记忆由定时记忆分析任务整理进当日日期文件，`/mem` 直接维护核心记忆，凌晨整理线程负责长期化和校验。

## 3. 回复冲动抑制

### 3.1 核心思路

要抑制的不是 LLM 的思考能力，而是它的“可见回复冲动”。

也就是说，LLM 可以在后台判断：

- 用户是否还没说完。
- 当前是否应该等待。
- 是否进入热聊。
- 是否要发短句、完整回复或暂不回复。

但只有 `text` 字段非空时，用户才看到回复。

这解决了一个关键矛盾：

```text
LLM 仍然可以思考和判断
但不会因为每次被调用就必须对用户说话
```

### 3.2 回合闭合判断

热聊中不自然的问题，往往不是内容错，而是插话时机错。系统需要判断用户是否还在连续表达。

可判断为“先等”的信号：

- 用户消息很短，例如“其实”“然后”“我今天真的”“唉”。
- 句尾明显悬空，例如省略号、破折、未完成结构。
- 用户短时间内连续输入。
- 情绪很强，但事件还没说完整。
- 用户刚开始讲一件事，还没有落点。

对应动作：

```json
{
  "action": "WAIT",
  "text": null
}
```

等待不是让 LLM 发一句“我先等你说完”，而是 `action:"WAIT"` 且 `items:null`。

### 3.3 离线生活态

角色不应该 24 小时盯着手机。默认情况下，它可以处于“离线生活态”。

离线生活态的含义：

- 用户消息先进入缓冲。
- 用户普通消息可以立即进入主回复 LLM，但会附带冷启动元信息，由 LLM 决定 `WAIT` 还是可见回应。
- 角色像普通人一样，在某些时刻“看一眼手机”。
- “看一眼手机”不是外部定时硬规则，而是通过 COLD 状态下的 prompt 元信息让 LLM 自己判断是否回应。
- 角色看到的是一段累计上下文，而不是每条都逐条处理。
- 如果 LLM 在 COLD 状态输出 `WAIT`，系统不发送文本、不清空 buffer，并继续保持 COLD。
- 如果 LLM 在 COLD 状态输出可见动作，例如 `REPLY`、`LIGHT_ACK`、`REACT` 或 `ENTER_CHAT`，系统进入 HOT。

COLD 状态下，传给 LLM 的输入可以附带冷启动元信息：

```json
{
  "timestamp": "10:00",
  "last_user_message_age": "24h ago",
  "msg_index": 1,
  "status": "COLD",
  "user_text": "我今天真的很无语"
}
```

字段说明：

- `timestamp`：当前消息时间。
- `last_user_message_age`：距离用户上次发消息的时间。
- `msg_index`：当天用户消息序号，供 LLM 感知这是当天第几条用户消息。
- `status`：当前关系在线状态，此处为 `COLD`。
- `user_text`：当前 buffer 中待处理的用户内容。

`msg_index` 是 LLM 可见的语义提示字段，不是工程幂等字段。它不能替代 `snapshot_id`。

进入 HOT 后，不再传这些冷启动元信息，只传普通聊天上下文。

HOT 回到 COLD 需要外部状态生命周期控制，因为用户不发消息时 LLM 不会被调用。这个生命周期只控制状态，不制造发送延迟。

用户侧表现不应该是状态提示，而应该是自然聊天行为：

```text
用户：我今天真的很无语
用户：老板又发疯了
用户：我都不知道怎么说

过一会儿：
角色：我刚看到。你这几句感觉是真被气到了。
```

### 3.4 热聊在场态

年轻人进入一段聊天后，确实常常有消息就看。因此系统不能只有离线收取，还需要“热聊在场态”。

热聊在场态的含义：

- 收取频率显著提高。
- 用户连续输入时仍然先缓冲。
- 等到用户停顿、语义闭合或明确提问后，再允许 `text` 非空。
- 可以短句、表情、轻回应，但不能逐条抢答。

问题分工：

```text
离线生活态解决：为什么它不是永远在线？
热聊在场态解决：为什么它在线时也不会抢话？
```

### 3.5 实时事件门 / 发送权层

为了让消息缓冲、撤回、主动消息和 `WAIT` 能稳定协同，系统需要一个实时事件门，也可以理解为发送权层。

它不是实时 LLM，不是第二个聊天大脑，也不是人格层。它不负责判断情绪、不负责决定关系、不负责安慰用户、不改写 LLM 文本，也不做人造延迟。

它只管一件事：

```text
这条回复现在还能不能发。
```

基本链路：

```text
平台事件
  ↓
实时事件门
  ↓
主回复 LLM 输出 { action, text }
  ↓
实时事件门检查结果是否仍有效
  ↓
发送 / 丢弃
```

#### 3.5.1 职责边界

平台事件第一版最小分类：

```text
TEXT       普通文本
IMAGE      图片
STICKER    表情包 / emoji / 平台表情
POKE       拍一拍 / 戳一戳
RECALL     用户撤回
COMMAND    /mem、/forget 等显式命令
```

实时事件门负责：

- 平台事件进入后排序和归并。
- `/mem`、`/forget` 等显式命令分流。
- 用户撤回后，判断旧回复是否还能发送。
- LLM 生成期间用户又发消息时，判断旧结果是否过期。
- 主动消息准备发送时，判断是否和用户新消息撞车。
- 发送幂等检查，避免同一个结果重复发送。

实时事件门不负责：

- 判断用户情绪。
- 判断关系状态。
- 决定要不要安慰。
- 决定回复内容怎么写。
- 改写 LLM 生成的 `text`。
- 制造“像真人”的额外等待。

主回复 LLM 仍然负责输出：

```json
{
  "action": "REPLY",
  "text": "来了。怎么了？"
}
```

是否 `WAIT`、`REPLY`、`LIGHT_ACK`、`REACT`，仍然主要由主回复 LLM 的 `action` 决策决定。

#### 3.5.2 与 WAIT 的关系

`WAIT` 是软约束入口，不是实时事件门的硬规则替代品。

规则：

- `WAIT` 时，`items:null`，不发送任何可见内容。
- `WAIT` 不清空 buffer。
- `WAIT` 只表示“这一刻先不说”，不表示“这段用户输入已经处理完”。
- `REPLY`、`LIGHT_ACK`、`REACT` 等可见输出实际发出后，才算当前 snapshot 已回应。
- `WAIT` 返回后，事件门不应对同一个 snapshot 无限循环调用 LLM；它应等待新事件或后续明确触发。

示例：

```text
用户：我今天真的
LLM: { "action": "WAIT", "text": null }
buffer 保留：“我今天真的”

用户：很无语
下一次 LLM 看到：
我今天真的
很无语
```

这个设计保证 `WAIT` 的价值不会被硬规则吃掉。

#### 3.5.3 不做人造延迟

MVP 不做人造延迟。

不做：

```text
为了像人而等待 1.4-2.8 秒
模拟打字
模拟犹豫
固定随机 delay
```

原因：

- LLM 拿到内容后的思考时间本身就是自然 delay。
- 如果再加硬编码 delay，会削弱 `WAIT` 的软约束价值。
- 硬规则越多，越容易把“该不该回”从 LLM 手里抢走。

因此：

- LLM 结果一旦有效，立即发送。
- `hot_until` 这类参数只用于热聊状态生命周期，不用于制造每条消息的发送延迟。

#### 3.5.4 COLD / HOT 调用规则

COLD 和 HOT 的区别不靠额外 LLM 输出字段表达，而是由实时事件门在调用主回复 LLM 时决定传入什么上下文。

COLD 状态：

- 用户普通消息进入 buffer。
- 实时事件门可以立即调用主回复 LLM。
- 调用时附带冷启动元信息，例如 `timestamp`、`last_user_message_age`、`msg_index`、`status:COLD`。
- LLM 输出 `WAIT` 时，不发送文本、不清空 buffer、继续保持 COLD。
- LLM 输出可见动作时，发送有效内容，并进入 HOT。

HOT 状态：

- 不再传冷启动元信息。
- 只传普通聊天上下文。
- 用户继续发消息时，可以继续调用主回复 LLM。
- 仍然受 `WAIT`、`single-flight` 和 snapshot 过期作废机制保护。

HOT 回到 COLD：

- 由外部状态生命周期控制，例如一段时间无互动后退回 COLD。
- 这不是发送 delay，也不是回复判断。
- 用户不发消息时 LLM 不会被调用，因此这部分不能完全交给 LLM 自己判断。

#### 3.5.5 single-flight 与 snapshot 过期作废

同一会话同时最多一个主回复 LLM 任务。

MVP 采用：

```text
single-flight + snapshot 过期作废
```

基本规则：

```text
1. 用户消息进入 buffer。
2. buffer_version 递增。
3. 如果当前没有 pending job，用当前 snapshot 启动主回复任务。
4. COLD 状态启动任务时附带冷启动元信息；HOT 状态只传普通聊天上下文。
5. 如果主回复生成期间用户继续发消息，只更新 buffer，并标记当前 job stale。
6. 主回复返回后，事件门检查 result.snapshot_id 是否仍然等于当前 buffer_version。
7. 如果已过期，丢弃旧结果，不发送。
8. 丢弃后是否立刻用最新 buffer 重新提交，取决于当前状态和是否仍有未回应输入。
9. 如果仍有效，立即发送有效的 text。
```

这个机制不模拟犹豫，只防止旧上下文发出去。

`snapshot_id` 和 `msg_index` 分工：

- `snapshot_id`：工程 envelope 字段，用来做版本校验、过期作废和发送幂等，不进入 LLM 主输出 schema。
- `msg_index`：LLM 可见的冷启动语义提示，用来表示当天用户消息序号。
- 两者不能混用。

#### 3.5.6 可见输出与 buffer

可见输出才标记已回应。

规则：

- `items:null` 不清空未回应消息。
- `REPLY` 发出后，当前 snapshot 可标记为已回应。
- `LIGHT_ACK` 发出后，通常也可标记为已回应。
- `REACT` 如果是对用户上一条消息的完整轻回应，可以标记为已回应。
- 如果 `REACT` 是主动轻开场，则标记主动候选已使用，但不一定等同于清空聊天 buffer。

`REACT` 的细节和表情包系统强相关，后续可在表情包系统选型时细化。

#### 3.5.7 撤回静默取消

用户撤回不是需要被回复的聊天内容，而是对上下文的编辑动作。

规则：

- 普通消息被撤回后，从当前 buffer 中移除对应事件。
- buffer_version 递增。
- 相关 pending job 标记为 stale。
- 如果回复还没发出，旧结果直接丢弃。
- 默认不主动追问、不解释、不提及撤回内容。
- 如果系统已经根据被撤回消息发出回复，MVP 阶段不补救，避免进一步放大割裂感。
- `/mem`、`/forget` 这类显式命令一旦执行，不应因为平台撤回自动回滚；记忆修改必须继续通过显式命令或用户手动编辑完成。

#### 3.5.8 命令分流

`/mem`、`/forget` 等维护命令优先级最高。

规则：

- 命令不进入普通聊天 buffer。
- 命令直接走维护链路。
- 命令可以返回确定性维护反馈，例如“已存入记忆”。
- 命令一旦执行，不因平台撤回自动回滚。

#### 3.5.9 主动消息撞车

用户消息优先于主动消息。

规则：

- 如果主动消息正在准备发送，用户突然发来消息，主动消息取消或挂起。
- 不能出现用户刚开始新话题，系统还自顾自发昨天的主动话题。
- 被取消或挂起的主动候选如何标记，按 `TOMORROW_TOPICS.md` 的候选生命周期处理。

#### 3.5.10 发送幂等

发送幂等属于外部 envelope，不属于 LLM 输出 schema。

LLM 永远只输出：

```json
{
  "action": "REPLY",
  "text": "来了。怎么了？"
}
```

工程 envelope 可以包含：

```json
{
  "job_id": "job_20260614_001",
  "session_id": "user_001",
  "snapshot_id": 18,
  "buffer_version": 18,
  "llm_output": {
    "action": "REPLY",
    "text": "来了。怎么了？"
  }
}
```

发送层可以记录：

```text
job_id
snapshot_id
sent_at
platform_message_id
send_status
retry_count
```

发送前检查：

- `job_id` 是否已经发送过。
- `snapshot_id` 是否仍然有效。

同一个结果最多发送一次，避免网络重试或回调重复导致连发。

发送失败进入日志、调试界面或工程监控，不在自然聊天里解释“刚才发送失败”。

#### 3.5.11 拍一拍 / 戳一戳

拍一拍 / 戳一戳可以强制刷新注意力。

规则：

- 它可以让当前 buffer 立刻进入主回复 LLM。
- 它可以触发进入热聊在场态。
- 它不绕过 snapshot 校验。
- 它本身会被翻译成一条用户社交输入交给 LLM，而不是只做状态机变化。
- 如果没有其他未处理消息，LLM 通常应该给一个短在线回应或自然追问，例如“在”“怎么了”。
- 工程层不硬编码固定回复文本；最终仍由 LLM 在 `LIGHT_ACK`、`REPLY` 或 `REACT` 中选择自然表达。
- 用户频繁拍一拍 / 戳一戳时，产品层不做硬冷却，由 prompt / `SOUL.md` 和主回复 LLM 软处理。
- 工程层只做重复事件去重，避免平台重复回调或网络重试导致多次触发。

## 4. 强制看手机：拍一拍 / 戳一戳

离线生活态需要一个用户可理解的强制激活入口。否则用户真正想被立刻关心时，会觉得系统冷淡或不可控。

推荐交互：借鉴 QQ、微信中的“拍一拍 / 戳一戳”。

规则：

- 用户普通消息进入缓冲。
- 用户“拍一拍 / 戳一戳”后，系统强制刷新注意力。
- 如果缓冲区已有消息，角色批量阅读后回应。
- 如果缓冲区没有未处理消息，拍一拍 / 戳一戳本身也会作为用户输入交给 LLM，生成类似“在”“怎么了”的在线提示或自然短回应。
- 触发后由实时事件门进入一段有限时长的热聊在场态，例如 10-30 分钟。
- 热聊态内仍保留回合闭合判断，避免逐条抢答。
- 如果用户频繁触发，不做产品层硬冷却，由 prompt / `SOUL.md` 软处理；工程层只做重复事件去重。

示例：

```text
用户：我今天真的很无语
系统处于离线生活态，转发层暂不发送 text。

用户：拍一拍
系统立即进入热聊在场态，读取缓冲消息，并允许生成可见回复。

角色：来了。怎么了？
```

这个动作比“立刻回复我”更符合聊天语境。它既保留角色不总在线的生活感，又允许用户在需要即时关心时主动拉近距离。

## 5. 微信/QQ 中的存在感表现

存在感系统不应该直接以聊天消息的方式暴露。

不要发送：

```text
当前状态：离线生活态
当前状态：热聊在场态
正在等待用户继续输入
```

这些都非常人机。

存在感应该通过行为表现：

- 回复时机：多久看、多久回、是否批量回。
- 回复形态：短句、表情、语气词、完整回复。
- 触发动作：拍一拍、戳一戳、连续消息。
- 自然解释：刚看到、来了、你前面那几条我一起看了。

内部可以有状态，但用户看到的是聊天行为差异，而不是状态标签。

## 6. 记忆系统总原则

记忆系统必须简单、可读、可维护。

核心判断：

- 不优先做向量库或 BM25 检索。
- 不在普通聊天中频繁询问“要不要记住”。
- 不把所有闲聊自动固化成长期记忆。
- 不把当天情绪固化成永久人格标签。
- 永久核心记忆必须能被用户直接查看和编辑。
- 普通聊天中产生的当天信息先进入 `dm/YYYY-MM-DD.md`，不直接写入 `MEMORY_CORE.md`。
- `dm/` 不只是当天摘要目录，也是未来记忆检索系统的历史档案基础。

记忆的主要价值不是展示“我记得”，而是改变行为。

好的使用：

```text
又被他说了啊。你先骂，我听着。
```

不好的使用：

```text
根据我的记忆，你不喜欢我立刻给建议。
```

记忆应该像熟悉感，不应该像审计日志。

## 7. 记忆文件结构

采用两层 Markdown 文件：

```text
MEMORY_CORE.md
dm/
  2026-06-13.md
  2026-06-14.md
```

含义：

- `MEMORY_CORE.md`：永久核心记忆，只有一个，会被读入上下文，因此必须节省 token。
- `dm/YYYY-MM-DD.md`：每日记忆文件。当天文件从创建时就使用日期命名，例如 `dm/2026-06-13.md`。
- `dm/`：每日记忆目录。目录名要短，来源引用使用相对路径，例如 `dm/2026-06-13.md`。

每日流程：

```text
dm/YYYY-MM-DD.md
  ↓
凌晨 3-4 点异步整理
  ↓
整理并更新 MEMORY_CORE.md
  ↓
日期切换后生成新的 dm/YYYY-MM-DD.md
```

每日文件不写“待整理候选”。当天文件只负责记录当天发生了什么，是否进入永久核心记忆由每日结束时的整理线程判断。

## 8. MEMORY_CORE.md 设计

`MEMORY_CORE.md` 是长期上下文的一部分，所以必须短、稳、可读。

建议结构：

```md
# 永久核心记忆

## 用户明确相处偏好
- [/mem]: 用户情绪很差时，不要用总结式安慰，先陪一会儿。

## 重要事实
- [dm/2026-06-13.md]: 用户的猫叫 xxx。

## 用户交际圈
- [dm/2026-06-13.md]: 老板是近期工作压力来源之一。

## 相处习惯
- [/mem]: 用户连续倾诉时，优先等待他说完，不要逐条抢答。

## 临时近期状态
- [dm/2026-06-13.md]: 用户最近工作压力较高。
```

格式规则：

```text
[来源]: 内容
```

约束：

- 每条记忆只保留必要内容。
- 每条记忆采用 `[来源]: 内容` 的单行格式。
- 不单独记录更新时间，因为每日文件本身带日期，时间可以从来源文件判断。
- `/mem` 可以作为来源。
- `dm/YYYY-MM-DD.md` 使用相对路径作为来源。
- `MEMORY_CORE.md` 可由用户直接编辑。

优先级：

```text
/mem 明确写入 > 用户直接编辑 MEMORY_CORE.md > 凌晨整理线程自动整理 > 模型临场判断
```

## 9. 每日日期文件设计

`dm/YYYY-MM-DD.md` 可以比 `MEMORY_CORE.md` 更细，因为它不会长期全部读入上下文，而是作为当天上下文和整理材料。

建议结构：

```md
# 每日记忆

## 今日大事
- 用户今天被老板在会上点名，明显很委屈。

## 重要事实
- 用户最近在准备一个方案。

## 相处习惯
- 用户今晚明确表示不想听方案，只想先吐槽。

## 临时近期状态
- 今天工作压力高。
- 晚上更想被陪着吐槽，不想听分析。
```

说明：

- `今日大事` 是每日日期文件相对 `MEMORY_CORE.md` 的额外部分。
- 每日日期文件不设置独立的“用户交际圈”分区。用户可以在“今日大事”“重要事实”“临时近期状态”等部分自然描述事件。
- 凌晨整理时，再从日期文件的事件描述中提取可能进入 `MEMORY_CORE.md` 的用户交际圈信息。
- `用户明确相处偏好` 主要进入 `MEMORY_CORE.md`，通常由 `/mem` 或用户直接编辑产生。
- 每日日期文件不放“待整理候选”。
- 每日日期文件不做 episode 化，不按对话片段拆成 `summary`、`user_events` 等复杂字段。
- 每日日期文件只保留当天可读摘要结果；中间分析字段不需要落盘。
- 凌晨整理线程再从每日日期文件中提取最终结果：未闭合事件进入 `TOMORROW_TOPICS.md` 的“未闭合话题”，短期避免重复进入“昨日记忆”，长期稳定内容进入 `MEMORY_CORE.md`。

## 10. 记忆相关定时任务

当前不采用实时的“后台异步记忆线程”。原因是正常情感聊天一天很难超过模型可读取的安全上下文范围，没有必要每条消息都实时整理成记忆。实时整理会增加工程复杂度、文件冲突和误记风险。

当前采用两类定时任务：定时记忆分析任务、凌晨整理线程。二者职责不同，不能混用。

职责表：

| 任务 | 主要职责 | 维护对象 |
| --- | --- | --- |
| 定时记忆分析任务 | 定时读取当天聊天上下文，整理当天可读记忆和明日未闭合话题 | `dm/YYYY-MM-DD.md`、`TOMORROW_TOPICS.md` 的“未闭合话题” |
| 凌晨整理线程 | 每天凌晨做收束、合并、校验和长期化 | `MEMORY_CORE.md`、`TOMORROW_TOPICS.md` 的“昨日记忆”，并校验“未闭合话题” |

### 10.1 凌晨整理线程

凌晨整理线程每日大约凌晨 3-4 点启动。

选择这个时间段的原因：

- 用户活跃度通常较低。
- 更接近自然日结束后的整理。
- 不打断白天或夜间正常聊天。

基本规则：

- 凌晨 3-4 点启动。
- 整理过程不打断正常聊天。
- 如果整理时用户仍在热聊，可以延后到空闲窗口。
- 整理失败时保留当日日期文件，下次重试，避免丢失当天记忆。
- 当天文件已经位于 `dm/` 且已经用日期命名，因此日终不需要移动或改名。

凌晨整理线程职责：

- 从当天记忆中提取少量值得进入 `MEMORY_CORE.md` 的内容。
- 维护 `MEMORY_CORE.md`。
- 为 `TOMORROW_TOPICS.md` 补充“昨日记忆”部分；这是凌晨整理线程的 prompt 设计层问题，不新增独立模块。
- 校验 `TOMORROW_TOPICS.md` 中“未闭合话题”是否仍然成立、是否应该删除或降权。
- 合并重复记忆。
- 修改已经变化的近期状态。
- 删除或弱化过期的临时近期状态。
- 保留来源路径，方便用户追溯。
- 日期切换后生成新的空白 `dm/YYYY-MM-DD.md`。

凌晨整理线程不应该把所有闲聊都转成长期记忆。长期核心记忆应该克制，尤其要避免把当天情绪固化成用户人格。

### 10.2 定时记忆分析任务

定时记忆分析任务负责在一天内少量几次读取当天聊天上下文，整理当天正在发生的内容。

基本判断：

- 不需要实时后台线程持续监听每条消息。
- 一天最多可以先按两次定时分析任务设计，例如日间 / 晚间各一次。
- 如果当天聊天量很低，可以跳过或只运行一次。
- 具体触发时间和次数后续结合工程实现再定。

主要职责：

- 维护当日 `dm/YYYY-MM-DD.md`。
- 从当天聊天上下文中整理今日大事、重要事实、相处习惯、临时近期状态。
- 维护 `TOMORROW_TOPICS.md` 中的“未闭合话题”。
- 不直接维护 `MEMORY_CORE.md`。
- 不负责生成 `TOMORROW_TOPICS.md` 中的“昨日记忆”。

定时记忆分析任务的具体架构选型见 `开发计划.md` TODO / Delay。

## 11. TOMORROW_TOPICS.md 设计

主动消息不应该做成提醒工具。用户如果需要“明天十点提醒我”，更适合交给手机助手或日程工具，而不是情感陪伴机器人。

情感陪伴里的主动消息更接近朋友式回访和自然联系，核心来源是未闭合话题。

建议维护一个 `TOMORROW_TOPICS.md`：

```text
TOMORROW_TOPICS.md
```

它的定位：

- 只关心“明天可以发什么主动消息给用户”。
- 主要由定时记忆分析任务维护。
- 凌晨整理线程只负责生成“昨日记忆”部分，并校验“未闭合话题”部分。
- 不需要每日存档，只维护当前一个文件。
- 第二天主动消息模块读取它，选择是否发送、何时发送、怎么发送。
- 发送后可以删除、标记已使用，或由下一次定时记忆分析任务后续覆盖。

建议结构：

```md
# 明日话题

**未闭合话题**
- [pending] [dm/2026-06-14.md]: 用户明天要和老板谈今天会上被点名的事，优先轻开场，例如“今天那事还顺利吗？”

**昨日记忆**
- [pending] [dm/2026-06-14.md]: 情绪余波：用户昨天工作压力高，晚上只想吐槽，不想听分析，今天如果提起，先短句陪，不要展开分析。
- [blocked] [dm/2026-06-14.md]: 短期避免重复：昨晚已经围绕睡眠安慰过一次，今天不要主动再催睡。

**生活感消息备选**
- [pending] [system]: 可以从角色视角轻轻提起“昨天你说的那件事”，但不要伪装成强烈想念或情绪召回；优先一句短话或 `REACT`。
```

三个部分的含义：

- `未闭合话题`：用户明确提到未来会发生、还没有结果的事情。优先级最高。
- `昨日记忆`：从昨日 `dm/YYYY-MM-DD.md` 中提取的轻量残留摘要，用于第二天自然衔接和避免重复。
- `生活感消息备选`：更像角色自然轻轻出现的备选内容，风险最高，只作为低优先级候选，不应高频使用。

维护职责：

- 定时记忆分析任务：维护当日 `dm/YYYY-MM-DD.md`，并维护 `TOMORROW_TOPICS.md` 的“未闭合话题”。
- 凌晨整理线程：维护 `MEMORY_CORE.md`，为 `TOMORROW_TOPICS.md` 补“昨日记忆”，并校验“未闭合话题”是否仍然成立、是否应该删除或降权。
- `生活感消息备选` MVP 阶段不自动维护；可由用户手动写入，或由 `SOUL.md` / 设置显式启用后再参与生成。

昨日记忆的生成规则：

- “昨日记忆”属于凌晨整理线程的 prompt 设计层规则，不新增独立模块。
- 它不是复述昨日聊天，而是提炼第二天仍可能影响自然相处的残留信息。
- 重点保留：情绪余波、可自然承接的轻量上下文、短期避免重复的内容。
- 明确的未来事件、待发生事项和未出结果事项，应进入“未闭合话题”，不要塞进“昨日记忆”。
- 不引入梦境碎片、角色日程残留、身体状态模拟或文学化意象。
- 它服务主动消息克制，不服务剧情延续。

原则：

- 不把用户明确提醒需求作为主动消息主来源。
- 不做“十点提醒我”这类工具型任务。
- 不用用户脆弱点做召回。
- 不用“我想你了”“你怎么不理我”这类情绪绑架式文案。
- 主动消息不默认强制进入热聊；用户回复或拍一拍后再进入热聊在场态。
- `TOMORROW_TOPICS.md` 是候选池，不代表第二天一定要发送。
- 当“未闭合话题”和“昨日记忆”都没有可用候选时，`生活感消息备选` 可以作为主动消息候选，但仍然必须低频、低压、可忽略。
- `生活感消息备选` 的边界主要通过 prompt 软约束处理，不新增硬规则状态机。
- prompt 应约束其不得表达强烈想念、责备、不满、等待感，不得伪造真实生活经历，不得利用用户脆弱点。

候选生命周期：

- `TOMORROW_TOPICS.md` 不是主动推送队列，而是明日自然续话素材池。
- 生命周期只用于避免重复、过期和压力感，不用于运营式召回。
- MVP 只保留四种状态：`pending`、`used`、`expired`、`blocked`。
- `pending`：可用，等待合适时机。
- `used`：已经实际发出过。
- `expired`：话题已过期，不再使用。
- `blocked`：暂时不要用，例如用户没接、语境不合适、情绪风险较高。
- 单条候选建议使用 `[状态] [来源]: 内容`，保持 Markdown 可读。
- 候选不是最终要发送的消息，而是供主回复 LLM 或主动消息发送模块参考的素材。
- 不引入 `score`、`repeat_count` 或复杂排序系统。
- `TOMORROW_TOPICS.md` 中的候选可以广泛采用轻开场写法。
- 轻开场不是为了诱导用户回复，而是为了降低主动消息的压迫感。

主动消息发送规则：

- 判断当前能不能发主动消息。
- 从 `TOMORROW_TOPICS.md` 里选择可见候选给 LLM。
- 在主动消息实际发出后，把候选标记为 `used`。
- 在候选过期、用户未接、语境不合适时，把候选标记为 `expired` 或 `blocked`。

主回复 LLM 职责：

- 决定是否真的使用候选。
- 决定如何自然表达。
- 可以选择轻开场、`REACT`、普通 `REPLY`，也可以放弃使用候选。
- 主动消息里的轻开场仍通过 `REPLY` 或 `REACT` 表达，不为此新增 action。

MVP 发送门槛：

- 用户当前没有未处理输入。
- 今日主动消息未超过上限。
- 候选状态是 `pending`。

## 12. 记忆记录结构

后台记录分成两条链路：存档链路和记忆整理链路。

```text
聊天事件流
  ├─ raw chat log：存档、审计、测评、回放
  └─ dm/YYYY-MM-DD.md：当天可读摘要
       ↓
     MEMORY_CORE.md：长期核心记忆
```

记忆整理链路：

```text
当天聊天上下文
  ↓
定时记忆分析任务
  ↓
dm/YYYY-MM-DD.md
  ↓
凌晨整理线程
  ↓
MEMORY_CORE.md
```

`raw chat log` 是旁路存档，不是 `dm/YYYY-MM-DD.md` 的上游。

### 12.1 raw chat log

`raw chat log` 是原始聊天日志。

它的定位：

- 供存档、审计、测评、回放使用。
- 可以记录结构化输入、模型 JSON 输出、转发结果、时间戳、触发事件等，但具体字段当前不重要。
- 可以人类可读性较差，因为它不是产品记忆界面。
- 不直接注入常规对话上下文。
- 不承担“用户可维护记忆”的职责。
- 不参与常规记忆整理。
- 除测评、审计、回放等场景外，系统不主动读取它。

说明：

- `raw chat log` 可以是 JSONL、数据库记录或其他机器友好格式。
- 它不会占用太多存储，保留价值高于存储成本。
- 它和 `dm/YYYY-MM-DD.md` 分工不同：前者保存事实流水，后者保存当天可读摘要。

### 12.2 dm/YYYY-MM-DD.md

`dm/YYYY-MM-DD.md` 是当天可读摘要，不是原始日志。

它的定位：

- 定时记忆分析任务从当天聊天上下文中整理出重要事件、事实、相处习惯和近期状态。
- 支撑当天和近期对话连续性。
- 作为凌晨整理线程更新 `MEMORY_CORE.md` 的材料。
- 作为未来记忆检索系统的历史档案基础。
- 保持 Markdown 可读，方便用户查看和理解。

### 12.3 MEMORY_CORE.md

`MEMORY_CORE.md` 是长期核心记忆。

它的定位：

- 被读入常规对话上下文。
- 必须短、稳、可维护。
- 只保留长期有价值或近期仍然重要的信息。
- 由 `/mem`、用户直接编辑和凌晨整理线程共同维护。

## 13. /mem 与 /forget

`/mem` 和 `/forget` 是显式身份切换入口。

它们的价值不是技术上的命令行，而是心理上的模式切换：用户从“关系对话模式”切到“设定维护模式”，不再需要假装自己仍在和一个真实朋友自然沟通。

示例：

```text
/mem 记住：我心情差的时候不要立刻给建议，先陪我待一会儿。
已存入记忆。
```

```text
/mem 修改：把“我喜欢被催睡觉”改成“我不喜欢被催睡觉”。
已存入记忆。
```

```text
/forget 忘记：关于我前任的那条记忆。
已从记忆中移除。
```

规则：

- `/mem` 拥有最高优先级。
- `/mem` 通常写入或修改 `MEMORY_CORE.md`。
- `/forget` 用于删除或弱化不该继续影响行为的记忆。
- 维护模式中可以给确定性反馈，例如“已存入记忆”。
- 关系模式中不要长篇解释“以后我会怎样改”。

## 14. 修复机制

修复机制必须和 `/mem` 互补，不能无限扩张。

用户说：

```text
这句不像你
```

关系模式里适合短回应：

```text
嗯，这句我撤回。
```

如果用户希望长期改变行为，应进入维护模式：

```text
/mem 记住：我情绪很差的时候，不要用总结式安慰。
已存入记忆。
```

原则：

- 关系模式少解释。
- 维护模式给确定反馈。
- 长期行为改变优先通过 `/mem` 写入。
- 不用“以后类似场景我会……”这类看似自然但很人机的长说明。

## 15. 暂不启用 BM25 记忆检索，但为检索预留结构

观察到市面上很多产品仍然使用 BM25 或类似检索排序做记忆系统。这类方案可能有道理，尤其在记忆规模很大、上下文无法完整放入模型时。

当前阶段暂不优先采用 BM25。

理由：

- 目标是保持 `MEMORY_CORE.md` 可读、可维护、可直接注入上下文。
- 永久核心记忆会被主动压缩，不应膨胀到必须检索。
- 每日历史文件保存在 `dm/`，先作为可追溯材料和未来检索语料，而不是当前实时检索库。
- 当前方案扩展性尚可，可以等遇到真实问题再启用检索。

但 `dm/` 必须保留，因为它为未来记忆检索系统做准备。`MEMORY_CORE.md` 只适合保存长期核心记忆，不能承载所有历史事件。用户如果一个月后重提某个重要事件，仅依赖 `MEMORY_CORE.md` 很可能不够，需要从 `dm/` 历史文件中召回。

未来检索路径可以是：

```text
常规聊天：MEMORY_CORE.md + 近期 dm
回忆旧事：检索 dm/ 历史文件
必要精查：读取 raw chat log
```

后续如果出现这些问题，再考虑 BM25 或混合检索：

- `MEMORY_CORE.md` 过长。
- 历史事件召回不足。
- 跨日期搜索困难。
- 用户要求查询很久以前的具体事情。

当前原则：先用简单 Markdown 结构跑通长期陪伴，不提前引入检索复杂度。

## 16. SOUL.md

角色稳定偏好、边界、说话习惯可以放在 `SOUL.md` 中。

这个方向可以参考 openclaw 一类已有方案，不需要重新发明复杂人格系统。

`SOUL.md` 承载：

- 角色稳定偏好。
- 角色边界。
- 说话习惯。
- 不喜欢或不会做的事。
- 与用户相处时的长期风格。

原则：

- 角色不能永远无条件服从。
- 角色风格要可读、可改、可审查。
- 不要让人格完全依赖临场 prompt 发挥。
- `SOUL.md` 和 `MEMORY_CORE.md` 分工不同：前者描述角色，后者描述用户记忆和相处习惯。

## 17. 主动消息

主动消息非常重要，但不能做成提醒工具或留存召回工具。

已确认设计：

- 主动消息对关系感很重要。
- 主动消息最容易变成骚扰、召回和情绪操控。
- 主动消息主要来自 `TOMORROW_TOPICS.md`。
- `TOMORROW_TOPICS.md` 主要由定时记忆分析任务维护。
- 凌晨整理线程只帮助生成“昨日记忆”并校验“未闭合话题”。
- 主动消息优先来自未闭合话题，其次是昨日记忆；如果二者都没有可用候选，生活感消息备选也可以作为低优先级候选。
- 用户提出的工具型提醒不作为主动消息主来源。
- 不用“我想你了”“你怎么不理我”这类情绪绑架式文案。
- 不用用户脆弱点做留存。
- `TOMORROW_TOPICS.md` 不是主动推送队列，而是明日自然续话素材池。
- 主动候选生命周期只用于避免重复、过期和压力感。
- MVP 只保留 `pending / used / expired / blocked` 四种状态。
- `TOMORROW_TOPICS.md` 候选可以广泛采用轻开场，避免一次性把话题讲满。
- 用户必须能控制频率和关闭方式。
- 主动消息不默认强制进入热聊。
- 用户回复主动消息或拍一拍后，再进入热聊在场态。

主动消息发送 MVP 规则：

- 普通对话不做人造延迟；主动消息必须有时间窗口和限额，因为主动消息本质上是打扰。
- 主动消息发送模块只做低频、防打扰、防重复的硬限制，不做复杂运营策略。
- 默认每日主动消息上限为 1 条。
- 用户显式调高后，最多 2-3 条。
- 默认免打扰时段为深夜和清晨，例如 00:00-09:00 不主动发消息。
- 免打扰只限制主动消息；如果用户在该时段主动发消息，仍然按普通回复处理。
- 如果主动消息发出后用户没有回复，同一天不再发送第二条主动消息。
- 用户未接的主动候选应标记为 `used` 或 `blocked`，不追问、不补发、不解释。
- 主动消息候选优先级：`未闭合话题` > `昨日记忆` > `生活感消息备选`。
- `生活感消息备选` 风险最高，MVP 阶段默认克制使用；当未闭合话题和昨日记忆均无可用候选时，可以低频作为主动消息候选。
- `生活感消息备选` 的边界交给 prompt 软约束，不新增硬规则模块。

轻开场规则：

- 主动消息支持轻开场，尤其适合 `TOMORROW_TOPICS.md` 里的未闭合话题和昨日记忆。
- 轻开场的目的不是提高回复率，而是降低主动消息压力。
- 不新增 `suspended` 状态；MVP 继续只使用 `pending / used / expired / blocked`。
- 轻开场发出后，该候选即视为已使用，可标记为 `used`。
- 用户没接就停止，不自动追发、不解释。
- 用户接了，再把相关候选作为普通聊天上下文自然承接。
- 主动消息里的轻开场通过 `REPLY` 或 `REACT` 表达；`LIGHT_ACK` 仍主要表示对用户消息的短文本轻回应。

主动消息示例：

```json
{
  "action": "REPLY",
  "text": "你今天跟他说了吗？"
}
```

这里的 `REPLY` 是对用户发起的一条主动消息，不代表系统已经进入热聊。热聊状态由用户后续回复、拍一拍 / 戳一戳或实时事件门根据状态规则判断。

## 18. 轻交互

轻交互非常重要，尤其是表情包系统。

已确认设计：

- 轻交互可以降低“每次都必须生成一段文本”的压力。
- 轻交互让对话更像真实聊天。
- `REACT` 必须存在，且直接指表情包动作。
- 用户会发表情包，系统不能只分析表情包后用语言描述回复。
- 系统也必须能发送表情包。
- `LIGHT_ACK` 是短文本轻回应。
- `REACT` 是表情包回应。
- 拍一拍、戳一戳属于低负担触发动作。

`REACT` 示例：

```json
{
  "action": "REACT",
  "text": "🙂"
}
```

```json
{
  "action": "REACT",
  "text": "emoji/smile.png"
}
```

说明：

- `REACT.items` 可以包含 ASCII emoji。
- `REACT.items` 也可以包含本地表情图资源 ID 或 `search_meme` 内部检索项。
- 平台一定要支持表情包系统，这点不作为不确定项。
- 表情系统选型见 `开发计划.md` TODO / Delay，后续单独研究优秀项目。
- 具体表情资源管理、检索、匹配、发送格式也在表情包系统选型中解决。

## 19. 情绪安全与回应边界

当前暂不做安全状态机。

情绪安全和情绪回应边界都先作为系统提示词中的基础准则，不做单独模块。

基础准则：

```text
可以同情，可以陪伴，可以承认用户的痛苦，但任何时候不能引导用户走向更负面的情绪。
```

情绪回应不采用固定“三段式模板”。例如“接住情绪 -> 贴近语境 -> 开放后续”这种结构虽然看起来合理，但容易让角色变成心理咨询师、客服或公众号式安慰。

更合适的做法是把反模式写进系统提示词，让模型避免明显的人机味：

```text
情绪回应边界：
- 不要立刻分析原因。
- 不要立刻给建议。
- 不要总结式命名用户情绪，例如“我能感受到你现在很……”
- 不要上价值，不要教育用户。
- 不要把用户推向更负面的情绪。
- 不要为了显得温柔而堆叠亲昵称呼。
- 用户连续倾诉时，优先 WAIT 或 LIGHT_ACK，不要抢着长回复。
- 如果需要回应，优先短句、贴近当前语境、像真实聊天一样接住对方。
```

说明：

- 这条约束和模型自身安全训练方向一致。
- 当前阶段作为系统提示词级别的弱约束处理。
- 不在本阶段设计单独的安全状态机。
- 不把安全逻辑做成突然割裂人格的强切换体验。
- 不单独设计“情绪回复模板模块”。这部分只作为系统提示词模板的一部分存在。
- 正向说话风格主要交给 `SOUL.md`、`MEMORY_CORE.md` 和当前上下文决定，不写死成统一模板。

## 20. 当前暂不讨论测评

测评体系重要，但当前架构还没有完全搭好，暂时不单独写测评文档。

后续等以下模块稳定后再设计测评指标：

- 注意力与转发 JSON。
- 拍一拍 / 戳一戳激活。
- `MEMORY_CORE.md`。
- `dm/YYYY-MM-DD.md`。
- 凌晨整理线程。
- `TOMORROW_TOPICS.md`。
- `SOUL.md`。
- 主动消息和轻交互规则。

## 21. 一句话原则

这个产品的核心不是让 LLM 更会回答，而是让系统更会相处。
